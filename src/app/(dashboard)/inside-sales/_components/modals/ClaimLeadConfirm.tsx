// Moved to src/components/leads/ClaimLeadConfirm.tsx (B3: shared with the ASM
// queue). Re-exported here so existing imports keep working.
export { ClaimLeadConfirm } from "@/components/leads/ClaimLeadConfirm";
