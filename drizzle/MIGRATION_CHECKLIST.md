# Migration Checklist

There is **no automatic migration runner** in this project. Each `E-<n>_*.sql` file
in `drizzle/` must be applied **by hand to every database**, because `DATABASE_URL`
points at different AWS RDS instances per environment and they drift independently.
(Migrations silently stopped applying on prod around ~E-145 — this checklist exists
to stop that recurring.)

## Environments

| Env | DB host (RDS) | Notes |
|-----|---------------|-------|
| **production** | `database-2.…ap-south-1.rds.amazonaws.com/postgres` | **This is prod** — what `crm.itarang.com` serves. Long mislabelled "local / dev" here because `.env.local` can be pointed at it |
| **sandbox** | `database-1.…ap-south-1.rds.amazonaws.com/postgres` | **This is sandbox** — what `sandbox.itarang.com` serves. ALSO the usual `.env.local` target, so "local / dev" and "sandbox" are one database |

> ⚠ **`database-2` is production.** Verified 2026-07-29 two ways: its `zoho_invoices`
> matched crm.itarang.com's CEO dashboard exactly while db-1 did not, and its
> `zoho_sync_state.last_run_at` tracks the live prod app's sync ticker to the minute.
> So the `local (db-2)` and `prod` columns below are the SAME database — which is why
> `prod` sat `☐` for ~70 migrations that were in fact applied. A migration is only
> genuinely unapplied on prod if BOTH are `☐`. Never point `db:push` at it.
>
> ⚠ **`database-1` is sandbox.** Corrected 2026-07-30. This file previously said
> db-1 was "nobody's deployment — a personal dev DB" and that sandbox had "its own
> RDS, genuinely separate". **Both claims are false.** Verified from the VPS: the
> live `sandbox-web` pm2 process (user `itarang-sandbox`) runs out of
> `releases/<sha>/standalone`, whose `.env` is a symlink to `shared/.env`, and that
> file's `DATABASE_URL` resolves — via `psql \conninfo` — to
> `database-1.…rds.amazonaws.com`. The same check against `crm.itarang.com`'s
> release returns `database-2`, so the two apps are correctly separated; it is the
> LABELS here that were wrong. The earlier "db-1 sat 40.9h stale through a sandbox
> deploy" observation was a deploy that didn't write to the DB, not evidence of a
> third instance.
>
> So the columns pair up: `local (db-2)` = `prod` = **database-2**, and
> `db-1` = `sandbox` = **database-1**. There are only TWO databases, not four.
> A migration ticked in `db-1` is thereby already on sandbox — which is why the
> five E-215..E-219 security rows sat `☐` under sandbox while in fact being live
> there since 2026-07-29/30. Tick both boxes of a pair together.

## How to apply one migration

All migrations are additive + idempotent (`ADD COLUMN IF NOT EXISTS`, etc.), so
re-running is a safe no-op. **Do not use `npm run db:push`** against any shared DB.

Paste the file's SQL into your DB client (DBeaver / psql / Supabase SQL editor), OR
(point `.env.local`'s `DATABASE_URL` at the target env first):

```bash
node -e "require('dotenv').config({path:'.env.local'}); const fs=require('fs'); const {Client}=require('pg'); const c=new Client({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}}); c.connect().then(()=>c.query(fs.readFileSync('drizzle/E-176_consent_signer_name_score.sql','utf8'))).then(()=>{console.log('applied'); return c.end();}).catch(e=>{console.error(e.message);process.exit(1);})"
```

## Legend

- ☐ = unverified / not confirmed applied in that env
- ✅ = confirmed present (checked via `information_schema` / `\d <table>`)

**Backfill note:** every row below was seeded as ☐ because actual per-env state was
never tracked. Migrations ≤ E-174 are *assumed* applied on dev historically but are
**unverified** — when you confirm one (or hit a "relation/column does not exist"
error and fix it), tick the box so the record becomes real over time. Prod is the
least trustworthy column (drift began ~E-145).

## Migrations

| Migration | What it adds | local (db-2) | db-1 | sandbox | prod |
|-----------|--------------|:---:|:---:|:---:|:---:|
| E-002_nbfc_portal_credentials | nbfc portal credentials | ☐ | ☐ | ☐ | ☐ |
| E-012_dealer_nbfc_assignments | dealer nbfc assignments | ☐ | ☐ | ☐ | ☐ |
| E-026_loan_sanctions_nbfc_lifecycle | loan sanctions nbfc lifecycle | ✅ | ☐ | ☐ | ☐ |
| E-026B_nbfc_tenant_bridge | nbfc tenant bridge | ✅ | ☐ | ☐ | ☐ |
| E-027_telemetry_ingestion_log | telemetry ingestion log | ☐ | ☐ | ☐ | ☐ |
| E-029_emi_schedules | emi schedules | ☐ | ☐ | ☐ | ☐ |
| E-030_pci_nightly_computation | pci nightly computation | ☐ | ☐ | ☐ | ☐ |
| E-046_telemetry_ingestion | telemetry ingestion | ☐ | ☐ | ☐ | ☐ |
| E-049_telemetry_alerts | telemetry alerts | ☐ | ☐ | ☐ | ☐ |
| E-050_telemetry_schema_apply | telemetry schema apply | ☐ | ☐ | ☐ | ☐ |
| E-065_ecosystem_metrics_cache | ecosystem metrics cache | ✅ | ✅ | ☐ | ☐ |
| E-066_nbfc_anomaly_flags | nbfc anomaly flags | ☐ | ☐ | ☐ | ☐ |
| E-067_nbfc_risk_rules | nbfc risk rules | ☐ | ☐ | ☐ | ☐ |
| E-080_compliance_screen | compliance screen | ✅ | ✅ | ☐ | ☐ |
| E-082_dual_approval | dual approval | ☐ | ☐ | ☐ | ☐ |
| E-083_nbfc_immobilisation_actions | nbfc immobilisation actions | ☐ | ☐ | ☐ | ☐ |
| E-084_loan_restructures | loan restructures | ☐ | ☐ | ☐ | ☐ |
| E-085_risk_rule_thresholds | risk rule thresholds | ☐ | ☐ | ☐ | ☐ |
| E-089_pii_access_grants | pii access grants | ☐ | ☐ | ☐ | ☐ |
| E-090_nbfc_consent_dpdpa | nbfc consent dpdpa | ☐ | ☐ | ☐ | ☐ |
| E-092_score_explainability | score explainability | ☐ | ☐ | ☐ | ☐ |
| E-093_auction_auto_bids | auction auto bids | ☐ | ☐ | ☐ | ☐ |
| E-103_product_selections_model_number_rename | product selections model number rename | ☐ | ☐ | ☐ | ☐ |
| E-104_scraper_runs_skipped_invalid_phone | scraper runs skipped invalid phone | ☐ | ☐ | ☐ | ☐ |
| E-105_dealer_leads_provider_column | dealer leads provider column | ☐ | ☐ | ☐ | ☐ |
| E-106_dealer_leads_region_hierarchy | dealer leads region hierarchy | ☐ | ☐ | ☐ | ☐ |
| E-106_dealers_backfill_from_approved_applications | dealers backfill from approved applications | ☐ | ☐ | ☐ | ☐ |
| E-106_expense_ai_fields | expense ai fields | ☐ | ☐ | ☐ | ☐ |
| E-107_backfill_dealer_leads_city_from_raw_address | backfill dealer leads city from raw address | ☐ | ☐ | ☐ | ☐ |
| E-107_nbfc_docs_verified | nbfc docs verified | ✅ | ☐ | ☐ | ☐ |
| E-108_location_reference_tables | location reference tables | ☐ | ☐ | ☐ | ☐ |
| E-108_nbfc_created_by_auth | nbfc created by auth | ✅ | ☐ | ☐ | ☐ |
| E-109_dialer_campaigns | dialer campaigns | ☐ | ☐ | ☐ | ☐ |
| E-109_nbfc_lsp_agreement_signers | nbfc lsp agreement signers | ☐ | ☐ | ☐ | ☐ |
| E-110_call_costs | call costs | ☐ | ☐ | ☐ | ☐ |
| E-110_nbfc_lsp_agreement_template_url | nbfc lsp agreement template url | ☐ | ☐ | ☐ | ☐ |
| E-111_drop_ai_call_logs_lead_id_fkey | drop ai call logs lead id fkey | ☐ | ☐ | ☐ | ☐ |
| E-111_nbfc_correction_rounds | nbfc correction rounds | ☐ | ☐ | ☐ | ☐ |
| E-112_dealer_leads_part0_columns | dealer leads part0 columns | ☐ | ☐ | ☐ | ☐ |
| E-112_nbfc_lsp_agreement_signer_status | nbfc lsp agreement signer status | ☐ | ☐ | ☐ | ☐ |
| E-113_lead_touchpoints | lead touchpoints | ☐ | ☐ | ☐ | ☐ |
| E-113_loan_products_scheme_highlights | loan products scheme highlights | ☐ | ☐ | ☐ | ☐ |
| E-114_lead_visits | lead visits | ☐ | ☐ | ☐ | ☐ |
| E-114_loan_products_active_locations | loan products active locations | ☐ | ☐ | ☐ | ☐ |
| E-115_lead_escalations | lead escalations | ☐ | ☐ | ☐ | ☐ |
| E-115_loan_products_cibil_range | loan products cibil range | ☐ | ☐ | ☐ | ☐ |
| E-116_dealer_lead_commercials | dealer lead commercials | ☐ | ☐ | ☐ | ☐ |
| E-116_lead_products | lead products | ☐ | ☐ | ☐ | ☐ |
| E-117_dealer_lead_status_history | dealer lead status history | ☐ | ☐ | ☐ | ☐ |
| E-117_widen_score_loan_refs | widen score loan refs | ☐ | ☐ | ☐ | ☐ |
| E-118_asm_territories | asm territories | ☐ | ☐ | ☐ | ☐ |
| E-118_nbfc_buyback_requests | nbfc buyback requests | ☐ | ☐ | ☐ | ☐ |
| E-119_nbfc_battery_evaluations | nbfc battery evaluations | ☐ | ☐ | ☐ | ☐ |
| E-119_upload_batches | upload batches | ☐ | ☐ | ☐ | ☐ |
| E-120_assignment_config | assignment config | ☐ | ☐ | ☐ | ☐ |
| E-121_holiday_calendar | holiday calendar | ☐ | ☐ | ☐ | ☐ |
| E-122_duplicate_merge_requests | duplicate merge requests | ☐ | ☐ | ☐ | ☐ |
| E-123_interest_level_overrides | interest level overrides | ☐ | ☐ | ☐ | ☐ |
| E-124_user_preferences | user preferences | ☐ | ☐ | ☐ | ☐ |
| E-125_cost_currency_default_inr | cost currency default inr | ☐ | ☐ | ☐ | ☐ |
| E-126_dealer_leads_source_column | dealer leads source column | ☐ | ☐ | ☐ | ☐ |
| E-127_onboarding_application_part0_columns | onboarding application part0 columns | ☐ | ☐ | ☐ | ☐ |
| E-128_dealer_lead_commercials_product_lines | dealer lead commercials product lines | ☐ | ☐ | ☐ | ☐ |
| E-129_seed_auction_lots_demo | seed auction lots demo | ☐ | ☐ | ☐ | ☐ |
| E-130_addendum_foundation | addendum foundation | ☐ | ☐ | ☐ | ☐ |
| E-131_nbfc_lead_assignments | nbfc lead assignments | ☐ | ☐ | ☐ | ☐ |
| E-132_zoho_finance_and_expenses | zoho finance and expenses | ☐ | ☐ | ☐ | ☐ |
| E-133_nbfc_service_config_and_rbac | nbfc service config and rbac | ☐ | ☐ | ☐ | ☐ |
| E-134_enach_mandates | enach mandates | ☐ | ☐ | ☐ | ☐ |
| E-135_video_kyc | video kyc | ☐ | ☐ | ☐ | ☐ |
| E-136_field_investigations | field investigations | ☐ | ☐ | ☐ | ☐ |
| E-137_nbfc_wallet_charging | nbfc wallet charging | ☐ | ☐ | ☐ | ☐ |
| E-138_manual_handoff | manual handoff | ☐ | ☐ | ☐ | ☐ |
| E-139_backfill_nbfc_tenant_binding | backfill nbfc tenant binding | ☐ | ☐ | ☐ | ☐ |
| E-140_backfill_assigned_lead_status | backfill assigned lead status | ☐ | ☐ | ☐ | ☐ |
| E-140_nbfc_financing_offers | nbfc financing offers | ☐ | ☐ | ☐ | ☐ |
| E-141_financing_dead_end | financing dead end | ☐ | ☐ | ☐ | ☐ |
| E-141_sync_prod_to_sandbox | sync prod to sandbox | ☐ | ☐ | ☐ | ☐ |
| E-142_nbfc_users_notification_prefs | nbfc users notification prefs | ☐ | ☐ | ☐ | ☐ |
| E-143_nbfc_users_role_normalise | nbfc users role normalise | ☐ | ☐ | ☐ | ☐ |
| E-144_nbfc_loans_drop_loan_application_fk | nbfc loans drop loan application fk | ☐ | ☐ | ☐ | ☐ |
| E-145_enach_razorpay_fields | enach razorpay fields | ☐ | ☐ | ☐ | ☐ |
| E-146_nbfc_loan_agreements | nbfc loan agreements | ☐ | ☐ | ☐ | ☐ |
| E-147_nbfc_service_config_enach_handoff_itarang_razorpay | nbfc service config enach handoff itarang razorpay | ☐ | ☐ | ☐ | ☐ |
| E-148_field_investigation_full_spec | field investigation full spec | ✅ | ☐ | ☐ | ☐ |
| E-149_nbfc_documents_storage_bucket | nbfc documents storage bucket | ☐ | ☐ | ☐ | ☐ |
| E-150_field_investigations_status_widen | field investigations status widen | ☐ | ☐ | ☐ | ☐ |
| E-151_nbfc_loan_agreement_source_doc | nbfc loan agreement source doc | ☐ | ☐ | ☐ | ☐ |
| E-152_vkyc_passive_link | vkyc passive link | ☐ | ☐ | ☐ | ☐ |
| E-153_vkyc_mode_allow_passive | vkyc mode allow passive | ☐ | ☐ | ☐ | ☐ |
| E-154_vkyc_session_video_url | vkyc session video url | ☐ | ☐ | ☐ | ☐ |
| E-155_field_investigations_status_check_fix | field investigations status check fix | ☐ | ☐ | ☐ | ☐ |
| E-156_field_investigations_partial_unique_index_fix | field investigations partial unique index fix | ☐ | ☐ | ☐ | ☐ |
| E-157_repair_orphaned_field_investigations | repair orphaned field investigations | ☐ | ☐ | ☐ | ☐ |
| E-158_intent_scoring_audit | intent scoring audit | ☐ | ☐ | ☐ | ☐ |
| E-159_intent_score_feedback | intent score feedback | ☐ | ☐ | ☐ | ☐ |
| E-160_nbfc_wallet_funds_provider | nbfc wallet funds provider | ☐ | ☐ | ☐ | ☐ |
| E-161_financing_offer_ceo_approval | financing offer ceo approval | ☐ | ☐ | ☐ | ☐ |
| E-162_nbfc_custom_rbac | nbfc custom rbac | ☐ | ☐ | ☐ | ☐ |
| E-163_nbfc_notification_channels | nbfc notification channels | ☐ | ☐ | ☐ | ☐ |
| E-164_dealer_onboarding_draft_step | dealer onboarding draft step | ☐ | ☐ | ☐ | ☐ |
| E-165_nbfc_byo_provider_handoff | nbfc byo provider handoff | ☐ | ✅ | ☐ | ☐ |
| E-166_nbfc_provider_credentials | nbfc provider credentials | ☐ | ✅ | ☐ | ☐ |
| E-167_whatsapp_onboarding | whatsapp onboarding | ☐ | ☐ | ☐ | ☐ |
| E-168_intent_qualification_band | intent qualification band | ☐ | ☐ | ☐ | ☐ |
| E-169_campaign_dropped_empty_not_failed | campaign dropped empty not failed | ☐ | ☐ | ☐ | ☐ |
| E-170_backfill_storage_urls_to_files_proxy | **Data repair, not DDL — and now urgent.** Rewrites stored absolute Supabase-storage URLs (`https://<ref>.supabase.co/storage/v1/object/public/<bucket>/<key>`) to the proxy path `/api/files/<bucket>/<key>` across twelve columns. Written during the S3 migration as a tidy-up and **never applied to either database** — which turned it into a live bug once the Supabase project `zziynfmqfvchkheqnqqr` was **deleted**: its subdomain no longer resolves, so a browser opening one of these links gets `DNS_PROBE_FINISHED_NXDOMAIN` rather than a 404. That is the "an already-onboarded dealer's documents are not visible" report — 284 rows on db-2 spanning 18 dealer applications, 455 more `kyc_documents` rows on db-1. Extended 2026-08-18 with `expense_submissions.bill_url` and `lead_touchpoints.recording_url` (both missed by the original enumeration) and constrained to the three buckets `/api/files` accepts — `documents`, `dealer-documents`, `call-recordings` — so a URL for any other bucket is left alone instead of becoming a link that 404s with "Unknown bucket" (verified: no other bucket appears in either DB, so it is a guard, not a behaviour change). **222 of the 284 prod rows have their object in S3 and start working the moment this runs; 62 do not** — 57 dealer documents across 6 dealers (ALISHAN MOTORS, AYANSH ENGINEERING, Khatu Shyam Automobile, Madhav Enterprises, M.S. ELECTRICIAN, UTKARSH AUTOMOBILE) plus 5 expense bills were already gone from Supabase when the copy ran and are unrecoverable; they need re-upload. Rewriting those anyway is still right: the link then returns a readable "file no longer stored" 404 from our own domain. Idempotent — dry-run in a rolled-back transaction on db-2 took 284 → 0 and a second pass changed nothing. The old ordering caveat (apply only after the `/api/files` code is deployed) is satisfied: prod already serves 599 proxy-path URLs. | ☐ | ☐ | ☐ | ☐ |
| E-170_discord_bot_service_user | discord bot service user | ☐ | ☐ | ☐ | ☐ |
| E-170_whatsapp_dealer_sessions | whatsapp dealer sessions | ☐ | ☐ | ☐ | ☐ |
| E-171_emi_tracker_columns | emi tracker columns | ☐ | ☐ | ☐ | ☐ |
| E-171_zoho_invoices_organization_id | zoho invoices organization id | ☐ | ☐ | ☐ | ☐ |
| E-172_emi_payment_attempts | emi payment attempts | ☐ | ☐ | ☐ | ☐ |
| E-172_expense_invoice_number_file_name | expense invoice number file name | ☐ | ☐ | ☐ | ☐ |
| E-173_emi_partial_payments | emi partial payments | ☐ | ☐ | ☐ | ☐ |
| E-174_leads_source_channel | leads source channel | ☐ | ☐ | ☐ | ☐ |
| E-174_zoho_invoice_payment_reference | zoho invoice payment reference | ☐ | ☐ | ☐ | ☐ |
| E-175_dealer_owner_aadhaar | dealer_onboarding_applications.owner_aadhaar_no + owner_aadhaar_verified (dealer agreement Aadhaar match) | ✅ | ✅ | ☐ | ☐ |
| E-176_consent_signer_name_score | consent_records.signer_name_match_score (Aadhaar e-sign name-match signal) | ✅ | ✅ | ☐ | ☐ |
| E-177_loan_calculator | dealer loan calculator: calc_* tables (nbfcs/schemes/model_caps/component_prices/coverage/settings/config_versions/audit_log/leads) + guard indexes & checks | ✅ | ✅ | ☐ | ☐ |
| E-178_calc_otp_search_history | calc_otp_verifications + calc_leads otp/wa columns (OTP-gated calculator, WhatsApp results, admin search history) | ✅ | ✅ | ☐ | ☐ |
| E-179_lead_registry | lead_registry — central capture of every new lead (dealer/customer/oem/nbfc, web+whatsapp) with name/phone + source link | ✅ | ✅ | ☐ | ☐ |
| E-180_consent_otp_verifications | consent_otp_verifications + consent_records otp_verification_id/otp_verified_at (OTP-based customer consent, replaces Digio Aadhaar e-sign) | ✅ | ✅ | ☐ | ☐ |
| E-181_nbfc_emi_tracker_overrides | nbfc_emi_tracker_overrides — per-loan display overrides for the EMI Tracker table (borrower/serial/emi/next_due/last_paid/progress/status/dpd/mandate/next_auto_debit) | ✅ | ✅ | ☐ | ☐ |
| E-182_emi_tracker_financier | nbfc_emi_tracker_overrides.financier — free-text financier label shown in a new Finance column on the EMI Tracker | ✅ | ✅ | ☐ | ☐ |
| E-183_emi_tracker_standalone | nbfc_emi_tracker_overrides.is_standalone + loan_application_id made nullable + partial unique index on (tenant, lower(vehicleno)) WHERE is_standalone — force-import bulk-upload rows with no matching loan as display-only tracker entries | ✅ | ✅ | ☐ | ☐ |
| E-184_device_battery_map_location | device_battery_map.state + city (Intellicar Fleet Overview State/City filters) | ✅ | ✅ | ✅ | ✅ |
| E-185_risk_card_verdict_source | risk_card_runs.verdict_source (hand_coded/sandbox/none/legacy_llm) + backfill; severity vocabulary widened to include inconclusive/error so a failed test stops rendering as a green OK card | ✅ | ✅ | ☐ | ☐ |
| E-186_risk_hypothesis_text_matches_code | Rewrites the 5 hand-coded risk_hypotheses descriptions so the text shown to operators matches the test that runs (geo-shift said "100 km from onboarding centroid" but checked an India bounding box) and stops hard-coding threshold values now governed by nbfc_risk_rules | ✅ | ☐ | ☐ | ☐ |
| E-187_risk_runs | risk_runs table (one row per risk-engine invocation) + risk_card_runs.run_id. Partial unique index (tenant_id) WHERE status='running' is the concurrency lock; the table is also the freshness source for the Risk page | ✅ | ✅ | ☐ | ☐ |
| E-188_risk_hypothesis_promotion | risk_hypotheses.promoted_at/promoted_by/retire_reason. An unvetted llm-v1 hypothesis is capped at severity=warn and cannot raise a High Alert until a human promotes it; hand-coded rows backfilled as promoted. Enables catalogue reuse + retirement instead of unbounded growth | ☐ | ✅ | ☐ | ☐ |
| E-189_inventory_transfers_canonical_ids | Rebuilds inventory_transfers on canonical id types (varchar(64) PK, varchar(255) dealer ids → accounts.id, uuid actor ids → users.id). This DB still had the pre-0038 integer-PK/integer-FK/rejected_* design, so every transfer read blew up with 22P02 "invalid input syntax for type integer: ACC-…". Supersedes 0038; guarded — drops only when the stale schema is present AND empty, else RAISEs | ✅ | ☐ | ☐ | ☐ |
| E-185_buyback_core | peakAmp Battery Buyback Portal core schema (BRD §3) — buyback_deal_status enum (all 21 states) + catalog_variants, business_entity_roles, buyback_pickup_addresses, buyback_requests/batches/lines/units, buyback_photos, provenance_records, info_requests, buyback_deals, negotiation_rounds(+_lines), final_offers(+_lines), deal_line_locks, buyback_activity_log (INSERT-only trigger), buyback_notification_events | ☐ | ✅ | ✅ | ☐ |
| E-186_buyback_vendor_leg | peakAmp vendor leg & fulfilment (M09–M11 + minimal M05) — scrap_vendors, vendor_threads (partial UNIQUE: one AGREED vendor per deal), vendor_thread_lines, purchase_orders(+_lines, tax cols modelled not ruled), pickups, buyback_po_no_seq; buyback_notification_events gains attempts/next_attempt_at/recipient_ref/attachment_s3_key for dispatch; deal_line_locks becomes FILL-ONCE via trigger (only vendor_price, only once). **Apply AFTER E-185.** | ☐ | ✅ | ✅ | ☐ |
| E-187_buyback_money | peakAmp money leg (M12–M14) — invoices (partial UNIQUE: one LIVE invoice per deal+leg, RETURNED ones kept as evidence), invoice_lines (per-line `matched` verdict + tax cols), settlement_transactions (TXN-{n}-D OUT / -V IN; **CHECK constraint makes an unevidenced MANUAL payout impossible**; UNIQUE leg_sub_id so a dealer cannot be paid twice), buyback_invoice_no_seq. **Apply AFTER E-186.** | ☐ | ✅ | ✅ | ☐ |
| E-188_buyback_compliance_trust | peakAmp compliance & trust (Sprints 3–4) — agreements (Digio eSign, dealers AND vendors; one live per entity+role, declined ones kept); catalog_price_history + catalog_price_reviews (M16 versioned price books + weekly nudge); bank_statement_imports/_rows (M13 STATEMENT reconcile) + settlement_transactions.statement_row_id; buyback_photos dedup (phash index + dup_flag: **DUPLICATE_CROSS_DEALER is the fraud case**); pickups BWM gate (expected_counts, variance_ack_required — blocks payout until the dealer acknowledges a count variance). **Apply AFTER E-187.** | ☐ | ✅ | ✅ | ☐ |
| E-190_battery_spec_models | battery_spec_models catalog (per-model rated voltage/capacity + under/over-voltage, over-current, over-temp display thresholds) + device_battery_map.battery_model logical FK — Intellicar electrical analytics judge packs against their model spec (resolution: spec > app_settings > env > defaults); seeds placeholder LFP-51V-105AH | ☐ | ✅ | ✅ | ☐ |
| E-191_buyback_line_battery_specs | buyback_lines gains 11 nullable dealer-declared spec columns: brand, chemistry (NMC/LFP), form_factor (CELL/PRISMATIC/CYLINDRICAL), nominal_voltage, nominal_ampere, unit_weight_kg, warranty_cycles, functional_qty, non_functional_qty, iot_battery, iot_brand_name. Required subset enforced by the submit gate, not NOT NULL. **Apply AFTER E-185** (no-ops with a NOTICE where buyback tables don't exist — i.e. prod). | ☐ | ✅ | ✅ | ☐ |
| E-192_buyback_scale_indexes | Scale pack for 10K+ dealers: `CREATE EXTENSION pg_trgm` (needs rds_superuser — see file header); btree `buyback_requests(submitted_at NULLS LAST, created_at)` (the review queue's own sort), `buyback_activity_log(deal_id)`, partial `settlement_transactions(txn_date DESC) WHERE closed_at IS NOT NULL` (the ledger), FK indexes on `buyback_lines.variant_id`/`buyback_photos.unit_id`/`pickups.batch_id`; GIN trigram indexes for leading-wildcard admin/dealer search (M23) on `buyback_requests.request_no`, `provenance_records.{vehicle_no,rc_number,prev_owner_name}`, `settlement_transactions.{leg_sub_id,group_txn_id,txn_ref}`, and `accounts.{business_entity_name,gstin}` (accounts indexes in their OWN DO block since `accounts` exists on every env, unlike the buyback tables). **Apply AFTER E-185/186/187** for the buyback-table indexes; the `accounts` indexes apply everywhere including prod. Mirrored in `src/lib/db/schema.ts` so `db:push` users don't drop these. | ☐ | ✅ | ✅ | ☐ |
| E-193_buyback_gateway_payments | buyback_gateway_transactions — the ATTEMPT behind an online gateway money move (RazorpayX payout to a dealer, Razorpay Payment Link to a vendor); only a terminal PROCESSED/PAID mints a settlement_transactions row (method='API'). buyback_gateway_kind/buyback_gateway_status enums; partial UNIQUE `(deal_id, leg)` WHERE status is in-flight is the double-click race guard, partial UNIQUE `provider_ref` is the webhook idempotency guard. Also adds `accounts.bank_beneficiary_name` (RazorpayX beneficiary name, falls back to business_entity_name). **Apply AFTER E-185/E-187** — the gateway table no-ops with a NOTICE where those (or their enum types) don't exist yet (prod); the `accounts` column applies everywhere. Live since the R-round: the gateway routes/webhooks/ticker and the settlement in-flight guards all read this table. | ☐ | ✅ | ✅ | ☐ |
| E-194_buyback_intake_and_queue | Intake + review-queue corrections, all traced to db-1 evidence that 24 of the first 32 requests died in DRAFT and only 1 of 4 dealers ever reached the review queue: `buyback_pickup_addresses.owner_kind` (TEXT + CHECK DEALER\|VENDOR\|CUSTOMER, default DEALER — a vendor's collection point and a driver/customer's doorstep previously had nowhere to live; TEXT not enum, per E-191's reasoning in the same table family); btree `buyback_requests(submitted_at DESC NULLS LAST, created_at DESC)` backing the queue's new newest-first sort (NOT a duplicate of E-192's ASC index — reverse-scanning that one yields DESC NULLS **FIRST**, floating unsubmitted requests to the top; the ASC index stays for /reports and FIFO readers); plus two COMMENT rewrites on `buyback_lines` that E-191 left false — the submit gate no longer requires `iot_brand_name` (blank now resolves to 'Intellicar' in line-spec.ts) and the functional/non-functional split is a **ceiling, not an equality** (it rejected 5 working + 3 dead out of 10, i.e. a partially tested lot). **Apply AFTER E-185/E-191**; no-ops with a NOTICE where the buyback tables don't exist (prod). Mirrored in `src/lib/db/schema.ts` so `db:push` users don't drop the column/index. | ✅ | ✅ | ✅ | ☐ |
| E-196_buyback_proforma_invoice | The Proforma Invoice — the document that answers the vendor's PO (items 8/15, BRD step 3). A SEPARATE `proforma_invoices` + `proforma_invoice_lines` table on its own `buyback_proforma_no_seq` (PI-{n}-V, starts 1001), NOT an `invoices` row: `invoices` is the TAX slot (INV-{n}-V on the statutory buyback_invoice_no_seq — already at 5004, four numbers burnt by documents whose own template says they are not tax invoices). The existing vendor invoice stays put and becomes a real tax invoice the day GST is ruled (BRD §10). buyback_proforma_status enum (ISSUED/SUPERSEDED/CANCELLED — no RETURNED; nobody approves a proforma). po_id NOT NULL ('against' is the point — no PO, no PI). total server-derived from vendorReceipt(deal_line_locks), CHECK total>0. Partial UNIQUE (deal_id) WHERE status='ISSUED' — one live per deal, re-issue SUPERSEDES; verified 23505 on a second live, 23514 on a zero total. NO tax columns — a proforma is tax-exclusive. Also widens state-machine exchange_pos to the vendor role (E-196) so the vendor raises their OWN PO (lib/buyback/po.ts, one impl two callers), and the dealer activity filter moved to action-based so the PO exchange stays visible whoever uploads second. Whole file gated on to_regclass('buyback_deals') — no-ops on prod. Mirrored in schema.ts (partial index is SQL-only). Verified end-to-end on db-1 (rolled back): BB-1024 vendor PO -> PI-1001-V ₹24150 -> re-issue -> PI-1002-V, exactly one live. | ✅ | ✅ | ✅ | ☐ |
| E-197_buyback_owner_identity | Who the battery's previous owner is and how to pay them (item 4). **This was unbuildable until the business answered a question**: `buyback_leg` has exactly two values (DEALER, VENDOR) and `settlement_transactions.leg` uses it, so nothing could record a payment to a driver — collecting their bank account would have been pure liability. The model: paying the driver INSTEAD of the dealer is the SAME OUT payment of the SAME locked dealer_price to a different beneficiary, so `settlement_transactions.payee_provenance_id` (NULL = the dealer, and every existing row) needs **no new leg and does not touch M14** — we still pay dealer_price out exactly once. A driver-AND-dealer SPLIT is a genuinely different thing and is NOT modelled: that needs a third buyback_leg value (shared by purchase_orders/invoices/negotiation_rounds, where DRIVER is meaningless), a rewrite of both-legs-closed=>SETTLED, and a re-derivation of M14. Adds to `provenance_records`: prev_owner_pan (full — a tax id, not a credential), prev_owner_aadhaar_last4 (**LAST FOUR ONLY**, CHECK `^[0-9]{4}$` — a full Aadhaar is unstorable, verified 22001; DPDP/UIDAI), prev_owner_aadhaar_ref + _verified_at (the Decentro reference IS the evidence — reuse src/lib/kyc/), payee_account_number/ifsc/bank_name/beneficiary_name (full — you cannot pay a last-four). CHECKs verified to bite: full Aadhaar refused, payee override on the money-IN VENDOR leg refused (23514). All nullable — required is the submit gate's job, per E-191. Mirrored in `schema.ts`. | ✅ | ✅ | ✅ | ☐ |
| E-198_notification_multi_attachment | `buyback_notification_events.attachment_s3_keys text[]` — the event could name exactly ONE file, so the vendor quotation's battery photos were base64-inlined INTO the PDF instead: capped at 2/line and rendered at 54x40px, a size at which a swollen cell and a healthy one look identical, which defeats the entire reason for sending them (item 14: "the vendor should not have to log in just to see the battery condition"). The mailer never had this limit — `sendEmail()` has always taken `attachments[]`. NOT stored in `payload`: attachment keys are snapshotted at emit time so the dispatcher never re-derives what it sends, same rule as `recipient_ref`. The singular column STAYS and keeps the PDF: its absence is fatal (dispatch throws rather than send a "please quote" with no quotation), while a missing photo here is SKIPPED — two columns, two failure policies, which is the reason not to merge them. Additive, no backfill (NULL = no extra files, true of every existing row). No-ops with a NOTICE where the buyback tables don't exist (prod). Mirrored in `schema.ts`. | ✅ | ✅ | ✅ | ☐ |
| E-199_nbfc_risk_card_visibility | `nbfc_risk_card_visibility` — per-NBFC STRICT allowlist of which risk-hypothesis cards a partner may see on /nbfc/risk. Presence of a `(tenant_id → nbfc_tenants.id, hypothesis_id → risk_hypotheses.id)` row = visible; no row = hidden; a tenant with zero rows sees zero cards. NO seed (every partner starts blank until an admin selects). Unique `(tenant_id, hypothesis_id)` + `(tenant_id)` index; both FKs ON DELETE CASCADE. Filtered at DISPLAY time in `loadCards()` so it survives every "Re-run analysis" (a re-run only writes `risk_card_runs`). Applies everywhere (`nbfc_tenants`/`risk_hypotheses` exist on the NBFC-dashboard envs). Mirrored in `schema.ts`. | ✅ | ✅ | ☐ | ☐ |
| E-200_nbfc_doc_requests | `nbfc_doc_requests` — the NBFC-originated document/KYC request thread on the Acquire workspace + two additive tag columns on `other_document_requests` (`nbfc_request_id`, `source DEFAULT 'admin'`). Turns Acquire from read-only into the 7-hop loop NBFC→Admin→Dealer→Customer→Dealer→Admin→NBFC. ONE wrapper row → MANY `other_document_requests` children (which carry the files, reusing the existing tokenised /upload-docs machinery); a `request_type='message'` row (admin→NBFC direct) has zero children. `source='admin'` keeps every existing admin→dealer row and query unchanged. Idempotent, no backfill. `other_document_requests` exists everywhere; the new table is unconditional CREATE IF NOT EXISTS. Mirrored in `schema.ts`. | ✅ | ✅ | ☐ | ☐ |
| E-201_nbfc_document_verifications | `nbfc_document_verifications` — per-NBFC KYC verdict (pending\|verified\|queried\|rejected + notes) on each customer/co-borrower document, kept SEPARATE from the admin's own verification. One row per `(assignment_id, doc_for, doc_key)`; upsert. Feeds the admin "NBFC KYC Verification" card. Idempotent, no backfill. Mirrored in `schema.ts`. | ✅ | ✅ | ☐ | ☐ |
| E-202_nbfc_doc_requests_checks | CHECK backstops for E-200/E-201: `item_count <= 10` (Step-4 extra-items cap), and `status` / `request_type` / `verdict` restricted to their enum value sets. App-level validation is primary; these fail a bad write loudly. Each ADD CONSTRAINT guarded (duplicate_object / undefined_table → NOTICE). SQL-only (Drizzle doesn't model CHECKs here). | ✅ | ☐ | ☐ | ☐ |
| E-203_nbfc_manual_consent_request_type | Extends the E-202 `request_type` CHECK to admit `'manual_consent'` — the NBFC uploads a DPDP consent PDF for wet/manual signing, which rides the E-200 loop (NBFC→Admin→Dealer→Admin→NBFC) as a wrapper of that type. DROP-if-exists then re-add so it is idempotent and works whether or not E-202 was applied here. Guarded (undefined_table → NOTICE). SQL-only. **Apply AFTER E-200/E-202.** | ☐ | ☐ | ☐ | ☐ |
| E-204_nbfc_co_borrower_request_type | Extends the E-202 `request_type` CHECK to admit `'co_borrower'` — an NBFC-initiated co-borrower request (when the admin never flagged one). Rides the E-200 loop NBFC→Admin; the admin one-clicks "Request co-borrower from dealer" (triggers the standard dealer co-borrower KYC flow) then the wrapper is pushed back to the NBFC. DROP-if-exists then re-add the FULL value set (incl. `manual_consent`), so it is idempotent and self-contained whether or not E-203 was applied. Guarded (undefined_table → NOTICE). SQL-only. **Required for the NBFC "Request Co-Borrower" button — the insert fails the CHECK without it.** | ✅ | ☐ | ☐ | ☐ |
| E-205_nbfc_consent_template | Adds `consent_template_url` + `consent_template_size` to `nbfc_service_config` — the NBFC's own DPDP consent-template PDF, uploaded once in Settings and reused across all leads (Acquire e-sign/OTP signs THIS doc instead of the per-lead iTarang consent). Additive `ADD COLUMN IF NOT EXISTS`. Mirrored in `schema.ts`. | ✅ | ✅ | ☐ | ☐ |
| E-206_consent_records_initiated_by_tenant | Adds `initiated_by_tenant_id uuid` (nullable) to `consent_records` — stamps the NBFC tenant that captured a consent from Acquire so the NBFC's DPDP card shows only its own (NULL = iTarang/dealer; admin view unfiltered). Additive `ADD COLUMN IF NOT EXISTS`, no FK. Mirrored in `schema.ts`. | ✅ | ✅ | ☐ | ☐ |
| E-207_nbfc_verdict_attachments | Adds `attachments jsonb DEFAULT '[]'` to `nbfc_document_verifications` — supporting files (any type, `{url,name,type,size}`) the NBFC attaches to a per-document Approve/Reject/Request-correction from the Acquire KYC table; viewable by the admin in the "NBFC Actions" section. Additive `ADD COLUMN IF NOT EXISTS`. Mirrored in `schema.ts`. | ✅ | ✅ | ☐ | ☐ |
| E-208_product_selections_pre_sanction_docs | Adds `pre_sanction_doc_urls jsonb DEFAULT '[]'` to `product_selections` — the Step-4 dealer pre-sanction bucket (≤10 items, all formats; `{url,name,type,size}`; a combined PDF = one item). Viewable by NBFC (Acquire dossier) + admin. Additive `ADD COLUMN IF NOT EXISTS`. Mirrored in `schema.ts`. | ✅ | ✅ | ☐ | ☐ |
| E-209_nbfc_verdict_forwarded | Adds `forwarded_at timestamptz`, `forwarded_request_id varchar(255)`, `forwarded_by uuid` to `nbfc_document_verifications` — stamps an admin "Forward to dealer" on a queried/rejected NBFC verdict (E-209) and links the correction wrapper it spawned; primary→Step 2, co-borrower→Step 3. Additive `ADD COLUMN IF NOT EXISTS`. Mirrored in `schema.ts`. | ☐ | ✅ | ☐ | ☐ |
| E-210_nbfc_doc_request_attachments | Adds `attachments jsonb DEFAULT '[]'` + `verdict_id integer` (+ index) to `nbfc_doc_requests` — the admin's own uploader: documents the admin attaches to an NBFC reply/message and sends straight to the NBFC (no dealer round-trip), `verdict_id` grouping the reply under the `nbfc_document_verifications` verdict it answers. Additive `ADD COLUMN IF NOT EXISTS`. Mirrored in `schema.ts`. **Required** — `listThreadForLead`/`createNbfcDocRequest` name every column, so without it the NBFC Actions card and the NBFC request thread 500 with "column attachments does not exist". | ✅ | ✅ | ☐ | ☐ |
| E-211_notifications_indexes | Four indexes on `notifications` — `(user_id, created_at DESC)` for the feed, a partial `(user_id) WHERE read IS NOT TRUE` for the badge, a partial `(dealer_id, created_at DESC) WHERE user_id IS NULL` for legacy dealer-keyed rows, and `(lead_id, created_at DESC)`. Groundwork for the CRM-wide notification hub, which turns the bell into the delivery surface for every portal (admin/dealer/NBFC/vendor) instead of buyback only — one row per recipient per business event, so volume goes up by an order of magnitude. **Index-only: no columns added, changed or dropped.** Provenance (`from`/`to`/`stage`) and inline `actions` ride in the existing `data` jsonb, so there is nothing to backfill. **Optional to ship** — the app is correct without it, just slower; unlike a column migration it can never 500 a route. **Applies EVERYWHERE incl. prod** (`notifications` exists on every env). The two partial indexes are migration-only (drizzle's builder has no partial-index syntax); the other two are mirrored in `schema.ts`. | ✅ | ✅ | ☐ | ☐ |
| E-195_scrap_vendor_login | `users.vendor_entity_id` (varchar(255) → accounts.id, partial index WHERE NOT NULL) — the missing link that let BRD M24's deferred vendor login finally exist. A vendor was already `accounts` + `business_entity_roles(role='SCRAP_VENDOR')` + `scrap_vendors` (E-186); nothing said WHICH vendor a `users` row acts for. **NOT** reused `users.dealer_id`: it holds the right value (accounts.id and dealer_code are the same string) but is joined against the `dealers` table by dealer_code app-wide, and a scrap vendor has no dealers row — every such join would silently return nothing instead of failing. **No enum to extend**: `users.role` is a bare varchar(50) with no constraint, so `'scrap_vendor'` needs no DDL — it is registered in six hardcoded TypeScript lists instead (middleware `roleDashboards`, sidebar `roleNavigation`, sidebar `inferredRole`, both login redirect chains, `lib/roles.ts`), and missing one fails **silently**. FK is NOT ON DELETE CASCADE — deleting an account must not silently delete logins. **Applies EVERYWHERE incl. prod** (`users`/`accounts` exist on every env, unlike the buyback tables); guarded anyway. Mirrored in `schema.ts`. | ✅ | ✅ | ✅ | ☐ |
| E-200_notifications_archive_delete | `notifications.archived_at` + `notifications.deleted_at` (both timestamptz, nullable) + partial index `notifications_user_active_idx ON (user_id) WHERE deleted_at IS NULL` — soft archive/delete for the buyback notification centre (items 12/13) so Archive/Delete hide a row without losing it (escalation-safety + audit). NULL = active/undeleted, so no backfill. **Applies EVERYWHERE incl. prod** — `notifications` is the whole CRM's bell and exists on every env (unlike the buyback tables), so no table guard. Renumbered from E-199 (Rushikesh's `E-199_nbfc_risk_card_visibility` took that slot on main). Mirrored in `schema.ts` so `db:push` users don't drop these. | ☐ | ☐ | ☐ | ☐ |
| E-201_battery_spec_mileage_band | `battery_spec_models.min_mileage_km_per_ah` + `max_mileage_km_per_ah` (both numeric(6,3), nullable) — the normal km-per-Ah efficiency band for the Intellicar mileage charts (Mileage Trend / Per-Cycle Mileage / Ah vs Mileage), which previously had only a COMPUTED "pack's own best" baseline and no configured range. Spec-only per-model, exactly like E-190's `rated_capacity_ah`: resolved via `device_battery_map.battery_model`, NO app_settings/env/default rung, NULL = no line drawn. Seeds the `LFP-51V-105AH` row with an example 0.35–0.65 band (placeholder, tune with real fleet numbers) via an idempotent `UPDATE … WHERE … IS NULL` that never clobbers a hand-edit. **Applies EVERYWHERE** — `battery_spec_models` exists on the Intellicar envs (E-190). Mirrored in `schema.ts`. | ☐ | ☐ | ☐ | ☐ |
| E-212_password_reset_tokens | New table `password_reset_tokens` (id, `user_id` → users.id ON DELETE CASCADE, email, `token_hash` varchar(64), expires_at, used_at, invalidated_at, requested_ip, requested_ua, created_at) + a UNIQUE index on `token_hash`, a `(user_id, created_at DESC)` index for the per-account throttle, and a partial `(user_id) WHERE used_at IS NULL AND invalidated_at IS NULL` index for the supersede sweep. Backs the self-service **"Forgot password?"** flow on `/login`, which did not exist before (the link was a dead `<a href="#">`). Only `sha256(raw)` is stored — the raw token rides in the emailed URL only; single-use via a conditional `UPDATE … WHERE used_at IS NULL` that doubles as the double-submit mutex. The reset endpoint writes **BOTH** password stores — `supabaseAdmin.auth.admin.updateUserById` first (authoritative for login), then `users.password_hash` + clears `must_change_password` — matched on `users.id`, **never on email** (see the case-sensitivity comment in `api/auth/change-password/route.ts`). **Required** — without it `/api/auth/forgot-password` and `/api/auth/reset-password` 500 with "relation password_reset_tokens does not exist"; the rest of the app is unaffected (new table, nothing existing is touched). New table + indexes only, no backfill. Mirrored in `schema.ts` except the partial index (migration-only, drizzle has no partial-index syntax). | ✅ | ✅ | ☐ | ☐ |
| E-213_password_change_otps | New table `password_change_otps` (id, `user_id` → users.id CASCADE, email, `otp_hash` varchar(64), expires_at, send_count, attempt_count, locked_until, verified_at, consumed_at, delivery_status, requested_ip, requested_ua, created_at, updated_at) + a `(user_id, created_at DESC)` index and two partial indexes (`WHERE verified_at IS NULL AND consumed_at IS NULL` for the open session, `WHERE verified_at IS NOT NULL AND consumed_at IS NULL` for the confirm claim). Backs the **in-app Change Password modal** reached from the header dropdown: 4-digit code → registered email → verify → set new password, with NO current-password field (mailbox control is the proof). Only `sha256(code)` is stored; single-use via a conditional `UPDATE … RETURNING` on `consumed_at` that doubles as the double-submit mutex; 10-min TTL, 3 sends / 30-min cooldown, 3 attempts / 5-min lock. Distinct from E-212 (`password_reset_tokens`), which is the UNAUTHENTICATED forgot-password link — this one is session-gated. The confirm endpoint writes **both** password stores (`supabaseAdmin.auth.admin.updateUserById` first, then `users.password_hash` + clears `must_change_password`), matched on `users.id`. **Required** — without it the three `/api/auth/change-password/{send-otp,verify-otp,confirm}` routes 500 with "relation password_change_otps does not exist"; the rest of the app, including the legacy forced-reset `/change-password` page, is unaffected. New table + indexes only, no backfill. Mirrored in `schema.ts` except the two partial indexes (migration-only). | ✅ | ✅ | ☐ | ☐ |
| E-214_whatsapp_onboarding_operators | New table `whatsapp_operators` (admin-managed allowlist of internal team members who onboard dealers over WhatsApp: wa_phone E.164-without-plus UNIQUE, display_name, optional `user_id` → users.id, is_active + deactivated_at/by, created_by, notes) + `session_kind`/`operator_id`/`parent_session_id` on `whatsapp_onboarding_sessions` + `onboarding_operator_id`/`onboarding_channel`/`wa_operator_session_id`/`operator_invited_at`/`operator_handoff_at` on `dealer_onboarding_applications`. Breaks the ONE-dealer-per-WhatsApp-number limit: `wa_phone` was never unique on the sessions table, so an operator now gets one `operator_hub` row (OP_* menu state) plus N `operator_file` rows — each carrying the OPERATOR's wa_phone (replies reach them) while `application_id` points at a different dealer, so the UNMODIFIED onboarding state machine + Gemini/Decentro document pipeline runs per dealer file. Adds the first-ever unique index on that table (`whatsapp_onboarding_sessions_operator_hub_key`, PARTIAL `WHERE session_kind = 'operator_hub'`) to stop the unlocked read-then-insert in getOrCreateSession from forking an operator menu; plain dealer rows are untouched. `onboarding_operator_id` is deliberately NOT `dealer_user_id` — the approve route overwrites that with the dealer's Supabase auth id, destroying creator attribution. **Required** for the operator flow; without it `resolveOperator()` 500s with "relation whatsapp_operators does not exist". Fully additive — every default (`session_kind='dealer'`, `onboarding_channel='self'`) reproduces pre-E-214 behaviour byte-for-byte, so with an empty allowlist the runtime is unchanged. Mirrored in `schema.ts` except the partial indexes (migration-only). | ✅ | ✅ | ☐ | ✅ |
| E-202_dealer_type | `dealer_onboarding_applications.dealer_type` + `dealers.dealer_type` (both varchar(16), nullable) — dealer business type New/Scrap/Both, selected in onboarding Step 1 and carried onto the canonical dealers row at approval. Free-text (not enum) per this table family's convention (company_type/source/payment_method). Backfilled to 'new' for existing rows (idempotent `UPDATE … WHERE dealer_type IS NULL`). Only CAPTURES the type — no agreement/dashboard branching yet. **Applies EVERYWHERE incl. prod** — `dealer_onboarding_applications`/`dealers` exist on every env; **apply BEFORE shipping the code** (Drizzle lists the column in every onboarding INSERT, so an unapplied column 500s every autosave/submit). Mirrored in `schema.ts`. | ☐ | ☐ | ✅ | ☐ |
| E-215_security_scanner | Three NEW tables for a standalone CRM security-scanner agent, **independent of the NBFC risk engine** (no shared tables/code): `security_scan_runs` (scan header + the single-flight lock — a partial unique on `(target_env) WHERE status='running'`, exactly the risk-run lock pattern), `security_checks` (catalogue of checks keyed by `slug`, seeded by the app), and `security_findings` (id, `run_id` → security_scan_runs CASCADE, check_slug, category, severity `critical|high|medium|low|info`, title/summary, target_url, http_method, evidence_json, reproduction, remediation, owasp_ref, cwe_ref, confidence, status `open|acknowledged|resolved|false_positive`, fingerprint, acknowledged_at/resolved_at, llm_* token cols). Findings are deduped while open by a partial unique on `(fingerprint) WHERE status <> 'resolved'`. The agent (Playwright + LangGraph) writes rows; the `/admin/security` dashboard reads them. **Required** only where the scanner runs — the three routes/pages 500 with "relation security_* does not exist" without it; the rest of the app is untouched (all-new tables). New tables + indexes only, no backfill. Mirrored in `schema.ts` except the two partial indexes (migration-only, drizzle has no partial-index syntax). Applied to db-1 on 2026-07-29. **Applied to prod (database-2) on 2026-07-30** as part of the five-file security family, applied together in one transaction and verified afterwards against the catalog: all 6 `security_*` tables, 22 indexes (incl. both partial uniques) and the 7 added `security_findings` columns are present. Sandbox needed no separate apply: sandbox.itarang.com runs on database-1, so the db-1 tick above IS the sandbox tick — confirmed 2026-07-30 by querying db-1 directly (6 tables, 22 indexes, 7/7 added columns). | ✅ | ✅ | ✅ | ✅ |
| E-216_security_events | New table `security_events` (id, occurred_at, event_type, severity `critical|high|medium|low|info`, action `blocked|logged`, ip, actor_user_id, actor_role, method, path, query, user_agent, matched_rule, evidence jsonb, status `new|reviewed|ignored`, alerted_at, created_at) + four plain indexes (occurred_at DESC, severity, (ip, occurred_at), status). The runtime-intrusion-detection half of the security feature: the middleware detection layer (`src/lib/security/detect.ts`) records SQLi/XSS/path-traversal/scanner-UA/sensitive-unauth events here via `POST /api/internal/security-events`. **Required** only if live detection is enabled (`SECURITY_DETECTION_ENABLED=1`); the `/admin/security/live` page + ingest route 500 with "relation security_events does not exist" without it, but the rest of the app (incl. E-215 scanner) is untouched. New table + indexes only, no backfill. All indexes non-partial → fully mirrored in `schema.ts`. Applied to db-1 on 2026-07-29. **Applied to prod (database-2) on 2026-07-30** as part of the five-file security family, applied together in one transaction and verified afterwards against the catalog: all 6 `security_*` tables, 22 indexes (incl. both partial uniques) and the 7 added `security_findings` columns are present. Sandbox needed no separate apply: sandbox.itarang.com runs on database-1, so the db-1 tick above IS the sandbox tick — confirmed 2026-07-30 by querying db-1 directly (6 tables, 22 indexes, 7/7 added columns). | ✅ | ✅ | ✅ | ✅ |
| E-218_security_resolution_chat | New table `security_finding_messages` (id, `finding_id` → security_findings.id CASCADE, role `user\|assistant\|system`, message, `proposed_fix` jsonb = `{file,search,replace,explanation}` on an assistant turn that proposes an applyable edit, created_at) + `(finding_id, created_at)` index; plus four additive columns on `security_findings`: `resolution_summary` text, `resolution_report` jsonb, `resolved_by` uuid, `resolution_method` varchar(16) (`auto_apply\|manual`). Backs the **AI resolution chat** on each finding card: a human converses with an agent that has read the affected source file, agrees a fix, then Resolve auto-applies it to disk (deterministic single-occurrence search/replace, `.bak` backup + verify + rollback, gated by `SECURITY_AUTOFIX_ENABLED=1`, refused on prod) and records the report here. **Required** only where the resolution chat runs — the `/api/it/security/findings/[id]/{chat,resolve}` routes 500 with "relation security_finding_messages does not exist" without it; the E-215 scanner + E-216 live detection are untouched (new table + additive columns, no backfill). Mirrored in `schema.ts`. Applied to db-1 on 2026-07-30. **Applied to prod (database-2) on 2026-07-30** as part of the five-file security family, applied together in one transaction and verified afterwards against the catalog: all 6 `security_*` tables, 22 indexes (incl. both partial uniques) and the 7 added `security_findings` columns are present. Sandbox needed no separate apply: sandbox.itarang.com runs on database-1, so the db-1 tick above IS the sandbox tick — confirmed 2026-07-30 by querying db-1 directly (6 tables, 22 indexes, 7/7 added columns). | ✅ | ✅ | ✅ | ✅ |
| E-217_security_event_types | **No structural change** — widens the *documented* `security_events.event_type` taxonomy (refreshed `COMMENT ON COLUMN`) and adds one index, `security_events_type_occurred_idx` on `(event_type, occurred_at DESC)`. The detection layer grew from 6 event types to 20: eleven new payload rules in `src/lib/security/detect.ts` (command_injection, ssrf, lfi_rfi, jndi_injection, nosql_injection, crlf_injection, proto_pollution, xxe, open_redirect, sensitive_file_probe, method_abuse) plus three volumetric/behavioural rules in the new `src/lib/security/rate-watch.ts` (rate_flood, path_enumeration, auth_bruteforce). `event_type` is varchar(32) with no CHECK/enum and the longest new value is 20 chars, so **the new code runs correctly against a DB that only has E-216** — this migration is a documentation + query-performance nicety, not a hard dependency. The index matters once the volumetric rules are on, because they make the table grow much faster and the dashboard now filters by attack type. Wrapped in `DO $do$ … EXCEPTION WHEN undefined_table` so it is a no-op where `security_events` doesn't exist yet. Index mirrored in `schema.ts`. Applied to db-1 on 2026-07-30. **Applied to prod (database-2) on 2026-07-30** as part of the five-file security family, applied together in one transaction and verified afterwards against the catalog: all 6 `security_*` tables, 22 indexes (incl. both partial uniques) and the 7 added `security_findings` columns are present. Sandbox needed no separate apply: sandbox.itarang.com runs on database-1, so the db-1 tick above IS the sandbox tick — confirmed 2026-07-30 by querying db-1 directly (6 tables, 22 indexes, 7/7 added columns). | ✅ | ✅ | ✅ | ✅ |
| E-219_security_finding_actions | New table `security_finding_actions` (id, `finding_id` → security_findings.id CASCADE, action `acknowledged\|resolved\|false_positive\|reopened\|auto_apply`, method `auto_apply\|manual` nullable, actor_user_id/actor_email/actor_role, **ip + user_agent** = the origin the action was performed from, summary, detail jsonb, created_at) + three indexes ((finding_id, created_at), (action, created_at DESC), (ip)); plus three additive columns on `security_findings`: `resolved_ip` varchar(64), `resolved_user_agent` text, `resolved_by_email` text (denormalised origin of the LATEST resolution so the History cards render without a join). Backs the **History tab** at `/it/security/history` — every resolved finding shown as a card with its append-only resolution timeline: how it was resolved (AI auto-apply vs manual triage), the applied diff, by whom, when, and **from which IP**. The trail is append-only, so a resolve → reopen still shows the past resolution. E-218 recorded only the final resolution state and the manual triage buttons wrote nothing beyond the status; this fills both gaps. **Required** wherever findings are triaged — without it the manual triage PATCH and the auto-apply resolve route error on `relation security_finding_actions does not exist` / `column resolved_ip does not exist`, and the History page 500s; the scanner (E-215) and live detection (E-216) are untouched. New table + additive columns only, no backfill (pre-E-219 resolutions fall back to `resolution_report` in the UI). Whole file wrapped in `DO $do$ … EXCEPTION WHEN undefined_table` so it is a notice-only no-op where E-215 hasn't been applied. Mirrored in `schema.ts`. Applied to db-1 on 2026-07-30. **Applied to prod (database-2) on 2026-07-30** as part of the five-file security family, applied together in one transaction and verified afterwards against the catalog: all 6 `security_*` tables, 22 indexes (incl. both partial uniques) and the 7 added `security_findings` columns are present. Sandbox needed no separate apply: sandbox.itarang.com runs on database-1, so the db-1 tick above IS the sandbox tick — confirmed 2026-07-30 by querying db-1 directly (6 tables, 22 indexes, 7/7 added columns). | ✅ | ✅ | ✅ | ✅ |
| E-216_drive_expense_ingestion | Three new tables — `drive_expense_folders` (which Drive folders to scan; in the DB rather than env because prod's `shared/.env` is rewritten from `PROD_ENV_FILE_B64` on every deploy, so an env-configured folder list would silently vanish — includes `include_names text NOT NULL DEFAULT 'purchase'` — an ALLOWLIST: only files at or below a folder matching it are imported, scope inherited by descendants. This is the scanner's main safety property: an accounts folder holds both sides of the books and only the purchase side is an expense, while the Sale side holds CUSTOMER invoices already synced from Zoho, so importing them would inflate expenses by the whole turnover AND double-count revenue. An allowlist rather than a denylist because a denylist fails open — the live folder names its sales side four ways across two yearly conventions (`Sale`, `Sales`, `Sale Invoices`, `Sales Invoices`). Plus `exclude_names text NOT NULL DEFAULT 'sale'` as a second guard for a sales folder misfiled inside a purchase branch, and `drive_expense_files.folder_path` for the audit trail), `drive_scan_runs` (one row per scan, with per-outcome counters; a `status='running'` row is also the concurrency lock, in the DB not memory so it survives a pm2 restart mid-run), and `drive_expense_files` (one row per file per outcome — audit trail plus the work queue behind the needs-attention panel; `UNIQUE (drive_file_id, md5_checksum)` is what makes re-scanning a folder free rather than re-billing every file to GPT-4o). Plus four columns on `expense_submissions`: `drive_file_id` varchar(128), `drive_row_ref` varchar(64), `needs_attention` boolean NOT NULL DEFAULT false, `attention_reason` text. Drive rows deliberately keep `source='ai'` — `ExpenseLedgerPanel`, `ExpenseTrackerView`, the CEO `ai_expenses` query and E-172's dedup index all filter on it, so a new source value would hide every Drive row in four places and bypass invoice-number dedup; provenance rides on `drive_file_id` instead. Also adds `expense_submissions_effective_date_idx` on `COALESCE(expense_date, approved_at::date)`, which the CEO Expenses card / drill-down / Net tile now window on so a March invoice counts in March regardless of when it was scanned (previously `approved_at`, i.e. import time). **Required** — without it `/api/admin/ai-expenses/drive/*`, the cron route and the 6-hourly ticker all 500 with "relation drive_expense_folders does not exist", and Drizzle lists the four new columns in every `expense_submissions` INSERT so the existing manual upload + bulk import routes 500 too. **Apply BEFORE shipping the code.** New tables + additive columns + indexes only, no backfill. Mirrored in `schema.ts` except the partial-index predicates (`WHERE drive_file_id IS NOT NULL`, `WHERE needs_attention`, `WHERE status = 'approved'`, `WHERE is_active`, `WHERE status = 'running'`, `WHERE status IN ('needs_attention','failed')`) — migration-only, drizzle has no partial-index syntax. **First version of this file failed with 42P17** (`functions in index expression must be marked IMMUTABLE`): it indexed `COALESCE(expense_date, approved_at::date)`, and `timestamptz::date` is STABLE because it reads the session TimeZone. Fixed by dropping that expression index for a plain partial index on `expense_date` and splitting the COALESCE into an OR in the query helper — same semantics, no volatility. Pinning the zone would have re-bucketed rows across month boundaries. Verify with `node --import tsx --env-file=.env.local scripts/_dryrun-migration.ts drizzle/E-214_drive_expense_ingestion.sql` (applies inside a transaction, then rolls back).  **Renumbered**: authored as E-214/E-215 and applied to db-1 under those names before main landed its own E-214_whatsapp_onboarding_operators; renamed to avoid a fifth duplicate number slot. The DDL is unchanged and idempotent, so the db-1 tick still holds — re-running the renamed file there only rewrites the E-214:/E-215: prefixes inside COMMENT ON. | ✅ | ✅ | ☐ | ✅ |
| E-217_expense_currency_conversion | New table `fx_rates` (base/quote/rate_date/rate/source, UNIQUE on the triple) + five columns on `expense_submissions` (`currency`, `original_amount`, `fx_rate`, `fx_rate_date`, `fx_source`) and a partial index on non-INR currency. **Fixes a real wrong number**: `amount` is the single column every dashboard SUMs, so E-216 booked a $200 Anthropic invoice as ₹200 — $1,123 of SaaS spend counted as ₹1,123, understating expenses by ~₹95,000 and yielding a total denominated in nothing. `amount` is now ALWAYS INR (no downstream query changes — every report just becomes correct) and the face value moves to `original_amount` + `currency`, with the rate that connects them recorded per row. Rates are keyed by the INVOICE date, not today, so a month total cannot drift when recomputed; fetched from the ECB series via frankfurter.app and cached in `fx_rates`. Also **backfills two E-216 mistakes**: reclassifies `drive_expense_files` rows that DID produce an expense but were marked `needs_attention` (the admin panel reads that as "this spend is not on the dashboard" — it was on it), and releases `drive_scan_runs` stranded at `running` by the 300s request timeout, which then blocked the next scan for 30 minutes. **Required** wherever E-216 is applied — Drizzle names the five new columns in every `expense_submissions` INSERT, so the manual upload and Drive import routes 500 without it. Additive + idempotent (verified by applying the file twice inside one transaction). Mirrored in `schema.ts` except the partial index.  **Renumbered**: authored as E-214/E-215 and applied to db-1 under those names before main landed its own E-214_whatsapp_onboarding_operators; renamed to avoid a fifth duplicate number slot. The DDL is unchanged and idempotent, so the db-1 tick still holds — re-running the renamed file there only rewrites the E-214:/E-215: prefixes inside COMMENT ON. | ✅ | ✅ | ☐ | ✅ |
| E-218_expense_buckets | Two columns on `expense_submissions` — `bucket varchar(24)` (tech \| rm \| misc \| others) and `bucket_source varchar(16)` (rule \| ai \| manual \| default) — plus two partial indexes (`(bucket, expense_date) WHERE status = 'approved'` for the CEO rollups, `(bucket_source) WHERE bucket_source IS NOT NULL` for finding what the backfill still owes and what a human has pinned). Backs the **bucket dashboard inside the CEO Expenses drill-down**: four tiles that sum to the card's total, a month-on-month stacked comparison, a department strip, and filters — replacing a flat 200-row list with one number on it. `bucket` is the coarse layer ABOVE E-106's `department`: department answers whose budget, bucket answers what kind of spend (a Trontek battery order and an AWS bill are both Tech as a *department* but are RM Purchase and Tech *spend*). Deliberately a NEW column rather than reusing `category varchar(64) NOT NULL` — that one is dead for the AI pipeline (all three AI/Drive insert paths hardcode the literal `"Invoice"`) while still carrying eight real values for manual submissions, so repurposing it would corrupt both populations. **Free text, no CHECK and no enum**, per this table family's convention (cf. E-202_dealer_type) — the taxonomy is fixed in `src/lib/expenses.ts` (`EXPENSE_BUCKETS`) and enforced by zod at every write path, and a DB constraint would force a migration to add a bucket while `ALTER TYPE` on a drifting shared DB is what these conventions exist to avoid. **Required** — Drizzle names both new columns in every `expense_submissions` INSERT, so the manual upload, bulk import and Drive scan routes 500 with "column bucket does not exist" without it. **No backfill in the file on purpose**: classifying existing rows needs the rule engine plus model calls for the leftovers, which belongs in `scripts/_backfill-expense-buckets.ts` (report by default, `--apply` writes, only touches `bucket IS NULL` so it can never overwrite a `bucket_source='manual'` correction). Until it runs, old rows read as "Unclassified" — shown as its own tile so the tiles still sum to the header total rather than silently under-reporting. Additive + idempotent (verified by applying the file twice inside one transaction). Mirrored in `schema.ts` except the partial predicates (migration-only). **Applied to db-1 on 2026-07-28**, and the backfill run there the same day: 214 rows classified, 173 by rule and 41 by `gpt-4o-mini`, none falling through to the `others` default — ₹2.03 Cr RM Purchase, ₹9.8 L Tech, ₹9.2 L Miscellaneous. A second run reports "nothing to classify", confirming the `bucket IS NULL` guard. sandbox/prod still owe both the migration AND the backfill — applying the migration alone leaves every existing row reading as Unclassified. | ✅ | ✅ | ☐ | ✅ |

<!-- E-185/E-186 verified on db-1 (database-1.…ap-south-1) on 2026-07-13 by querying
     pg_tables / pg_enum / pg_trigger / pg_indexes directly: all 24 tables, 21 enum
     states, both immutability triggers and the one-AGREED-per-deal partial index are
     present, and catalog_variants is seeded (14 variants). The `sandbox` and `prod`
     columns are a DIFFERENT RDS instance each — still unapplied there.

     E-187/E-188 re-run + verified on db-1 on 2026-07-14 (idempotent re-apply, every
     statement skipped as existing): all 8 money/compliance tables, buyback_invoice_no_seq,
     invoices_one_live_per_deal_leg + settlement_transactions_leg_sub_unique +
     agreements_one_live_per_entity_role partial indexes, settlement_manual_needs_proof +
     settlement_amount_positive CHECKs, pickups variance/e-way columns and buyback_photos
     dedup columns all present.

     SANDBOX ticked for E-185..E-188 on 2026-07-14 because sandbox is NOT a separate
     RDS: its shared/.env DATABASE_URL points at db-1 (database-1.…/postgres), verified
     by reading the box env and querying through it. The "different RDS instance each"
     note above is stale for sandbox; it still holds for prod. -->

<!-- 2026-07-18 (session): db-2 (database-2.…, what .env.local's DATABASE_URL points at)
     was throwing `column "vendor_entity_id" does not exist` on every /api/user/profile
     and `buyback-dispatch tick failed` every 30s. Root cause was per-DB migration lag,
     confirmed by querying information_schema.columns on BOTH db-1 and db-2:
       - E-195 (users.vendor_entity_id) — applied to db-2, now ✅ on db-1 AND db-2.
       - E-196/E-197/E-198 (buyback proforma / owner-identity / attachment_s3_keys) —
         applied to db-2 (were already on db-1); attachment_s3_keys was the direct cause
         of the buyback-dispatch failure. All three now ✅ on db-1 AND db-2.
       - E-189 (inventory_transfers canonical ids) — ✅ on db-2 (pre-existing) but NOT
         applied on db-1: its guard RAISEs because db-1's inventory_transfers still holds
         the pre-0038 integer-PK design AND is non-empty, so it can't auto-rebuild. Left ☐
         on db-1 — needs a manual data migration or an empty-table window.
       - E-194 (buyback_pickup_addresses.owner_kind) — still MISSING on db-2. db-2's
         intake queue lacks the owner_kind column; left ☐, still to apply.
     Ticks reflect information_schema truth, not assumption. -->

<!-- 2026-07-25 (session): full sweep of BOTH dev DBs. Trigger was /nbfc/settings
     500-ing on `select … "consent_template_url" … from "nbfc_service_config"` — the
     Error Boundary mislabels it "Couldn't reach the database / connection timed out",
     but it was E-205 simply absent from db-1 (which .env.local's DATABASE_URL was
     pointing at, NOT db-2 — see the "DATABASE_URL sometimes flips" row above).
     Method: parsed every `E-*.sql` for the objects it asserts (CREATE TABLE / ADD
     COLUMN / CREATE INDEX / CREATE TYPE / ALTER TYPE ADD VALUE) and diffed that set
     against information_schema + pg_indexes + pg_enum on each DB, then applied only
     the files with genuinely missing objects.
       - db-1 gained: E-065, E-080, E-200_nbfc_doc_requests, E-201_nbfc_document_
         verifications, E-205, E-206, E-207, E-208, E-209, E-210, E-211. The whole
         NBFC Acquire interactive-loop family (E-200…E-210) was missing there.
       - db-2 gained: E-026, E-026B (index only), E-065, E-080, E-107_nbfc_docs_
         verified, E-108_nbfc_created_by_auth, E-148 (index only), E-189, E-194, E-211.
       - E-026B and E-148 were NOT run whole — E-026B carries the legal_name backfill
         UPDATE that once collapsed 15 nbfc rows onto one tenant, and E-148 rewrites FI
         state. Only the missing CREATE INDEX from each was issued.
       - E-189 correction: the 2026-07-18 note above has it backwards. db-1's
         inventory_transfers was ALREADY canonical (varchar ids, 6 rows) — nothing to
         do. It was db-2 that still carried the pre-0038 integer-PK table; it was empty,
         so E-189's guard rebuilt it cleanly. Both DBs are canonical now.
       - E-194 is now applied on db-2 (supersedes the "still MISSING" line above).
       - E-136's `field_investigations_lead_nbfc_unique` reads as missing on both DBs
         and MUST STAY THAT WAY — E-148 deliberately drops it in favour of the partial
         `field_investigations_current_unique`. Do not "fix" it.
     After the sweep, every object asserted by all 163 E-*.sql files exists on both
     db-1 and db-2, with that one intended exception. sandbox/prod NOT touched. -->

<!-- NUMBER COLLISION: E-185..E-188 exist twice — the risk-engine family
     (E-185_risk_card_verdict_source … E-188_risk_hypothesis_promotion, developed on
     Rushikesh-claude) and the peakAmp buyback family (E-185_buyback_core …
     E-188_buyback_compliance_trust, developed on main). Both were numbered from the
     same free slot before the branches merged. They are independent — no shared
     tables, no cross-family ordering — and each family is internally ordered, so both
     were kept rather than renumbered (they are already applied under these names).
     SAME STORY AGAIN at E-216..E-218 (and E-215): the security family
     (E-215_security_scanner, E-216_security_events, E-217_security_event_types,
     E-218_security_resolution_chat, E-219_security_finding_actions, developed on
     Rushikesh-claude) and the expense family (E-216_drive_expense_ingestion,
     E-217_expense_currency_conversion, E-218_expense_buckets, developed on main) were
     both numbered from the same free slot before the branches merged, so E-216, E-217
     and E-218 each exist twice. The two families are independent — the security ones
     touch only `security_*` tables, the expense ones only `drive_expense_*` / `fx_rates`
     / `expense_submissions` — and each family is internally ordered (security: 215 →
     216 → 217 → 218 → 219; expense: 216 → 217 → 218). Both families were already
     applied to db-1 under these names when the branches met, and the expense family is
     also on db-2 + prod, so — exactly as with E-185..E-188 — both were KEPT rather than
     renumbered. E-215 additionally holds a THIRD claim: it was applied to db-1 as
     E-215_expense_currency_conversion before that file was renamed to E-217, so the
     number 215 on db-1 means one of two different things depending on the date. Treat
     E-215..E-219 as strictly filename-keyed; the number alone tells you nothing.

     A THIRD TIME, at E-224..E-226: the NeoDove family (E-224_neodove_integration,
     E-225_neodove_campaign_mirror, E-226_neodove_call_recording_and_priority_dial,
     developed on Rushikesh-claude) and an unrelated main family
     (E-224_expense_department_rm_reclass, E-225_dealer_agreement_manual,
     E-226_oem_reference_prices) were again numbered from the same free slot before
     the branches merged, so 224, 225 and 226 each exist twice. The two families are
     independent — the NeoDove ones touch only `neodove_*` plus nullable columns on
     `dealer_leads` / `lead_touchpoints`, the main ones only `expense_submissions`
     (data-only), `dealer_onboarding_applications`, and `oem_reference_prices` /
     `dealer_lead_commercials` — and each family is internally ordered (NeoDove:
     224 → 225 → 226, since 225 and 226 each add a column to the `neodove_campaigns`
     table 224 creates; main: 224 → 225 → 226, mutually independent). Both families
     were already applied to db-1 — which IS sandbox — under these names when the
     branches met, so exactly as with E-185..E-188 and E-215..E-219 both were KEPT
     rather than renumbered.

     Next free number is E-257 (E-256 = product_master_gst_defaults, 2026-08-20; this line was written as E-230 here until 2026-08-18,
     nineteen numbers stale — do not trust this line without checking `ls
     drizzle/`). E-249..E-251 (ai_dialer vocabulary / intent_review_loop /
     sandbox_to_prod_drift) took 249/250/251 on main on 2026-08-18; the auction
     branch's own E-249/E-250 were RENUMBERED to E-252/E-253 on 2026-08-19 at
     merge (same treatment as E-241..E-244 -> E-245..E-248 the day before). Historic note:
     E-222_security_auto_block_event took 222 and
     E-223_scrap_vendor_onboarding took 223, both on 2026-07-31, and three files
     took 227/228/229 on 2026-08-06: E-227_scraper_multi_query,
     E-228_dialer_campaign_scheduling and
     E-229_component_vendor_department_reclass. Those three were authored in the
     same working tree within minutes of each other; 229 skipped past the first
     two deliberately rather than open a FOURTH duplicate slot. 224, 225
     and 226 are each TWICE-taken (see the note directly above):
     E-224_neodove_integration (2026-08-01) and E-224_expense_department_rm_reclass
     (2026-08-01); E-225_neodove_campaign_mirror (2026-08-03) and
     E-225_dealer_agreement_manual (2026-08-01);
     E-226_neodove_call_recording_and_priority_dial (2026-08-03) and
     E-226_oem_reference_prices (2026-08-03).
     (E-214 is TWICE-taken — whatsapp_onboarding_operators on main; drive_expense_ingestion was authored as E-214 and renumbered to E-216 to avoid a fifth duplicate slot, with expense_currency_conversion following as E-217 to keep the apply order; E-199 is TWICE-taken — nbfc_risk_card_visibility on main and, historically, a notifications one renumbered to E-200; E-200 is notifications_archive_delete). Match on the FULL filename, not the number alone.

     E-190_battery_spec_models was authored as E-189 and applied to db-1 + sandbox under
     that name, before E-189_inventory_transfers_canonical_ids landed on main. It was
     renumbered to E-190 rather than adding a THIRD duplicate slot. The DDL is unchanged
     and idempotent, so the db-1/sandbox ticks below still hold — re-running the E-190
     file against them is a no-op (it only rewrites two COMMENT ONs). -->

| E-220_lead_visit_meeting_mode | One nullable column `meeting_mode varchar(16) DEFAULT 'ground'` on `lead_visits`, plus an index on `(meeting_mode, actual_visit_date)`. Backs the CEO **Meetings (MTD)** report, which splits meetings by mode. `lead_visits` already records that a meeting happened, who held it (`asm_id`, resolves 10/10 to a user) and its outcome, but none of its 15 columns say HOW — so a WhatsApp meeting and a doorstep visit are indistinguishable once written, and WhatsApp meetings had nowhere to be recorded at all. A column rather than a separate table because a meeting is a meeting whatever the medium (same scheduling, same outcome, same follow-up); splitting by medium would fork every "did we meet this dealer" query and guarantee the halves drift. **Free text, no CHECK and no enum**, per this table family's convention (cf. E-202_dealer_type, E-218_expense_buckets) — vocabulary fixed in `src/lib/admin/types.ts` (`MEETING_MODES`) and enforced by zod at the write path. Backfills existing rows to `'ground'`, which is not a guess: they predate any other mode being recordable and the table carries `gps_check_in_lat/lng`, so it was built for physical visits; guarded by `WHERE meeting_mode IS NULL` so a re-run never overwrites a real value. **NOT required** to keep writes working — the column is nullable with a default, and the report catches `undefined_column` and falls back to treating every meeting as ground, so an unapplied environment renders the report with an empty WhatsApp column rather than 500ing. Additive + idempotent. Mirrored in `schema.ts` except the index. **Applied to db-1 on 2026-07-29** — verified present with `column_default 'ground'::character varying`, index `lead_visits_mode_date_idx` created, all 10 existing rows backfilled to `'ground'` and 0 rows left NULL. | ☐ | ✅ | ☐ | ☐ |

> Add a new row whenever you create an `E-<n>_*.sql` file. When `DATABASE_URL`
> points at a DB and you confirm a migration is present, tick that env's box.

| E-225_dealer_agreement_manual | Three nullable columns on `dealer_onboarding_applications` — `agreement_mode varchar(16)` (`esign` \| `manual`), `agreement_ref text`, `agreement_signed_on date`. Delivers the agreement half of what E-202 deferred ("the per-type agreement templates and per-type dashboards are deliberately a later change"): only NEW battery dealers have a drafted agreement to generate, so only they go through Digio; **scrap** and **new+scrap** dealers sign on paper and an admin uploads the scan on `/admin/dealer-verification/[dealerId]`. That upload route already existed (it completed a stalled e-sign) but was gated on a Digio `provider_document_id` and had nowhere to record which document it is or when it was signed. **`agreement_ref` is deliberately NOT `provider_document_id`** — the same rule E-223 set for scrap vendors: "we hold a scan" and "eSign completed" are different assurances, and one column would let the weaker satisfy a check written for the stronger. `agreement_mode` is the discriminator, so nothing has to infer intent from which columns are NULL. **No change to `agreement_status`** — it is `varchar(50)` with no CHECK, and the manual path reuses the existing terminal `'completed'`; a new status value would have to be taught to all ~6 sites that compare it to a string literal (`approve/route.ts`, `canInitiateStatuses`, `lib/agreement/status.ts`, `AgreementBadge`, `agreement-tracking`) and any one missed would read a fully executed agreement as incomplete. Mode and status are orthogonal: HOW it was signed vs WHETHER it is done. Free text, no CHECK, no enum per this family's convention (cf. E-202, E-218, E-220, E-221, E-223); vocabulary in `src/lib/dealer/dealer-capabilities.ts`, enforced by zod at the write path. Backfills `agreement_mode='esign'` **only** where `provider_document_id IS NOT NULL` (anything that reached Digio is by definition e-sign); rows with no agreement yet are left NULL on purpose rather than guessed at — `agreementModeFor(dealer_type)` derives the intended mode at read time, so writing one here would freeze a guess made before the dealer type was consulted. **REQUIRED** — Drizzle names all three columns in every `dealer_onboarding_applications` INSERT, so an unapplied environment 500s onboarding autosave/submit with "column agreement_mode does not exist". **Depends on E-202_dealer_type**, which is still `☐` on prod — apply that first there. Additive + idempotent. Mirrored in `schema.ts`. | ☐ | ☐ | ☐ | ☐ |

| E-226_oem_reference_prices | One new table `oem_reference_prices` (append-only OEM price book: `asset_type`, `product_id`, `oem_price numeric(14,2)`, `effective_from`/`effective_to`, `note`, `created_by`) plus two nullable columns on `dealer_lead_commercials` — `approval_mode varchar(16)` (`auto` \| `manual`) and `oem_evaluation jsonb`. Turns E-221's blanket CEO gate into a **price-aware** one: a quote whose every line is at or above the OEM reference auto-approves and releases immediately; one line below reference, one line the rep left unpriced, or one product with no reference on file sends the whole quote to the CEO queue. The mapping needed no new plumbing — `dealer_lead_commercials.product_lines` (E-128) already carries `product_id`, and it is literally `product_master_*.id::text` (`src/app/api/inside-sales/products/route.ts:31`), which is why **`product_id` here is `text` and not `uuid`**: matching the stored type keeps the hot lookup off a cast. **A table and not a column on `product_master_*`** because two of the three masters (`batteries`, `paraphernalia`) have no price column at all, only `chargers.base_price` does, and a column cannot hold history — the requirement is explicitly to preserve historical prices for audit. Same `valid_from`/`valid_to` shape `calc_component_prices` (E-177) already uses for prices that must reproduce a past quote. **No FK** — the parent is one of three tables selected by `asset_type`, so a single FK is not expressible (this family carries none anyway). **Append-only**: a revision closes the live row (`effective_to = now()`) and inserts a new one, never `UPDATE`s `oem_price`; the partial unique index `oem_reference_prices_live_uniq ON (asset_type, product_id) WHERE effective_to IS NULL` makes two live prices for one product unrepresentable, so "the latest price" is a lookup rather than a `MAX(effective_from)` race. **No change to `approval_status`** — it keeps its three values; a fourth would have to be taught to every site comparing it to a string literal and one missed site would read an approved quote as unapproved (the reasoning E-225 applied to `agreement_status`). `approval_mode` answers HOW, `approval_status` answers WHETHER. **Ships EMPTY on purpose — `product_master_chargers.base_price` is deliberately NOT backfilled in**: a "base price" and an "OEM reference price" are different assurances, and seeding one as the other would let a number nobody chose for this purpose start auto-approving quotes. Combined with the fail-closed rule (no reference ⇒ pending), day-1 behaviour is **identical to today** — everything still goes to the CEO — and auto-approval switches itself on product by product as real prices are loaded. No flag, no cutover, no risky moment. Neither new column is backfilled either: NULL is correct for a row decided before the rule existed. Free text, no CHECK, no enum per this family's convention (cf. E-202, E-218, E-220, E-221, E-223, E-225); vocabulary in `src/lib/leads/oemPricing.ts`, enforced by zod at the write path. **REQUIRED** — Drizzle names `approval_mode` and `oem_evaluation` in every `dealer_lead_commercials` INSERT, so the inside-sales commercials route 500s with "column approval_mode does not exist" without it. **Depends on E-221_lead_quote_approval**, which is still `☐` on sandbox and prod — apply that first there. Additive + idempotent. Mirrored in `schema.ts` except the partial unique index. **Idempotency proven against database-1 on 2026-08-03** by applying the file TWICE inside one transaction: the second run raised only `42P07`/`42701` "already exists, skipping" notices. The invariant was exercised in the same transaction — a second live price for one product is refused with `23505`, and after closing the first row a revision inserts cleanly leaving exactly one live row with the old price retained as history. **Applied to db-1 — which IS sandbox — on 2026-08-03** (both ticks below are that one apply, per the database-1-is-sandbox correction above). Verified afterwards: `oem_reference_prices` present with all 11 columns and 3 indexes (incl. the partial unique), both `dealer_lead_commercials` columns present, the table empty (0 rows — it ships empty by design) and all 13 existing commercials rows left with `approval_mode` and `oem_evaluation` NULL. **Landed as a side effect of the idempotency check**, not a deliberate apply: postgres.js sends a multi-statement `unsafe()` string over the simple query protocol, which does not enrol in the enclosing explicit transaction, so the DDL committed while the parameterised test INSERTs in the same block rolled back. Harmless here — the file is additive, was destined for db-1 anyway, and no existing row was touched — but the rollback in that harness cannot be trusted for DDL. **Observed present on database-2 as well on 2026-08-03**, checked after the db-1 apply: `oem_reference_prices` exists with all three indexes — **including the partial `oem_reference_prices_live_uniq`, which exists ONLY in this .sql file and not in `schema.ts`**, so the file itself was run there; `db:push` could not have produced that index. `approval_mode` and `oem_evaluation` are present too, as is E-221's `approval_status` (which this checklist still shows as `☐` for prod — that row is stale for database-2). database-2 carried 7,981 `dealer_leads` / 50 users / 112 onboarding applications and 0 rows in `oem_reference_prices` at the time of checking. **Who applied it and to which environment database-2 actually corresponds is NOT established here** — the four column labels below predate the "there are only TWO databases" correction above and cannot be trusted to resolve it. Left unticked for `prod` deliberately rather than guessed. | ✅ | ✅ | ✅ | ☐ |

| E-222_security_auto_block_event | **COMMENT-ONLY — no DDL, nothing to break.** Two `COMMENT ON COLUMN` statements on `security_events`: `event_type` gains `ip_blocked` (written when a request is refused because its source IP is under an automatic ban — see `src/lib/security/blocklist.ts`), and `evidence` gets its key taxonomy documented (`net` / `client` / `geo` / `request` / `flags` / `ban` / `body_sample` / `provenance`). **NOT required** for any code path: `event_type` is `varchar(32)` with no CHECK and no enum, `'ip_blocked'` is 10 characters, and the entire attacker profile rides in the existing `evidence` jsonb — so the auto-block feature and the enriched Live Attacks drawer work identically on a DB that never sees this file. It exists so the database documents the real taxonomy rather than a stale subset, exactly as E-217 did when the volumetric rules landed. Additive + idempotent; re-running rewrites the same two comments. Nothing to mirror in `schema.ts` (comments only). | ☐ | ☐ | ☐ | ☐ |

| E-221_lead_quote_approval | Four columns on `dealer_lead_commercials` — `approval_status varchar(16) DEFAULT 'approved'`, `approved_by text`, `approved_at timestamptz`, `rejection_reason text` — plus a partial index on `(created_at) WHERE approval_status = 'pending'` for the CEO queue. Closes a real gap: issuing a quote to a dealer was **ungated** (any `inside_sales_rep` / `asm` / `admin` could put any number in front of a dealer) while the DEAL that quote becomes needs three levels of approval. Every `quote_issue` / `quote_revision` now lands `pending` and waits for the CEO; the dealer sees nothing until approval. Columns rather than a new approvals table because `dealer_lead_commercials` is **already versioned** (`version_no` per lead, one `is_current` row, enforced by `dealer_lead_commercials_lead_version_uniq`) — that version IS the unit being approved, so keeping the decision on the same row means version and approval state cannot drift, rejection is moving `is_current` back to the newest APPROVED prior version, and a revision after rejection is the existing `version_no = max+1` path with no special casing. **Existing rows backfill to `'approved'`**, not pending: they predate the gate and many are already acted on — defaulting them to pending would retroactively invalidate every quote and flood the CEO queue with history. **REQUIRED** — Drizzle names all four columns in every `dealer_lead_commercials` INSERT (verified against the generated SQL), so the inside-sales commercials route 500s with "column approval_status does not exist" without it. Free text, no CHECK, no enum per this family's convention (cf. E-202, E-218, E-220); vocabulary in `src/lib/leads/quoteApproval.ts`. Additive + idempotent (verified by applying the file twice inside one transaction, then rolling back). Mirrored in `schema.ts` except the partial index. **Applied to db-1 on 2026-07-29** — all four columns present, `dealer_lead_commercials_pending_idx` created, the 13 existing rows backfilled to `'approved'` and 0 left NULL. The reject rollback was then exercised against this DB inside a rolled-back transaction: rejecting v3 correctly moved `is_current` to v2, and rejecting v2 as well fell through to v1, with exactly one current row at every step. | ☐ | ✅ | ☐ | ☐ |

| E-223_scrap_vendor_onboarding | Two new tables — `scrap_vendor_documents` and `vendor_portal_credentials` — plus three nullable columns on `scrap_vendors` (`udyam_number`, `agreement_ref`, `agreement_signed_on`). Backs the admin scrap-vendor onboarding form, which replaces the 7-field "+ Add vendor" modal: three mandatory documents (GSTIN certificate, PAN card, Udyam certificate), an optional manually signed agreement, a registered address, and a **generated** portal password emailed to the vendor (the NBFC E-002 pattern) instead of the vendor choosing their own on the public page. **`scrap_vendor_documents.entity_id` is NULLABLE on purpose** — the files are picked before the vendor exists, so the upload route inserts an unclaimed row and returns its id, and submitting claims it (`entity_id` + `claimed_at`); the alternative, letting the browser hand a storage key back at submit, would make a client-supplied string a storage path, which `src/lib/buyback/storage.ts` explicitly refuses. Hence the uniqueness rule is a **partial** `unique (entity_id, doc_type) WHERE entity_id IS NOT NULL` — unclaimed rows all have NULL and would otherwise collide. `vendor_portal_credentials` mirrors `nbfc_portal_credentials` including **no password column** (the plaintext is emailed, never persisted) and is one row **per dispatch attempt**, not per vendor, so a retry after a bounced email does not erase the evidence the first send was tried. `doc_type` is free text, no CHECK and no enum, per this family's convention (cf. E-202, E-218, E-220, E-221); vocabulary in `src/lib/buyback/vendor-docs.ts`, enforced by zod. **REQUIRED on any DB that has buyback** — Drizzle names `udyam_number`/`agreement_ref`/`agreement_signed_on` in every `scrap_vendors` INSERT, so vendor creation 500s with "column udyam_number does not exist" without it. **BUYBACK-ONLY**: sandbox and production have no buyback tables, so every block is guarded and the file is a clean no-op there. Additive + idempotent. Mirrored in `schema.ts` except the two partial indexes. **Applied to db-1 — which IS sandbox — on 2026-07-31** (both ticks below are that one apply, per the database-1-is-sandbox correction above): both tables and all three columns verified present. Idempotency and the partial-index semantics were proven first by applying the file **twice inside one transaction and rolling back**: two UNCLAIMED `GSTIN` rows (entity_id NULL) coexist, and a second CLAIMED `GSTIN` for the same vendor is refused with a unique violation. The whole feature was then exercised against this DB end to end (create → credentials email → ACTIVE, plus the bounced-email PENDING path and its retry) and the test rows removed. **NOT on prod (db-2)** — prod has no buyback tables at all, so the file is a guarded notice-only no-op there until the E-185..188 family is applied. **Authored and applied to db-1 as `E-222_scrap_vendor_onboarding`**, then renumbered to E-223 when `E-222_security_auto_block_event` landed on main the same day — same treatment as E-190_battery_spec_models, and for the same reason (do not add a further duplicate slot). The DDL is byte-identical and idempotent, so the db-1/sandbox ticks below still hold; re-running the file under its new name is a no-op. | ☐ | ✅ | ✅ | ☐ |
| E-224_neodove_integration | Three new tables — `neodove_campaigns`, `neodove_lead_links`, `neodove_sync_events` — plus two nullable columns on `dealer_leads` (`neodove_synced_at`, `neodove_sync_status`). Backs the NeoDove ⇄ iTarang two-way lead sync: the business runs telecalling in NeoDove (a Vyapar product) while the prospect pool, scraper, AI dialer and loan funnel live here, and the two share nothing. **The schema is shaped by one constraint: NeoDove has no read API.** Its entire programmable surface is a per-campaign "Custom Integration" endpoint we POST leads to, plus Workflows → Send Webhook firing back at us; nothing can be queried. So `neodove_sync_events` is **not a debug log, it is the reconciliation ledger** — the thing a NeoDove CSV export is diffed against to find and backfill webhooks that were silently dropped, since no query can recover them. Its partial unique index on `external_event_id WHERE direction='inbound'` is the inbound idempotency guard (same idiom as `whatsapp_messages.provider_message_id`: INSERT `.onConflictDoNothing()`, zero rows returned = already seen), backed independently by E-113's `lead_touchpoints_external_uniq` — a replayed webhook is refused in two places. **Deliberately NOT folded into `dialer_campaigns`**: that table drives the AI voice dialer (`advanceCampaign` claims rows `FOR UPDATE SKIP LOCKED` and places real Bolna calls; `/api/cron/ai-dialer` and `dialer-watchdog` sweep for `status='running'`), so a NeoDove row parked there would eventually be robot-dialled against an audience meant for human agents; the two are UNIONed at the API layer instead. **A link table rather than columns on `dealer_leads`** because one lead can sit in several NeoDove campaigns over time — columns would collapse that to the most recent push and destroy the per-campaign counters. **`push_endpoint_ref` stores the NAME of an env var, never the URL**: that URL carries an opaque per-campaign token and is the only credential NeoDove issues, so a DB leak must not confer write access to their system. The two `dealer_leads` columns are denormalised on purpose — the `/leads` list renders 50 rows a page and a "synced" chip must not cost a join against a per-(lead,campaign) table. Status/`push_status` are free text, no CHECK and no enum, per this family's convention (cf. E-202, E-218, E-220, E-221); vocabulary in `src/lib/neodove/types.ts`, enforced by zod. **NOT required for existing behaviour** — with zero rows and `NEODOVE_ENABLED` unset the runtime is byte-for-byte unchanged; **required before any NeoDove route is used.** Additive + idempotent (verified by applying the file twice inside one transaction against db-1, then rolling back). Mirrored in `schema.ts` except the three partial/unique indexes **and the two `dealer_leads` columns, which are deliberately NOT mirrored** — ~20 call sites do a bare `db.select().from(dealerLeads)`, which Drizzle expands to an explicit column list, so naming them would hard-fail the /leads list, both call schedulers and the role dashboards on any environment that hadn't applied E-224. They are written only through raw `sql` UPDATEs in `src/lib/neodove/*` (same treatment as `duplicate_merge_requests`). The one place absence DID bite was `/api/campaigns/unified`: a missing relation fails a statement at PARSE time, not with zero rows, so its NeoDove CTE took the AI-dialer half of the Campaigns tab down with it — the route now probes `to_regclass` and omits the CTE entirely when the tables are absent, so an unapplied E-224 degrades to AI-dialer-only instead of erroring. **Applied to db-1 — which IS sandbox — on 2026-08-01** (both ticks below are that one apply, per the database-1-is-sandbox correction above): all three tables, both `dealer_leads` columns and the three unique indexes verified present. Idempotency was proven first by applying the file **twice inside one transaction and rolling back**, and the unified-campaigns union was exercised in that same rolled-back transaction against a seeded campaign (2 links / 3 dispositions / 7 pushed mapped correctly). **NOT on prod (db-2)** — until it is applied there, the Campaigns tab renders AI-dialer rows only, which is the intended degradation and not an error. Apply with `node scripts/apply-e224.mjs` (verifies 3 tables / 2 columns / 3 unique indexes). **APPLIED to database-2 — which IS production — on 2026-08-12** (the first and last tick below are that one apply, per the `local (db-2)` = `prod` = database-2 pairing above). Applied as part of a db-1 → db-2 catch-up: a full `information_schema` diff found exactly eight migrations present on database-1 and absent on database-2 (E-224, E-225, E-226, E-232, E-233, E-234, E-235, E-236), and they were applied in numeric order — which is also dependency order, since `refurbishment_jobs` and `auction_lot_items` both reference E-232's `recovery_batteries`. Each file ran inside ONE transaction (E-232/233/234 carry their own `BEGIN`/`COMMIT` and were sent verbatim; the rest were wrapped by the runner), and all **54** expected objects — 8 tables, 30 columns, 16 indexes — were verified present on database-2 immediately afterwards by querying `information_schema` / `pg_indexes` rather than trusting the absence of an exception. Re-running the diff afterwards reported **0 tables and 0 columns** differing between the two databases in either direction. Every one of the eight is pure additive DDL — all eight were scanned for `INSERT`/`UPDATE`/`DELETE`/`TRUNCATE`/`DROP COLUMN`/`ALTER TYPE`/`SET NOT NULL` beforehand and contain **none**, so no production row was read or written by this run. The three `neodove_*` tables were created **empty** on production and no data was copied from database-1 — this was a schema catch-up, not a data migration. Prod therefore has the tables but no campaigns, no lead links and no sync events until somebody configures a campaign there and sets the matching `NEODOVE_PUSH_ENDPOINT_*` variables in the production environment. | ✅ | ✅ | ✅ | ✅ |

| E-225_neodove_campaign_mirror | One nullable `mirror_config jsonb` column on `neodove_campaigns` (E-224). Stores what a human has OBSERVED about the campaign inside NeoDove — pipeline, managing user, agent list, lead distribution — so a sales_head can see who is working a campaign without opening NeoDove. **Descriptive, never authoritative:** NeoDove has no campaign-creation and no read API (`docs/neodove-contract.md`), their Create Campaign form lives entirely in their UI, and writing this column changes nothing on their side — the modal says so on every field, for the same reason `push_endpoint_ref` stores an env var NAME: this integration has already been bitten once by state that looked configured and was not. **One jsonb rather than four typed columns** because these fields mirror a third-party form we neither control nor version (NeoDove adding a field should be a UI change, not another migration), and nothing joins, filters or aggregates on it — it is rendered and nothing else. Agents are free text because there is no API to fetch NeoDove's user list, so any dropdown would be a hand-maintained copy that goes stale silently whenever somebody joins or leaves. **REQUIRED before the campaign modal is used** — `POST /api/neodove/campaigns` and its PATCH name `mirror_config` in the statement whenever the form carries a mirror, and a missing column fails at PARSE time, so New campaign / Campaign settings 500 with `column "mirror_config" of relation "neodove_campaigns" does not exist`. Campaigns saved with every mirror field left blank omit the column entirely and work without it. Additive + idempotent. Mirrored in `schema.ts`. Apply with `node scripts/apply-e225.mjs` (verifies the column exists and is jsonb). **Applied to db-1 — which IS sandbox — on 2026-08-03** (both ticks below are that one apply, per the database-1-is-sandbox correction above): `mirror_config` verified present and of type `jsonb`. **NOT on prod (db-2)** — until it is applied there, saving a campaign with any NeoDove-settings field filled in 500s; a campaign with that whole block left blank still saves. **APPLIED to database-2 — which IS production — on 2026-08-12** (the first and last tick below are that one apply, per the `local (db-2)` = `prod` = database-2 pairing above). Applied as part of a db-1 → db-2 catch-up: a full `information_schema` diff found exactly eight migrations present on database-1 and absent on database-2 (E-224, E-225, E-226, E-232, E-233, E-234, E-235, E-236), and they were applied in numeric order — which is also dependency order, since `refurbishment_jobs` and `auction_lot_items` both reference E-232's `recovery_batteries`. Each file ran inside ONE transaction (E-232/233/234 carry their own `BEGIN`/`COMMIT` and were sent verbatim; the rest were wrapped by the runner), and all **54** expected objects — 8 tables, 30 columns, 16 indexes — were verified present on database-2 immediately afterwards by querying `information_schema` / `pg_indexes` rather than trusting the absence of an exception. Re-running the diff afterwards reported **0 tables and 0 columns** differing between the two databases in either direction. Every one of the eight is pure additive DDL — all eight were scanned for `INSERT`/`UPDATE`/`DELETE`/`TRUNCATE`/`DROP COLUMN`/`ALTER TYPE`/`SET NOT NULL` beforehand and contain **none**, so no production row was read or written by this run. | ✅ | ✅ | ✅ | ✅ |

<!-- E-219 is ANOTHER twice-taken number, and the two claims are of different
     kinds. On main it has NO .sql file: it is the CEO Realization dashboard — a
     code-only change (new /api/dashboard/ceo/overview + green-km routes, the
     Realization / Leads / Green KM / Responsible Recycling cards, and the
     expense drill-down filter row). Nothing was added to the schema, but the
     number is referenced throughout that code and in
     docs/ceo-dashboard-realization.md, so it was reserved rather than reused —
     which is why E-220_lead_visit_meeting_mode is the next SQL migration on
     that side. On Rushikesh-claude the same number was taken by a REAL
     migration, E-219_security_finding_actions.sql (see the collision note
     above). Both were kept at the merge. So "E-219" alone is ambiguous in a way
     the other collisions are not: it may mean a schema change or no schema
     change at all. Always match on the full filename. -->


| E-226_neodove_call_recording_and_priority_dial | Two nullable columns on `lead_touchpoints` — `recording_url`, `external_agent_name` — plus `is_priority_dial boolean NOT NULL DEFAULT false` and a partial unique index on `neodove_campaigns` (E-224). Closes the last two legs of the NeoDove requirement chain: what came back from a call is now visible on the lead, and a single lead can be handed to the calling team on demand. **The two touchpoint columns are DELIBERATELY NOT MIRRORED in `schema.ts`** — `lead_touchpoints` is the one table in this family written through Drizzle (`db.insert(leadTouchpoints)` in `src/lib/touchpoints/write.ts`) and Drizzle names every column of the table object in its INSERT, so listing them would break EVERY touchpoint write (calls, status changes, escalations, reactivations) on any DB without this file, prod included. They are written instead by a best-effort raw `UPDATE` in `attachCallEvidence()` (`src/lib/neodove/inbound.ts`), run **after** `writeTouchpoint`'s transaction commits — a failed statement aborts an open transaction in Postgres, so folding it into the `tx` would trade a recording URL for the whole touchpoint — and read back via `to_jsonb(t) ->> '...'`. Same treatment, same reason, as E-224's two `dealer_leads` columns. `is_priority_dial` IS mirrored, because every access to `neodove_campaigns` is raw `sql`. **`is_priority_dial` marks the single campaign the per-lead "Call now" action pushes into.** NeoDove has no dial API and no call control (`docs/neodove-contract.md`) — nothing the CRM sends can make a phone ring — so what makes such a lead urgent is the NeoDove-side lead distribution on that campaign, which is why the route says "queued for" and the modal says the CRM cannot dial. The partial unique index (`WHERE is_priority_dial`) enforces at-most-one; both the POST and the PATCH clear the incumbent first so ticking the box is a move rather than a constraint violation the operator cannot act on. **NOT required for existing behaviour**: without the touchpoint columns the webhook still writes its touchpoint and merely drops the recording URL and agent name (caught and warned, so this half is silent data loss rather than an outage — check it, don't wait for a bug report); without `is_priority_dial` the Call now button answers "no campaign is set as the priority-dial destination" and ticking the box in campaign settings 500s. On a DB with no `neodove_*` tables at all (prod today, pre-E-224) the second half is a **guarded no-op** and `scripts/apply-e226.mjs` reports it as SKIPPED, not FAILED. Additive + idempotent; both halves wrapped in `DO $do$ … EXCEPTION WHEN undefined_table` per CLAUDE.md. Apply with `node scripts/apply-e226.mjs` (verifies both touchpoint columns, the campaign column and its unique index, and distinguishes skipped-because-E-224-absent from failed). **Applied to db-1 — which IS sandbox — on 2026-08-03** (both ticks below are that one apply, per the database-1-is-sandbox correction above): `recording_url` + `external_agent_name` verified present on `lead_touchpoints`, and `is_priority_dial` present on `neodove_campaigns` with `neodove_campaigns_priority_dial_key`. It was authored on 2026-08-03 but **not applied until after it had already 500'd** — ticking the priority-dial box in Campaign settings failed with `column "is_priority_dial" does not exist` on the very DB the feature was built against. The file and its apply script both existed; nobody ran the script. That is the failure mode this checklist exists to catch, and it is worth noting that the row was written *predicting* the exact error message and still did not prevent it. **NOT on prod (db-2)** — prod has no `neodove_*` tables, so the second half is a guarded no-op there and the script reports SKIPPED; the touchpoint half would still apply and is worth running for that reason alone. **APPLIED to database-2 — which IS production — on 2026-08-12** (the first and last tick below are that one apply, per the `local (db-2)` = `prod` = database-2 pairing above). Applied as part of a db-1 → db-2 catch-up: a full `information_schema` diff found exactly eight migrations present on database-1 and absent on database-2 (E-224, E-225, E-226, E-232, E-233, E-234, E-235, E-236), and they were applied in numeric order — which is also dependency order, since `refurbishment_jobs` and `auction_lot_items` both reference E-232's `recovery_batteries`. Each file ran inside ONE transaction (E-232/233/234 carry their own `BEGIN`/`COMMIT` and were sent verbatim; the rest were wrapped by the runner), and all **54** expected objects — 8 tables, 30 columns, 16 indexes — were verified present on database-2 immediately afterwards by querying `information_schema` / `pg_indexes` rather than trusting the absence of an exception. Re-running the diff afterwards reported **0 tables and 0 columns** differing between the two databases in either direction. Every one of the eight is pure additive DDL — all eight were scanned for `INSERT`/`UPDATE`/`DELETE`/`TRUNCATE`/`DROP COLUMN`/`ALTER TYPE`/`SET NOT NULL` beforehand and contain **none**, so no production row was read or written by this run. | ✅ | ✅ | ✅ | ✅ |

| E-224_expense_department_rm_reclass | **DATA-ONLY — no DDL.** One guarded `UPDATE` moving every `department = 'tech' AND bucket = 'rm'` expense to `department = 'ops'`, plus a rewritten `COMMENT ON COLUMN expense_submissions.department`. Fixes a real wrong number on `/ceo`: E-218 taught BOTH classifier prompts the example *"a battery order raised by the tech team is department tech but bucket rm"*, and the model applied it faithfully — every Trontek / Ecostar / Ayansh / Trinity component invoice landed with the right bucket (`rm`) and the wrong department (`tech`). The by-department strip read **₹38.42L of "Tech" against ₹75K of Operations** for Jul–Aug 2026 while Tech had spent almost none of it. The invariant `bucket = 'rm' ⇒ department <> 'tech'` is now enforced in three places, and all three are needed: the prompts (new extractions), `resolveDepartment()` in `src/lib/expenses/departmentRules.ts` (every write path — Drive scan + bulk AI import — deterministically, beating the model), and this file (the rows already in the table, which re-prompting cannot reach). **NOT scoped to `source = 'ai'`** — the invariant is a property of the data, and a hand-entered battery invoice on the Tech budget is wrong for the same reason. **NOT REQUIRED for the app to run** — no code reads a column this file adds, so an unapplied DB simply keeps showing the old, wrong split; it is required for the dashboard to be *correct*. No CHECK constraint, per this family's convention (cf. E-202, E-218, E-220, E-221) and so an admin correction in the expense tracker still has the last word. `ai_raw` retains the original model output on every AI row, so the pre-reclass department stays auditable. **Two steps, in order.** Verifying the reclass against database-1 turned up a second, smaller bug first: `bucketRules.ts` carries a deliberately broad `electronics -> rm` catch-all (the safety net for the next unnamed "X Electronics" component supplier) and it was swallowing consumer-electronics RETAILERS — a ₹24,999 Samsung "Mobile Phone" and two Dawntech televisions, bucketed `rm`. They are IT hardware. Step 1 re-buckets those to `tech` (guarded by `bucket_source <> 'manual'`, the same guard the E-218 backfill script relies on) so that step 2 does not then move them off the Tech budget as raw material — the exact opposite of this migration's purpose. The matching code fix reorders `VENDOR_BUCKET_RULES` so a named retailer precedes the catch-all, which is the ONE place order in that array now matters (documented in the file and pinned by a test). **Measured against database-1 on 2026-08-01 before applying:** 97 rows / ₹2,20,35,690.63 at `tech`+`rm` (Trontek 68, Ecostar 24, Trinity 1, Neuron 1 — a genuine connector — plus the 3 retailer rows step 1 takes); approved spend by department moves `tech` ₹2,28,75,426 → ₹8,81,732 and `ops` ₹16,76,544 → ₹2,36,70,238. **Idempotency proven** by applying the file TWICE inside one transaction against database-1 and rolling back: run 1 raised "re-bucketed 3" then "reclassified 94", run 2 raised 0 and 0, and the rollback left all 97 rows untouched. **Applied to db-1 — which IS sandbox — on 2026-08-01** via pgAdmin (both ticks below are that one apply, per the database-1-is-sandbox correction above). Verified against database-1 afterwards and it landed exactly on the pre-computed projection: the invariant `bucket='rm' AND department='tech'` returns **0 rows**; approved spend reads `tech` ₹8,81,732 / 63 rows and `ops` ₹2,36,70,238 / 139 rows, matching the predicted ₹8,81,732 and ₹2,36,70,238 to the rupee. Step 1 confirmed too — the Samsung phone and both Dawntech TVs sit at `bucket='tech', bucket_source='rule', department='tech'`, i.e. they stayed on the Tech budget rather than being swept into Operations, and what remains in Tech is genuine tech spend (Anthropic, Hostinger, Decentro, Jio Haptik, iTarang Technologies). **NOT on prod (db-2)** — the code shipped to sandbox only (`deploy-production.yml` triggers on a push to `production`, which has not happened), so prod still shows the old split until this file is run there. Nothing to mirror in `schema.ts` (no schema change). | ☐ | ✅ | ✅ | ☐ |

| E-229_component_vendor_department_reclass | **DATA-ONLY — no DDL beyond one `COMMENT ON`.** One guarded `UPDATE` moving every `department = 'tech'` expense whose vendor matches `%trontek%` or `%ecostar%` to `department = 'ops'`, plus a restated `COMMENT ON COLUMN expense_submissions.department`. **This is E-224's fix re-keyed so that it works on production.** E-224 corrects the same misfiling but its predicate is `department = 'tech' AND bucket = 'rm'`, and `bucket` is E-218's column: on **database-2 (prod) the E-218 backfill was never run**, so 211 of 222 expense rows have `bucket IS NULL` — including every Trontek and Ecostar invoice — and E-224 applied there would raise `reclassified 0` while changing nothing. Keying on `vendor`, the one column that IS populated, this file is correct on a DB whose bucket backfill has run and on one whose has not. **Measured against database-2 on 2026-08-06 before applying:** 79 rows / ₹1,95,58,875.63 sit at `tech` (Trontek Electronics 56 rows ₹1,88,62,725.63; Ecostar Innovation 23 rows ₹6,96,150.00), all `status='approved'`, all `source='ai'`. Approved spend by department moves `tech` ₹2,04,08,098.97 → ₹8,49,223.34 (139 → 60 rows) and `ops` ₹40,81,770.45 → ₹2,36,40,646.08 (59 → 138 rows) — landing within ₹30k of sandbox's post-E-224 figures (`tech` ₹8,81,732.34 / `ops` ₹2,36,70,237.33), which is the cross-check that the two environments are being brought to the same place. **Only these two vendors** are named even though `VENDOR_DEPARTMENT_RULES` lists twelve: the other ten match zero rows on either DB, and this file is a record of a specific measured correction, not a second drifting copy of a list that already lives in TypeScript. **NOT scoped to `source='ai'`** — same reasoning as E-224, the rule is a property of the vendor rather than of who typed the row. **On sandbox (db-1) it is a deliberate no-op** — E-224 already moved those rows on 2026-08-01, so it matches 0 rows there and proves the environments agree. **Idempotency + syntax proven** by applying the file TWICE inside one transaction against database-1 and rolling back: both runs raised `reclassified 0`, all four snapshot figures were unchanged mid-transaction and after, and a fresh connection confirmed the `COMMENT ON` did **not** survive the rollback (so this file leaves no trace when rolled back — cf. `pg_unsafe_ddl_escapes_rollback`, which does not bite here). The production effect was verified read-only, by SELECT projection rather than a rolled-back UPDATE. **NOT REQUIRED for the app to run** — no code reads a column this file adds; an unapplied DB simply keeps showing the wrong department split. Ships with the matching code fix: `POST /api/admin/ai-expenses` now runs `resolveDepartment()` like the Drive scan and bulk import already did — it was the last write path that took a submitted department on trust, and since `ExpenseTrackerView` prefills that select from the extractor's own answer, it was a live route back to the same bug. Nothing to mirror in `schema.ts` (no schema change). **APPLIED to database-1 (sandbox) AND database-2 (production) on 2026-08-06** via pgAdmin, and verified read-only afterwards on both. Production landed exactly on the pre-computed projection: `department='tech'` with a component vendor returns **0 rows**, approved `tech` reads ₹8,49,223.34 / 60 rows against the predicted ₹8,49,223.34, and the `COMMENT ON` is present (the comment now names both E-224 and E-229). The two environments have converged to the rupee — Trontek 72 rows / ₹2,19,02,848.63 and Ecostar 29 rows / ₹9,97,973.00, all in `ops`, identical on both DBs — which is the real check that this file did on production what E-224 did on sandbox. What remains in Tech on both is genuine tech spend (iTarang Technologies, Decentro, Jio Haptik, Anthropic, Hostinger, Maruti Nandan Telecomm, Kartik Aggarwal). Note the sandbox row counts are 2 higher than the pre-apply measurement on both DBs (+₹2,34,310 in `ops`): those are new invoices ingested between measuring and applying, not an effect of this file. | ✅ | ✅ | ✅ | ✅ |

| E-227_scraper_multi_query | Adds `scraper_runs.last_progress_at`, `scraper_run_chunks.query_text` and `.city`, plus three indexes (`idx_scraper_run_chunks_run_id`, partial `idx_scraper_run_chunks_pending`, partial `idx_scraper_runs_running`). Groundwork for entering **multiple search queries and multiple cities in one scrape** — the operator previously ran one query at a time, and the global single-run lock (409 while any run is `running`) forced them to wait between each. **The query×city fan-out already existed**: `startChunkedRun()` crosses AI query variations with AI-chosen cities and writes one `scraper_run_chunks` row per combination; the change is only that the two lists become user-supplied. That moves a run from ~300–450 combinations to ~1,500 (10 user queries → ~150 AI variations × 10 user cities, since the team chose to KEEP AI expansion on top of the explicit lists), which turns two pre-existing behaviours from untidy into fatal. **`last_progress_at` is the run-killer fix**: `reapStuckRuns()` (`src/lib/scraper/storage/runStore.ts`) force-fails ANY run whose `started_at` is older than `STUCK_RUN_THRESHOLD_MS` = **10 minutes**, and it is called at the top of `POST /api/scraper/run` and `/api/scraper/cron`. A 1,500-chunk run takes hours, so it would be stamped `failed` at minute 11 while its chunks kept working — the row lies, finalize never fires, and the leads strand in `scraper_raw`. The column lets the reaper reap on **silence** (`COALESCE(last_progress_at, started_at) < now() - interval '20 minutes'`) instead of on age; it is bumped inside the SAME atomic statement that already does `completed_chunks = completed_chunks + 1` in `executeChunk()`, so it costs no extra round-trip. `COALESCE` is load-bearing — rows written before this file have a NULL heartbeat and must keep falling back to `started_at`, or a run in flight at deploy time becomes unreapable. **`query_text`/`city` exist because `combination_query` is lossy**: it stores only the joined `"<variation> in <city>"` string, and since a variation may itself contain `" in "` it cannot be split back apart reliably — yet "which of my ten cities actually produced dealers?" is the first question asked of a finished multi-city run. Both are nullable; pre-E-227 chunks have no components to backfill and the UI renders NULL as unattributed rather than guessing. **Deliberately NOT `scraper_city_queue`** — that table already exists with almost exactly the right columns but **nothing writes it** (dead from an earlier design); adopting it would mean maintaining a second work queue beside the one the pipeline actually drives, and keeping both consistent through cancel/finalize. Two columns on the live table is far less surface; `scraper_city_queue` is left untouched. **The indexes matter at this scale**: `scraper_run_chunks` had **no index on `run_id` at all**, while `/api/scraper/runs/[id]/progress` polls `WHERE run_id = ? GROUP BY status` every 2.5s — a sequential scan per tick over ~1,500 rows for the whole multi-hour run. The partial `(status, created_at)` index serves the new in-process chunk-drain ticker, which claims work with `WHERE status='pending' ORDER BY created_at LIMIT n FOR UPDATE SKIP LOCKED` on a table whose pending rows shrink to a minority. **REQUIRED** — Drizzle names every column of a table in its INSERT/UPDATE statements, so an unapplied DB throws `column "last_progress_at" does not exist` on the first chunk write and the scraper is dead, not degraded. **Schema-mirror caveat:** `scraper_runs` is declared TWICE in `src/lib/db/schema.ts` — `scraperRuns` (~line 2592) and `scrapeRuns` (~line 3193), both `pgTable("scraper_runs")` with different column sets, and call sites import whichever alias they were written against. The mirror touches `scrapeRuns` only (the alias `chunkedPipeline`/`runStore` use), and `idx_scraper_runs_running` is intentionally left out of `schema.ts` so drizzle-kit could never emit it twice. Reconciling that duplication is a separate change with its own blast radius and was not attempted here.  **VERIFIED PRESENT on db-1 — which IS sandbox — on 2026-08-16** (both middle ticks below are that one verification), found while probing as a hard prerequisite of E-241: a read-only `information_schema.columns` query returned `scraper_runs.last_progress_at`, `scraper_run_chunks.query_text` and `scraper_run_chunks.city` all present, so this file had already been applied there despite the row reading ☐ on every environment — the same probe-before-you-assume case the E-227 columns caused once before. It was not re-applied; the boxes were ticked to match what the database actually says. **The heartbeat halves are only NOW wired up**, by E-241: `executeChunk()` stamps `last_progress_at` through `touchRunProgress()` (a separate best-effort raw `sql` UPDATE issued after the chunk accounting commits, so a database without this file loses the heartbeat and never the chunk count), and `reapStuckRuns()` reaps on `COALESCE(last_progress_at, started_at) < now() - interval '20 minutes'` instead of on total run age. Both latch a `42703` SQLSTATE once and fall back to the old 10-minute age rule, so an unapplied E-227 degrades rather than breaking. `scraper_run_chunks.query_text`/`.city` are also finally written — populated at fan-out from the two halves of the combination, which `combination_query` cannot be split back into reliably because a query may itself contain " in ". **Still ☐ on database-2 — which IS production** — not probed on 2026-08-16. E-241 depends on this file and its applier refuses to run without it. | ☐ | ✅ | ✅ | ☐ |

| E-228_dialer_campaign_scheduling | Adds `window_start varchar(5)`, `window_end varchar(5)`, `window_days jsonb`, `paused_at`, `resume_after`, `last_advanced_at` to `dialer_campaigns`, plus partial `idx_dialer_campaigns_resume`. Introduces the campaign status value **`scheduled`**. Makes the AI dialer respect a **business-hours window** (e.g. 11:00–15:00 IST) on selected weekdays and **resume automatically the next configured day** from the leads that remain. Today a campaign has no clock at all: `advanceCampaign()` places one call, the provider webhook finalizes it, and `finalizeCall()` re-enters `advanceCampaign()` 5s later — a chain reaction that runs until no `pending` rows remain in `dialer_campaign_leads`. Nothing in that loop asks the time, so a campaign started at 18:00 dials dealers through the night, and one that doesn't finish simply stalls until a human clicks Resume. **Why a new status and not a reuse of `stopped`:** `stopped` means "a human pressed Force stop" — `/api/ai-dialer/campaigns/[id]/stop` drains in-flight rows to `failed`, `/resume` gates on exactly that value, `campaign-status-badge.tsx` renders a grey "Stopped" pill, and reports count it as an abandoned run; overloading it would make all three lie about a campaign that is merely waiting for 11am. `scheduled` also buys the most important safety property for free — **both** watchdogs (`/api/cron/dialer-watchdog` and the in-process ticker in `src/instrumentation-node.ts`) select `WHERE status = 'running'`, so a paused campaign leaves their sweep with no change to their queries. Free text, **no CHECK and no pgEnum**, per this family's convention since E-202 (also E-218, E-220, E-221): the vocabulary lives in TS and is enforced by zod at the write path, and `ALTER TYPE` on a shared drifting DB is what these conventions exist to avoid. **No new settings table**: the window is per-campaign, pre-filled from a global default, and `assignment_config` (E-120) already carries `working_hours_start`/`working_hours_end`/`working_days` — columns currently read by **nothing** (only `intent_score_threshold` is consumed, by `finalizeCall.ts`) and already exposed through a validated admin API and form. `varchar(5)` `'HH:MM'` + a jsonb day array mirrors that table's shape so one predicate module reads either without translation. **`resume_after` is stored, not computed**, so the 60s ticker answers "which campaigns are due?" with an indexed `status='scheduled' AND resume_after <= now()` rather than loading every paused campaign and running a date computation per row — and so a PM2 restart mid-pause is a non-event, since the state is on disk. **`last_advanced_at` fixes the morning-after trap**, the subtlest thing in this file: `/api/cron/dialer-watchdog` force-finalizes a campaign as `stopped` when `campaignAgeMs > STALL_FINALIZE_AGE_MS` (2h) with no lead activity in 10 minutes, measuring age from `started_at`. A campaign paused overnight still carries YESTERDAY's `started_at`, so the instant the ticker flips it back to `running` it is hours old with no recent activity and the watchdog would kill it before its first call of the day — every day. Stamping `last_advanced_at` on each successful advance and measuring from `COALESCE(last_advanced_at, started_at)` is what makes multi-day resumption survive contact with the watchdog. **Backward compatible by construction:** every existing campaign has NULL window columns, and NULL means UNSCHEDULED — dial continuously, no gate, exactly today's behaviour. Nothing is retroactively constrained by this file; the only thing that reaches old campaigns is the provider-level safety net in `triggerBolnaCall`/`triggerElevenLabsCall`, which is code, not schema. **REQUIRED** — Drizzle names every column of `dialer_campaigns` in its INSERTs, so an unapplied DB throws `column "window_start" does not exist` the moment ANY campaign is created, scheduled or not. Depends on E-109 (`dialer_campaigns`, `dialer_campaign_leads`) and, for the global default only, on E-120 (`assignment_config`); neither is altered here. | ☐ | ☐ | ☐ | ☐ |

| E-230_oem_price_validity | Adds `valid_until timestamptz` and `expiry_notified_at timestamptz` to `oem_reference_prices`, swaps `oem_reference_prices_live_uniq` for `oem_reference_prices_open_from_uniq ON (asset_type, product_id, effective_from) WHERE effective_to IS NULL`, and adds the partial `oem_reference_prices_expiry_idx`. **Makes E-226's price book DATED**, which is what the 2026-08-06 requirement actually asked for (`docs/quotation-approval-flow.md` §3): a price carries a validity period ("50,000 is valid until August 1st"), you change it by **adding a second line for the same model starting after the first ends**, a notification warns Admin + CEO before a window lapses, and — the part that changes the approval rule's behaviour — **the lookup resolves against the validity dates**, i.e. a quote is judged against the price in force on ITS OWN date, not against the newest row. None of that was expressible: E-226 rows have no end date, and its partial unique index made a queued successor literally impossible to insert. **`valid_until` and `effective_to` are deliberately NOT the same column.** `valid_until` is the admin's declared intent; `effective_to` means "superseded by a revision". A row whose `valid_until` has passed while `effective_to` is still NULL is **expired** — the product has no reference price and its quotes quietly stop auto-approving — and that state is exactly what the expiry notification exists to prevent, so collapsing the two would erase the difference between "we replaced this" and "this ran out". **The index swap is the one non-additive statement in the file** (called out because CLAUDE.md's default is strictly additive): it drops no data and no column, the replacement is created in the same `BEGIN/COMMIT`, and the degenerate case E-226 was guarding — two open rows for one product starting at the same instant — stays refused with `23505`. What is now legal is two open rows with *different* starts: the one in force plus the queued successor. **Window non-overlap is enforced in the write transaction, not in SQL** — `setOemPrice()` (`src/lib/leads/oemPrices.ts`) locks the product's open rows `FOR UPDATE`, supersedes the line it replaces by stamping `effective_to` at the new line's start (so history reads contiguously, with no gap), and REFUSES a line that would straddle an already-queued one rather than silently deleting the admin's schedule. A `btree_gist` `EXCLUDE` constraint would say this in SQL but needs `CREATE EXTENSION` on a shared drifting DB, and this table family has enforced its vocabulary at the write path by convention since E-202. **No backfill, and that is load-bearing**: every existing row keeps `valid_until` NULL, NULL means open-ended, and open-ended is precisely what those rows have meant since E-226 — so applying this file changes the behaviour of exactly zero existing prices. Stamping a guessed expiry would instead start firing "your price is about to lapse" at Admin and CEO about prices nobody ever dated. **REQUIRED** — `setOemPrice()` writes through the Drizzle query builder, which names every column of the table in its INSERT, so an unapplied DB throws `column "valid_until" does not exist` the first time anybody saves a price and `/oem-pricing` is dead rather than degraded. The read path fails differently and more quietly: `listOemCatalogue()` and `loadLiveOemPrices()` are raw `sql` and select `valid_until` explicitly, so they too error rather than silently reverting to E-226 semantics — which is the outcome you want, since silently ignoring validity would auto-approve quotes against expired prices. **Depends on E-226_oem_reference_prices** — apply that first; on a DB without it the whole file fails on the first `ALTER`. Mirrored in `schema.ts` (both columns; neither partial index, per E-226's precedent). **APPLIED to db-1 — which IS sandbox — on 2026-08-07** via pgAdmin (both ticks below are that one apply, per the database-1-is-sandbox correction above). Verified read-only afterwards with `node scripts/apply-e230.mjs --check`: `valid_until` + `expiry_notified_at` present, `oem_reference_prices_open_from_uniq` and `oem_reference_prices_expiry_idx` created, and **`oem_reference_prices_live_uniq` gone** — the index swap is the half worth confirming, since a DB left holding the old index would refuse the queued successor line that is the whole point of the file. The no-backfill claim was checkable rather than asserted here only because the table was **empty (0 rows)** at apply time: E-226 ships empty by design and no price had ever been set on this DB, so the column adds and the index swap touched no data at all. **This file was authored and shipped BEFORE it was applied, and that is how the gap was found** — `/oem-pricing` rendered "Couldn't load the OEM price book" and `[instrumentation:oem-price-sweep]` logged a failed query on its first tick. Worth recording because it contradicts the usual assumption about this family: E-230 breaks the **READ** path, not just writes. `listOemCatalogue()` and `listExpiringOemPrices()` both name `valid_until` explicitly, so an unapplied DB gives a dead screen rather than a degraded one — there is no silent-fallback mode to mistake for working. Post-apply the app path was re-verified end to end against the live dev server: `GET /api/dashboard/ceo/oem-prices` → **200**, `GET /api/dashboard/ceo/oem-prices/battery/<id>/history` → **200**, and the catalogue reads **11 active models (3 battery, 3 charger, 5 paraphernalia), 0 priced, 0 scheduled** — which is the correct day-1 state: with the register empty and the rule fail-closed, every quotation still goes to the CEO exactly as before, and auto-approval switches on model by model as prices are entered. ~~**NOT on prod (db-2)**~~ — **STALE, corrected 2026-08-08.** That claim was written when this file shipped and was never revised once the file was actually run. **E-230 IS on database-2**, confirmed read-only on 2026-08-08 with `node scripts/apply-e230.mjs --check --env-key=…`: `valid_until` + `expiry_notified_at` both present, `oem_reference_prices_open_from_uniq` and `oem_reference_prices_expiry_idx` both created, and **`oem_reference_prices_live_uniq` absent** — the check queries the catalog for all three index names at once, so live_uniq missing from the result proves the index SWAP completed rather than merely that the new indexes were added, which is the half worth confirming here. The table reads **0 rows / 0 open** on prod, so the "no backfill" guarantee is trivially intact there. **E-231's row below already said this** ("the pre-apply probe also found E-229 and E-230 already present on database-2 … those two claims are stale, not this one") — the note was correct and this row simply never got the tick, which is exactly how a file gets re-applied by someone trusting the boxes over the prose. The parenthetical about `deploy-production.yml` stands and is unaffected: the DB is now ahead of the code on prod, which is the safe order for this file, since an applied E-230 is invisible to E-226-era code (both new columns are nullable and nothing reads them) whereas the reverse is a dead `/oem-pricing`. Nothing was applied on 2026-08-08 — this entry is a correction to the record, not a new apply. | ✅ | ✅ | ✅ | ✅ |

| E-231_notification_access | One new table, `notification_access` — composite PK `(dashboard, notification_type)`, `enabled boolean`, `updated_by text`, plus a `CHECK (dashboard = lower(dashboard))`. Backs the admin **Settings → Notification Access** screen, which decides which notification types reach which dashboard's in-app bell. **NO ROW = ENABLED** — the table holds only decisions a human actually made, so it ships empty and day-1 behaviour is byte-identical to today (cf. E-226). **`enabled` is never DELETEd**: re-enabling upserts `true`, so "who turned this back on, and when" survives; the hot path reads only `WHERE enabled = false`, so the `true` rows cost it nothing. The lowercase CHECK is shape, not vocabulary (cf. E-202/E-218/E-220/E-226 on why this family carries no vocabulary CHECKs): `emit.ts` matches roles with `LOWER(users.role)` because seeded roles are inconsistently cased, and a mixed-case row here would match nothing — the gate would fail OPEN, i.e. appear to work while doing nothing, which is this feature's most dangerous silent failure. **Scope is the in-app bell only** — a blocked recipient still gets the email, so an admin cannot silence an agreement link from this screen. **NOT required for existing behaviour**: `src/lib/notifications/access.ts` fails open on any error, so on a database without this file the gate reads zero denials, logs once per 60s cache window, and every notification is delivered exactly as it is today. That is deliberate and is the reason default-on is not negotiable — there is no migration runner here, migrations have silently stopped applying on prod before (~E-145 above), and a deny-by-default gate would have turned the entire CRM bell off with no error anywhere. Mirrored in `schema.ts` as `notificationAccess` (the CHECK is not — drizzle's builder has no CHECK syntax, so the migration is the source of truth). **Numbered E-231, not E-227**, because `docs/battery-auction-flow.html` reserves E-227→E-230 for the battery-auction design; those files do not exist yet, so E-227 is free on disk but spoken for on paper. Additive and idempotent — safe to re-run. Apply with `node --env-file=.env.local scripts/apply-e231.mjs`, which applies the file and then PROVES the objects exist (columns, the 2-column primary key, the CHECK, row count) rather than trusting the absence of an exception. **Applied to db-1 — which IS sandbox — on 2026-08-06** (both ticks below are that one apply, per the database-1-is-sandbox correction above). Verified afterwards: all five columns present, primary key `(dashboard, notification_type)`, `notification_access_dashboard_lower` CHECK present, 0 rows. **Idempotency proven** by running the script twice — the second run reported "table already present" and re-applied cleanly with no error and no rows. **The CHECK was proven to bite**, not just to exist: inserting `dashboard='Sales_Head'` was rejected with `23514 notification_access_dashboard_lower`, while `'sales_head'` was accepted — that constraint is the only thing standing between a mixed-case row and a gate that silently fails open. Test rows were deleted; the table is back to 0. It was authored and shipped BEFORE being applied, and announced itself exactly as this checklist predicts — `relation "notification_access" does not exist`, a 500 on `GET /api/admin/notification-access` the first time the tab was opened. Note that the read path is designed to fail OPEN, so the only casualty was the settings screen; every notification in the CRM kept being delivered normally throughout. **APPLIED to database-2 (prod) on 2026-08-07** via `scripts/apply-e231.mjs`, ahead of the code reaching prod — which is the safe order for this file and avoids the 500-on-load the sandbox saw. Verified by the script immediately afterwards on database-2: all five columns present, primary key `(dashboard, notification_type)`, `notification_access_dashboard_lower` CHECK present, 0 rows. It ships empty, so prod bell behaviour is unchanged until somebody unticks something. The pre-apply probe also found E-229 and E-230 **already present on database-2** even though both of their rows below still read "NOT on prod" — those two claims are stale, not this one. | ✅ | ✅ | ✅ | ✅ |

| E-232_auction_catchup_battery_master_bidder_repoint | Phase 0 of the battery-auction build (`docs/battery-auction-flow.html`). Three halves. **PART 1 is catch-up DDL and is the half that matters most**: six of the nine tables in the auction/recovery family have **no migration file anywhere** — `auction_lots`, `auction_bids`, `auction_settlements`, `nbfc_recovery_pipeline`, `nbfc_auction_lot_actions`, `nbfc_auction_cancel_requests`. They exist only in `schema.ts` and in whichever database happened to receive a `db:push`; only `auction_auto_bids` (E-093), `nbfc_battery_evaluations` (E-119) and `nbfc_buyback_requests` (E-118) were ever migrated properly, and E-129 seeds demo rows into a table it never creates. That is not a tidiness complaint — `ALTER TABLE auction_lots ADD COLUMN …` aborts the **whole file** with `42P01` on a database that never got the push, so the `CREATE TABLE IF NOT EXISTS` statements are in the same file, ahead of the ALTERs, and PART 1 is a pure no-op wherever the push landed. Transcribed from `schema.ts` verbatim (column order, types, nullability, defaults, index names; unnamed `.unique()` becomes Postgres's own `<table>_<column>_key`) so the two cannot drift further. **PART 2** adds `recovery_batteries`, the battery master BRD §3 asks for — serial (globally UNIQUE: it is what an operator reads off the casing, and the same battery must not exist twice because it changed hands), model, capacity, condition_grade, recovery_date, warehouse, lat/lng, city/state, loan_sanction_id, `image_urls text[]`, `state_code`. **Deliberately a separate table, not twelve more columns on `nbfc_recovery_pipeline`**, because the two have different lifetimes: a pipeline row is a workflow position ending at `resold`, while a battery is a physical asset that outlives it and can come back through recovery again. `state_code` is named that way because `state` on the same row is the geographic state. **PART 3 is the bidder re-point — the structural change of this BRD.** `auction_bids.tenant_id` is today the *bidding NBFC's* tenant; the BRD (§9) says dealers are the only bidders and other NBFCs must never see a lot. `bidder_dealer_id` is added **ALONGSIDE `tenant_id`, never instead of it**: `tenant_id` is NOT NULL and dropping it would rewrite the table, existing NBFC-to-NBFC bids stay readable, and it can be retired later in its own migration per CLAUDE.md's two-step rule. On a dealer bid **both** columns are populated — `tenant_id` carries the **seller's** tenant, which is what keeps the `nbfc_audit_log` row for that bid in the right tenant's log and every existing tenant-scoped index on the table meaningful — and `bidder_kind` (default `'nbfc'`, which is exactly what every existing row is) says which one is authoritative so nothing infers it from NULLness. `auction_lots.seller_tenant_id` **fixes a shipped defect for free**: `approveWinningBid()` writes a zero-UUID sentinel into `auction_settlements.seller_tenant_id` because the seller genuinely is not knowable at that layer, and `listSettlements()` then filters `seller_tenant_id = caller` — so **every settlement that route has ever created is invisible to every NBFC**. Also adds `auction_type`, `starts_at`, `anti_snipe_seconds` (default 120), `reserve_price` (so `setReservePrice()` stops overwriting `base_price` and destroying the published starting price), `title`, `published_at`; `auction_settlements.winner_dealer_id`; and `nbfc_recovery_pipeline.battery_id` — the real key replacing the string join in `settlements.ts` that matched `battery_serial` against `auction_lots.lot_code`, two values that are never equal, so marking a battery `resold` has never once worked. **`auction_lots.status` gains `draft` and `scheduled` but the SQL DEFAULT stays `'live'`** — changing it would silently alter every INSERT that omits the column, so the draft default is applied in code where it is reviewable. **No CHECK constraints**, per this family's convention (cf. E-202/E-218/E-220/E-226/E-231): the status vocabulary is enforced at the write path in `src/lib/nbfc/auction/`. **Numbered E-232, not E-227**, and the reservation note in E-231's row below is now **moot**: E-227 and E-228 were taken on 2026-08-06 by `E-227_scraper_multi_query` and `E-228_dialer_campaign_scheduling`, and E-229/E-230/E-231 are likewise taken — so the whole E-227→E-230 block `docs/battery-auction-flow.html` reserved on paper is gone, and the battery-auction files take a fresh contiguous block E-232 → E-234. **REQUIRED once `schema.ts` mirrors these columns** — Drizzle names every column of the table object in its INSERTs, so an unapplied database throws `column "bidder_kind" does not exist` on the first bid or lot write. PART 1 alone is safe to run anywhere. Mirrored in `schema.ts` (`recoveryBatteries` plus the new columns on `auctionLots`/`auctionBids`/`auctionSettlements`/`nbfcRecoveryPipeline`); the two **partial** indexes `auction_lots_open_due_idx` and `auction_lots_close_due_idx` — the scheduler's two hot queries, partial because draft/ended/cancelled lots accumulate forever and the ticker never looks at them — are NOT mirrored, per the E-226/E-230 precedent that drizzle's builder cannot express them. Additive and idempotent throughout — safe to re-run. Apply with `node --env-file=.env.local scripts/apply-e232.mjs`, which reports **per table whether it pre-existed** (that line is the actual finding — it tells you whether this database ever got the `db:push`) and then PROVES every column, the `text[]` type of `image_urls`, the `'nbfc'` default on `bidder_kind` (a wrong default would silently mislabel the entire pre-E-232 bid history) and all eight indexes, rather than trusting the absence of an exception. `--check` verifies without writing. **APPLIED to db-1 — which IS sandbox — on 2026-08-08** (both ticks below are that one apply, per the database-1-is-sandbox correction above). Verified read-only afterwards with `--check`: all 11 new columns present across the four altered tables, `recovery_batteries` created with 20 columns and **0 rows**, and **8/8 indexes** including both partial scheduler indexes. **The catch-up half turned out to be a no-op on this database** — all six tables reported `present`, i.e. db-1 did receive the historic `db:push`. That is the finding, and it is NOT a reason to drop PART 1: it says nothing about db-2, and the tables still have no migration file of their own anywhere else. **Idempotency proven** by applying twice — the second run re-applied cleanly, raised only `42P07 … already exists, skipping` NOTICEs, and left the row count at 0 and the index count at 8/8. The `'nbfc'` default on `bidder_kind` was confirmed to have labelled the **38 pre-existing bids** correctly (38 legacy nbfc bids, 0 dealer bids), which is the check worth making — a wrong default would have silently relabelled the entire historic bid log as dealer activity. **20 lots carry no `seller_tenant_id`** and that is expected: they predate the column, and `placeBid()` deliberately REFUSES a dealer bid on such a lot rather than mis-attributing it, so those 20 are readable but not biddable until a seller is set. **NOT on prod (db-2).** Worth recording how this file nearly went to the wrong database: `.env.local` had **three commented `DATABASE_URL` lines and one active one, and the active one was db-2 (prod)** — the sandbox line at 153 was commented out and the prod line at 166 was not. A `grep DATABASE_URL` that does not check for a leading `#` reads that file backwards. The read-only `--check` run is what surfaced it, by printing the host before touching anything; that host line exists for exactly this reason and should not be removed. **CORRECTED 2026-08-08, same day, after the first apply: three columns were declared `uuid` and had to become `varchar(255)`** — `auction_bids.bidder_dealer_id`, `auction_settlements.winner_dealer_id` and `recovery_batteries.loan_sanction_id`. Dealers live in `accounts`, whose primary key is `character varying(255)` holding application-issued strings like `ACC-ITARANG-20260409-971` (measured on db-1: **62 rows, ZERO uuid-shaped**), and `loan_sanctions.id` is likewise varchar; `users.dealer_id`, the column pointing at the same thing, is `character varying` too. The uuid draft **applied cleanly and verified cleanly** and would have rejected every real dealer with `22P02 invalid input syntax for type uuid` at the first bid — days after this row was ticked. That is the failure mode worth internalising: a migration that verifies is not a migration that works, and column TYPE is not checked by "does the column exist". Because the wrong draft had already reached db-1, correcting the `ADD COLUMN` statements alone would have left this database permanently wrong while still reporting success — so the file carries a **guarded, self-correcting PART 4** (`IF data_type = 'uuid' THEN ALTER … TYPE varchar(255) USING …::text`) that repairs an already-applied database and is a no-op everywhere else. Safe because all three columns were **100% NULL** (added by this same file, nothing had written to them), so the `USING` clause had no rows to convert. It is a type WIDENING on empty columns, not the destructive change CLAUDE.md's additive-only rule is about, and it is named here rather than left to be discovered. Re-applied to db-1 on 2026-08-08: the three `RAISE NOTICE` corrections fired once, the apply script now asserts **3/3 varchar**, and a third run fired **zero** corrective ALTERs — so the guard is proven idempotent, not merely assumed. **Re-applied to db-1 again on 2026-08-09** on request, after re-reading `.env.local` line by line for the leading `#` (line 153 = db-1 active, line 166 = db-2 commented) and running `--check` first. All six PART 1 tables reported `present (no-op)` again, the run raised only `42701`/`42P07 … already exists, skipping` NOTICEs, **PART 4 fired no corrective ALTERs** (types were already `3/3 varchar` in the pre-run `--check`, which is the evidence the guard is a true no-op on a corrected database), and the invariants held unchanged: `recovery_batteries` 20 columns / **0 rows**, **8/8 indexes**, **38 legacy nbfc bids / 0 dealer bids**, and the same **20 pre-E-232 lots with no `seller_tenant_id`** — still readable, still deliberately not biddable. **APPLIED to database-2 — which IS production — on 2026-08-12** (the first and last tick below are that one apply, per the `local (db-2)` = `prod` = database-2 pairing above). Applied as part of a db-1 → db-2 catch-up: a full `information_schema` diff found exactly eight migrations present on database-1 and absent on database-2 (E-224, E-225, E-226, E-232, E-233, E-234, E-235, E-236), and they were applied in numeric order — which is also dependency order, since `refurbishment_jobs` and `auction_lot_items` both reference E-232's `recovery_batteries`. Each file ran inside ONE transaction (E-232/233/234 carry their own `BEGIN`/`COMMIT` and were sent verbatim; the rest were wrapped by the runner), and all **54** expected objects — 8 tables, 30 columns, 16 indexes — were verified present on database-2 immediately afterwards by querying `information_schema` / `pg_indexes` rather than trusting the absence of an exception. Re-running the diff afterwards reported **0 tables and 0 columns** differing between the two databases in either direction. Every one of the eight is pure additive DDL — all eight were scanned for `INSERT`/`UPDATE`/`DELETE`/`TRUNCATE`/`DROP COLUMN`/`ALTER TYPE`/`SET NOT NULL` beforehand and contain **none**, so no production row was read or written by this run. The PART 1 catch-up half was **not** a no-op here, unlike on db-1: `recovery_batteries` was created rather than skipped. Production's auction tables were near-empty at apply time (`auction_lots` 5 rows; `auction_bids`, `auction_auto_bids`, `auction_settlements`, `nbfc_recovery_pipeline`, `nbfc_battery_evaluations` all **0**), so PART 3's re-point columns landed on essentially no history and PART 4 had no uuid-typed columns to repair. | ✅ | ✅ | ✅ | ✅ |

| E-233_refurbishment_jobs | Phase 2 of the battery-auction build. One new table, `refurbishment_jobs`, plus two additive columns on `nbfc_battery_evaluations` (`photo_urls text[]`, `condition_grade varchar(24)`). **DEPENDS ON E-232** — `battery_id` points at `recovery_batteries`; `scripts/apply-e233.mjs` probes for that table first and fails with the real reason rather than a bare `42P01` from mid-file. Backs BRD §5 (NBFC raises a job → iTarang workshop works it → battery re-enters at `ready_for_auction`) and §15 (charger, harness and SOC meter must be NEW on every refurbished battery, costed separately at ~₹7–8k and then rolled into the lot base price so the dealer sees ONE number). **`estimated_cost` and `actual_cost` are deliberately two columns**: the estimate is what the NBFC agreed to before work started, the actual is what the workshop spent, and the pair is the only thing a workshop can be audited against — it is also exactly what a dispute is about. **`accessories` and `checklist` are jsonb, not columns**, following `nbfc_battery_evaluations.step1/2/3`: three boolean+price column pairs would need a migration the first time a fourth accessory exists, and this is precisely the list that grows. **No FK to `recovery_batteries`**, consistent with the whole `nbfc_*` family (E-119's evaluations table has no FK to the pipeline row it names either) — these tables are applied by hand across databases that drift, and a FK turns a missing parent into a failed INSERT at runtime rather than a dangling id somebody can repair. **The one non-obvious object is the PARTIAL unique index `refurbishment_jobs_one_open_per_battery` (`WHERE status IN ('requested','in_progress')`)**: without it two operators raising a job for the same battery on the same day produce two, and "the refurbishment cost of this battery" — the number that rolls into the auction base price — stops having a single answer. It MUST stay partial; a non-partial version would look identical in a name-only check while forbidding a battery from ever being refurbished a second time, so the apply script asserts the `WHERE` clause is present rather than just the index name. **This file ships with the code that makes the three disagreeing SOH rules agree.** `evaluation.ts` scrapped only when `soh < 70 AND decision='scrap'`; `RecoveryKanban.tsx` enforced `REFURBISHABLE_MIN_SOH = 70` **client-side only**, as a disabled button and a tooltip; the BRD's Figure 1 says scrap below 55. `transitionStage()` — the real write path, reachable from a plain API call — accepted a 30% SOH battery as refurbishable. The bands now live once, in `stages.ts` (`SOH_SCRAP_BELOW = 55`, `SOH_REFURBISHABLE_MIN = 70`), are enforced by `assertSohAllowsStage()` on the server, and the 55–70 gap becomes the `partial_working` grade BRD §6 introduces and nothing had a home for. The SOH floor now also OVERRIDES the operator's decision — a mis-clicked "minor repair" on a 20% cell no longer sends it to auction. **The stage machine gained the BYPASS ARC** `needs_inspection -> ready_for_auction`: refurbishment is RECOMMENDED, NEVER MANDATORY (BRD §5), and until now a battery that came back in perfect condition had to be booked into a workshop it did not need before it could be sold — not a policy decision, a missing edge. `recordEvaluation()` is now one transaction (it was two unguarded statements, so a failure between them left an evaluation recorded against a battery still sitting in `needs_inspection`). The orphaned `src/components/nbfc-portal/RecoveryPipelineBoard.tsx` was DELETED — imported by nothing, and its transition map contradicted `RecoveryKanban.tsx`'s. **REQUIRED once `schema.ts` mirrors the columns** — `recordEvaluation()` writes through the Drizzle query builder, which names every column in its INSERT, so an unapplied database throws `column "photo_urls" does not exist` on the first evaluation saved. Mirrored in `schema.ts` as `refurbishmentJobs` plus the two evaluation columns; the partial unique index is NOT mirrored (drizzle's builder cannot express a WHERE clause on an index — same treatment as E-093, E-226, E-230, E-232). Additive and idempotent — safe to re-run. **APPLIED to db-1 — which IS sandbox — on 2026-08-08** (both ticks below are that one apply, per the database-1-is-sandbox correction above). Verified read-only afterwards with `--check`: `refurbishment_jobs` present with 17 columns and **0 rows**, both evaluation columns present with `photo_urls` confirmed as `ARRAY` (not `text`), and the one-open-job index confirmed **partial**. **Idempotency proven** by re-running — the second pass reported "already present — expecting a no-op" and left the row count at 0. **NOT on prod (db-2)**, which does not have E-232 either; apply them in order. **Re-applied to db-1 on 2026-08-09** on request — `.env.local` re-checked line by line for the leading `#` first (line 153 = db-1 active, line 166 = db-2 commented), then `--check` run before writing. Third idempotency proof: the run raised only `42701 … already exists, skipping` NOTICEs for both evaluation columns, and left `refurbishment_jobs` at 17 columns / **0 rows** with the one-open-job index still **partial**. **APPLIED to database-2 — which IS production — on 2026-08-12** (the first and last tick below are that one apply, per the `local (db-2)` = `prod` = database-2 pairing above). Applied as part of a db-1 → db-2 catch-up: a full `information_schema` diff found exactly eight migrations present on database-1 and absent on database-2 (E-224, E-225, E-226, E-232, E-233, E-234, E-235, E-236), and they were applied in numeric order — which is also dependency order, since `refurbishment_jobs` and `auction_lot_items` both reference E-232's `recovery_batteries`. Each file ran inside ONE transaction (E-232/233/234 carry their own `BEGIN`/`COMMIT` and were sent verbatim; the rest were wrapped by the runner), and all **54** expected objects — 8 tables, 30 columns, 16 indexes — were verified present on database-2 immediately afterwards by querying `information_schema` / `pg_indexes` rather than trusting the absence of an exception. Re-running the diff afterwards reported **0 tables and 0 columns** differing between the two databases in either direction. Every one of the eight is pure additive DDL — all eight were scanned for `INSERT`/`UPDATE`/`DELETE`/`TRUNCATE`/`DROP COLUMN`/`ALTER TYPE`/`SET NOT NULL` beforehand and contain **none**, so no production row was read or written by this run. | ✅ | ✅ | ✅ | ✅ |

| E-234_auction_lot_items_visibility_audience | Phase 3 of the battery-auction build. Three new tables — `auction_lot_items`, `auction_lot_visibility`, `auction_lot_audience` — plus `auction_auto_bids.bidder_dealer_id` and a re-keyed unique index. **DEPENDS ON E-232.** `auction_lot_items` is what makes "5 batteries, one lot" expressible at all: `auction_lots.quantity` is an integer and `publishLotFromRecovery()` hard-coded it to **1**. **`condition` lives on the ITEM, not the lot**, because a pallet may legitimately mix grades — three refurbished cells and two partial-working ones — and putting it on the lot forces the seller to either split the pallet or lie about two of the five, at which point a dealer who bid on "refurbished" and received partial-working has been mis-sold. EVs reuse the same column later with no schema change, which is the other half of why it is a varchar and not an enum. This table also **replaces two string joins that matched zero rows every single time**: `settlements.ts` matched `nbfc_recovery_pipeline.battery_serial = auction_lots.lot_code` and the cancel service matched `inventory.serial_number = auction_lots.lot_code`, but `lot_code` is `"LOT-" + 8 hex of a pipeline uuid` and is never equal to either — both failed silently because both were best-effort, so **no battery has ever actually been marked `resold`**. `auction_lot_visibility` is one row per lot (india / state / city / radius) — a separate table rather than four nullable columns on `auction_lots` because only one scope is ever active and the parameter sets are disjoint: `states[]` is meaningless for a radius, lat/lng meaningless for a state list. `auction_lot_audience` freezes the eligible dealers **once at publish**, with per-channel delivery state, one row per (lot, dealer, channel). **Frozen rather than re-evaluated on read** so a notified dealer keeps seeing the lot they were told about, a dealer onboarding mid-auction does not silently join a race that is half over, and "who did we tell?" stays answerable afterwards — which is the question a disputed auction turns on. `dealer_id` is **varchar(255)** matching `accounts.id`, per the E-232 lesson. **The half that was nearly missed: `auction_auto_bids` needed a dealer column.** E-093 keyed a standing maximum on `(lot_id, tenant_id)`, correct when every bidder was an NBFC tenant. After the E-232 re-point a dealer bid writes the SELLER's tenant into `auction_bids.tenant_id`, so **every dealer bidding on one lot shares a tenant_id** — with two fatal consequences: the proxy engine cannot tell whose standing order is whose (resolving the dealer from "most recent bid by this tenant" returns whoever bid last, i.e. usually the current leader, whose own proxy is then correctly skipped, so **nothing ever fires**), and E-093's partial unique index means **the second dealer to set a maximum on a lot silently cancels the first one's**. So the column is added and `auction_auto_bids_lot_tenant_active_uidx` is **DROPPED and replaced** by `auction_auto_bids_lot_bidder_active_uidx ON (lot_id, COALESCE(bidder_dealer_id, tenant_id::text)) WHERE status='active'`. That index swap is the one non-additive statement in the file (called out because CLAUDE.md's default is strictly additive): it drops no data and no column, the replacement is created in the same `BEGIN/COMMIT`, and the degenerate case E-093 guarded — one bidder holding two active maxima on one lot — stays refused with `23505`. What becomes legal is what should always have been: two DIFFERENT dealers each holding one. Mirrored in `schema.ts` as `auctionLotItems` / `auctionLotVisibility` / `auctionLotAudience` plus the new auto-bid column; the two partial indexes (`auction_lot_audience_pending_idx`, `auction_auto_bids_lot_bidder_active_uidx`) are NOT mirrored, per the E-093/E-226/E-230/E-232 precedent that drizzle cannot express a WHERE clause on an index. Additive and idempotent otherwise — safe to re-run. Apply with `node --env-file=.env.local scripts/apply-e234.mjs`, which asserts `dealer_id` is varchar, both UNIQUE constraints exist, the pending-fan-out index is **partial**, and — the check worth having — that the **old auto-bid index is GONE**, since a database left holding it still refuses a second dealer's standing maximum, which is the precise bug this file exists to remove. **APPLIED to db-1 — which IS sandbox — on 2026-08-08** (both ticks below are that one apply). Verified: items 6 columns, visibility 8, audience 12, `dealer_id` character varying, 2/2 uniques, auto-bid column present + new index present + **old index dropped**, pending index partial, both new tables 0 rows. **Proven end-to-end, not just structurally**, by `npm run verify:auction` (`scripts/verify-auction-e2e.ts`) — 24 assertions across the real services against this database: a 3-battery lot composes as **draft** (not auto-published), a 72 h window is refused, `india=47 > city=2` dealers, a 25 km radius around Nashik returns 2 and a 25 km radius around Delhi correctly excludes them, publish freezes 47 dealers × 2 channels = 94 rows, a below-base bid is refused **without inserting a row**, dealerB's proxy counter-bids at **162,000 — one increment over 160,000, not its 200,000 ceiling** — and does NOT fire against its own owner, a bid in the final 30 s pushes the deadline out ~91 s, and the scheduler closes the lot, creates a settlement with a **real** `seller_tenant_id` and a populated `winner_dealer_id`, marks all 3 batteries sold, and does not re-close on the next tick. **That script found three real bugs that structural verification could not**: a JS array expanded by drizzle's `sql` template into a record tuple (`42846 cannot cast type record to uuid[]`, in `composeLot` AND latently in `attachBatteryPhotos`), a JS `Date` passed through `db.execute` (`ERR_INVALID_ARG_TYPE`, in the scheduler), and the auto-bid dealer-key gap above. **NOT on prod (db-2)**, which has neither E-232 nor E-233; apply in order. **Re-applied to db-1 on 2026-08-09** on request, after re-checking `.env.local` for the leading `#` (line 153 = db-1 active, line 166 = db-2 commented) and running `--check` first. Second idempotency proof: only `42P07 … already exists, skipping` and a `DropErrorMsgNonExistent` NOTICE for the already-dropped old auto-bid index; all three tables still 6 / 8 / 12 columns, **0 rows**, `dealer_id` varchar, 2/2 uniques, **old index still gone**. The E2E behind this row has since grown from 24 to **36 assertions** (E-234 Phase 5 added the notification fan-out: audience drained to 0 pending, every channel row terminal, no bell row claimed for a dealer account with no active login, and SMS recorded `skipped/sms_disabled_no_dlt_template` rather than silently not sending). It also caught a defect this structural check could not — the scheduler compared `ends_at` against the **Node process clock** rather than Postgres `now()`, so ~1 s of host/RDS skew stopped a lot whose deadline had demonstrably passed from closing; every comparison now uses `now()` in SQL. **APPLIED to database-2 — which IS production — on 2026-08-12** (the first and last tick below are that one apply, per the `local (db-2)` = `prod` = database-2 pairing above). Applied as part of a db-1 → db-2 catch-up: a full `information_schema` diff found exactly eight migrations present on database-1 and absent on database-2 (E-224, E-225, E-226, E-232, E-233, E-234, E-235, E-236), and they were applied in numeric order — which is also dependency order, since `refurbishment_jobs` and `auction_lot_items` both reference E-232's `recovery_batteries`. Each file ran inside ONE transaction (E-232/233/234 carry their own `BEGIN`/`COMMIT` and were sent verbatim; the rest were wrapped by the runner), and all **54** expected objects — 8 tables, 30 columns, 16 indexes — were verified present on database-2 immediately afterwards by querying `information_schema` / `pg_indexes` rather than trusting the absence of an exception. Re-running the diff afterwards reported **0 tables and 0 columns** differing between the two databases in either direction. Every one of the eight is pure additive DDL — all eight were scanned for `INSERT`/`UPDATE`/`DELETE`/`TRUNCATE`/`DROP COLUMN`/`ALTER TYPE`/`SET NOT NULL` beforehand and contain **none**, so no production row was read or written by this run. The index swap was the one statement worth watching on prod and it is confirmed: `auction_auto_bids_lot_tenant_active_uidx` **was present on database-2** before the run (it had already been replaced on db-1) and is **gone** afterwards, with `auction_auto_bids_lot_bidder_active_uidx` in its place. `auction_auto_bids` held **0 rows** on production, so the unique-index replacement had nothing to conflict with. | ✅ | ✅ | ✅ | ✅ |


| E-235_dealer_leads_list_indexes | Four partial indexes on `dealer_leads`, no schema change: `dealer_leads_active_touch_idx (last_touchpoint_at DESC NULLS LAST, created_at DESC)`, `dealer_leads_active_owner_idx (current_owner_id)`, `dealer_leads_active_created_idx (created_at DESC)` — all three `WHERE is_active IS NOT FALSE` — plus `dealer_leads_asm_idx (asm_id) WHERE asm_id IS NOT NULL`. Supports the merged **Leads + Leads Info** screen: the two pages were separate views over the same table (one filtered `phone IS NOT NULL` and showed `current_status`, the other filtered `is_active IS NOT FALSE` and showed `lead_status`, so the same lead read differently on each) and are now one list served by `src/lib/leads/leadListQuery.ts` behind `GET /api/dealer-leads`. **The partial predicate is `IS NOT FALSE`, not `= true`, on purpose** — E-112's existing `dealer_leads_is_active_idx ... WHERE is_active = true` cannot serve either query, because both filter `is_active IS NOT FALSE` (which also admits NULL) and Postgres cannot prove `IS NOT FALSE` is a subset of `= true`, so it declines the index. `/admin/leads-info` has therefore been full-scanning since E-112; matching the predicate exactly is what finally makes it indexable. **NOT required for correctness** — the page works without it, just with a full scan + sort per request, so an unapplied E-235 shows up as latency, never as a "column does not exist" error. Deliberately NOT indexed: `final_intent_score` (four distinct values in practice — 0/30/60/90 band constants — and the stat aggregate scans the filtered set regardless), `source` (~5 values), `state`/`city` (unusable while those filters are `ILIKE '%…%'`), and search (needs `pg_trgm`). Additive and idempotent; `schema.ts` needs no mirror edit, per E-168 / E-221. **Applied to db-1 on 2026-08-09** — all four indexes present with the correct partial predicates, verified by re-running `node --env-file=.env.local scripts/apply-e235.mjs` (idempotent no-op on the second run). dealer_leads was 3,244 rows, so the CREATE INDEX write lock was instant. **APPLIED to database-2 — which IS production — on 2026-08-12** (the first and last tick below are that one apply, per the `local (db-2)` = `prod` = database-2 pairing above). Applied as part of a db-1 → db-2 catch-up: a full `information_schema` diff found exactly eight migrations present on database-1 and absent on database-2 (E-224, E-225, E-226, E-232, E-233, E-234, E-235, E-236), and they were applied in numeric order — which is also dependency order, since `refurbishment_jobs` and `auction_lot_items` both reference E-232's `recovery_batteries`. Each file ran inside ONE transaction (E-232/233/234 carry their own `BEGIN`/`COMMIT` and were sent verbatim; the rest were wrapped by the runner), and all **54** expected objects — 8 tables, 30 columns, 16 indexes — were verified present on database-2 immediately afterwards by querying `information_schema` / `pg_indexes` rather than trusting the absence of an exception. Re-running the diff afterwards reported **0 tables and 0 columns** differing between the two databases in either direction. Every one of the eight is pure additive DDL — all eight were scanned for `INSERT`/`UPDATE`/`DELETE`/`TRUNCATE`/`DROP COLUMN`/`ALTER TYPE`/`SET NOT NULL` beforehand and contain **none**, so no production row was read or written by this run. The `sandbox` box is ticked here too — it had been left `☐` while `db-1` was `✅`, which the database-1-is-sandbox correction at the top of this file makes contradictory; they are one database and the boxes pair. `dealer_leads` was **8,911 rows** on production, so the four CREATE INDEX write locks were instant. | ✅ | ✅ | ✅ | ✅ |
| E-236_lead_disposition_taxonomy | Five nullable columns on `dealer_leads` (`last_disposition`, `last_disposition_bucket`, `last_connect_status`, `last_disposition_at`, `last_disposition_source`) and five on `lead_touchpoints` (`disposition`, `disposition_bucket`, `connect_status`, `external_stage`, `external_tag`), plus two partial indexes. Persists the CC team's call disposition so it can be **filtered on**. Every NeoDove webhook already carried it — `lead_tag_name` holds the L3 the agent tapped, and `parseInboundEvent()` has always extracted it — but `remarksFor()` then flattened tag, stage and label into one prose string on `lead_touchpoints.remarks` (`"[NeoDove] Disposition: … · Stage: … · Tag: …"`). Prose cannot be grouped or counted, so "show me every lead marked Price High" — the question the telecalling loop exists to answer — was unanswerable in the CRM and a sales head had to read NeoDove's own dashboard. **Two tables, not one**: `lead_touchpoints` keeps the per-call record (a lead is called many times and each call's disposition is the evidence behind any later dispute), `dealer_leads` carries a denormalised *latest* because the /leads WHERE clause runs on every keystroke and resolving "most recent NeoDove touchpoint per lead" through a lateral join is exactly what E-224 added `neodove_sync_status` to avoid. **The bucket is stored, not derived in SQL**: the L3→L2 map lives in `src/lib/leads/dispositions.ts` and has one real ambiguity — "Commercials Explained" is listed under both Warm and Hot in the source sheet — settled using NeoDove's `lead_stage_name` on the event, which the query does not have. Storing the answer also keeps a 37-arm CASE out of the hot path and means the filter and the chip on the row can never disagree. **NOT mirrored in `schema.ts`**, per the E-224 / E-226 precedent: drizzle names every column of a table object in its INSERT and in a bare `db.select()`, so listing these would break every `db.insert(leadTouchpoints)` and every `db.select().from(dealerLeads)` on a database without this file — which today includes prod. They are written by best-effort raw UPDATEs in `src/lib/neodove/inbound.ts` **after** `writeTouchpoint`'s transaction commits (a missing column costs a disposition label, never a touchpoint), and read by raw ``sql`` in `leadListQuery.ts`, which already builds its WHERE from raw fragments. **Degrades safely**: the disposition predicates are emitted only when the filter is set, so an unapplied E-236 shows up as an empty filter dropdown, never as a "column does not exist" error on the leads list. Both blocks are `DO $do$ … EXCEPTION WHEN undefined_table` guarded. Additive and idempotent. Apply with `node --env-file=.env.local scripts/apply-e236.mjs`, which verifies the ten columns and that both indexes are genuinely **partial**, then reports how many leads carry a disposition so far — zero is the cue to run `npm run backfill:dispositions`, which re-parses every inbound body stored in `neodove_sync_events` through the same classifier. **APPLIED to db-1 — which IS sandbox — on 2026-08-11** (both ticks below are that one apply). Verified: all ten columns present, both indexes present and genuinely **partial**, and a second run of the applier a clean no-op. The backfill was then run against it: 2,178 stored inbound webhooks re-parsed, **654 matched the CC sheet**, 1,524 named no disposition at all (the LEAD_DISPOSE trigger sends `lead_tag_name: null` unless the agent tagged the lead) — leaving **646 touchpoints and 399 leads** carrying a disposition, top values `No requirement in current` (113), `As to Call Back` (78), `Details Shared` (45), `REJECTED BY US` (36). **The dry run earned its keep three times.** It caught (1) a keyset that paged ONE ROW PER QUERY because `SELECT id::text` + `ORDER BY id` sorts by the uuid's TEXT form while the cursor compared real uuids; (2) a non-terminating loop — `created_at` round-trips through a millisecond-precision JS `Date`, so the final row stayed greater than itself and was re-read forever (fixed with a microsecond `to_char` cursor, plus a cursor-not-advancing guard); and (3) 31 rows whose "disposition" was **`Open` / `Follow-Up` / `Closed` / `6`** — NeoDove's lead-STATUS vocabulary arriving via `lead_status_name`, which would have shown up in the filter dropdown as though a telecaller had picked it. `dispositionFor()` now lets only `lead_tag_name` supply an UNMAPPED value, and the next dry run reported **0 outside the sheet**. **NOT on prod (db-2)** — which also has neither E-224 nor E-226, so the neodove half is a no-op there until those land. **APPLIED to database-2 — which IS production — on 2026-08-12** (the first and last tick below are that one apply, per the `local (db-2)` = `prod` = database-2 pairing above). Applied as part of a db-1 → db-2 catch-up: a full `information_schema` diff found exactly eight migrations present on database-1 and absent on database-2 (E-224, E-225, E-226, E-232, E-233, E-234, E-235, E-236), and they were applied in numeric order — which is also dependency order, since `refurbishment_jobs` and `auction_lot_items` both reference E-232's `recovery_batteries`. Each file ran inside ONE transaction (E-232/233/234 carry their own `BEGIN`/`COMMIT` and were sent verbatim; the rest were wrapped by the runner), and all **54** expected objects — 8 tables, 30 columns, 16 indexes — were verified present on database-2 immediately afterwards by querying `information_schema` / `pg_indexes` rather than trusting the absence of an exception. Re-running the diff afterwards reported **0 tables and 0 columns** differing between the two databases in either direction. Every one of the eight is pure additive DDL — all eight were scanned for `INSERT`/`UPDATE`/`DELETE`/`TRUNCATE`/`DROP COLUMN`/`ALTER TYPE`/`SET NOT NULL` beforehand and contain **none**, so no production row was read or written by this run. The parenthetical above — "NOT on prod (db-2), which also has neither E-224 nor E-226, so the neodove half is a no-op there" — is now spent: E-224 and E-226 went in ahead of it in the same run, so both halves applied for real. **No backfill was run on production.** `npm run backfill:dispositions` re-parses `neodove_sync_events`, and that table was created empty on db-2 by E-224 minutes earlier — there is nothing to re-parse until prod starts receiving its own webhooks. `lead_touchpoints` held 248 rows. | ✅ | ✅ | ✅ | ✅ |
| E-237_neodove_crm_assignment | One nullable column on `neodove_campaigns` (`crm_owner_user_id text`) and two on `neodove_lead_links` (`assigned_owner_id text`, `assigned_at timestamptz`) plus a partial index `neodove_lead_links_assigned_owner_idx (neodove_campaign_id, assigned_owner_id) WHERE assigned_owner_id IS NOT NULL`. Makes a NeoDove push also assign the leads in the CRM, so they land in the rep's `/inside-sales` queue instead of on nobody's dashboard. **Nothing is ever sent to NeoDove** — their Custom Integration endpoint has no assignee/agent/owner param and they have no read API, so `crm_owner_user_id` records the CRM counterpart of an assignment a human already made in NeoDove's own campaign settings. **Unlike `mirror_config` (E-225) this is ACTED ON**, not descriptive — same category as `is_priority_dial` (E-226) — so the campaign form must not present it inside the "recorded, never applied" block. `text` not `uuid` and no FK, matching `dealer_leads.current_owner_id` (`users.id` is uuid, hence every join's `::text` cast); validity is enforced at write time like `current_owner_id`'s is. **All three columns ARE mirrored in `schema.ts`**, unlike E-224's `dealer_leads` columns and E-226's `lead_touchpoints` columns — the difference is access pattern, not migration state: `neodove_campaigns` and `neodove_lead_links` are reached exclusively through raw `sql` (verified zero references outside `schema.ts`), so no bare `db.select().from()` exists for Drizzle to expand into a column list naming a missing column. **Every read still goes through `to_jsonb`** (`to_jsonb(c) ->> 'crm_owner_user_id'`, per `getPriorityDialCampaign()`) because a missing column fails at PARSE time, which would take all four push routes and the campaigns list down where this is unapplied; the link-table stamp is additionally wrapped in a nested try/catch so a missing column costs the audit stamp and never the assignment. **NOT required for existing behaviour**: without it no campaign default ever resolves and the assignee picker is simply unavailable — pushes work exactly as they do today. Guarded `EXCEPTION WHEN undefined_table` so it is a reported no-op on prod, which has no `neodove_*` tables until E-224 lands there. Apply with `node scripts/apply-e237.mjs` — **but note that applier hardcodes `.env.local`, which IS sandbox, so it cannot reach production at all.** **Prod boxes ticked 2026-08-18: E-251 delivered these three columns and the partial index to database-2**, verified present by the applier's after-state read and by the 15:04 drift re-run. | ✅ | ✅ | ✅ | ✅ |
| E-238_offer_negotiation | One new table, `nbfc_offer_negotiations` (19 columns), plus four additive columns on `nbfc_financing_offers` (`negotiation_status varchar(16) NOT NULL DEFAULT 'open'`, `negotiation_round integer NOT NULL DEFAULT 0`, `fixed_at timestamptz`, `fixed_by uuid`). Adds a dealer ⇄ NBFC negotiation loop over the firm financing offer, ended by an NBFC **Fix** action. Until now the offer was take-it-or-leave-it: the NBFC submitted terms and the dealer's only action was `Select as winner`, so a customer asking for 16% over 48 months instead of 18.5% over 36 had no route through the system — that conversation happened on the phone and was never recorded, even though `select-winner` is the formal sanction moment (Addendum §14.2). **A separate table rather than more columns on `nbfc_financing_offers`** because that table is UNIQUE on `assignment_id` and the submit route upserts in place, so every resubmit DESTROYS the previous terms — there is nowhere on the row for round 1 to live once round 3 exists, and "what did they originally offer?" is exactly what a disputed sanction turns on. **Rounds, not `ask/counter/agreed` columns** as `vendor_thread_lines` (E-186) uses: that shape fits one number haggled once, but a financing offer is six interdependent values (drop the ROI and the EMI moves) negotiated as a set and more than once, so the unit is a full snapshot of what one party put on the table at one moment; the UI diffs consecutive rounds to show only what changed. **`negotiation_status` is stored, not derived** from the last round's `party` — the dealer's offers list renders a button per row from it on every poll, and deriving would mean a correlated subquery on that path (same call E-224 made for `neodove_sync_status`). `negotiation_round` doubles as the concurrency handle: the round written is always `negotiation_round + 1`, and **the UNIQUE index `nbfc_offer_negotiations_offer_round_uidx (offer_id, round)` is the point of the design, not tidiness** — it turns two simultaneous submits into a `23505` instead of two rows both claiming "round 3". No FKs and no CHECK constraints on `party`/`kind`/`negotiation_status`, per the `nbfc_*` family convention (E-202/E-218/E-226/E-231/E-232/E-233): vocabulary is enforced in the route layer and recorded in `COMMENT ON COLUMN`. **The invariant the file exists to support: a round is written ONLY when its terms are visible to the dealer.** E-161 holds an out-of-band offer at `ceo_approval_status='pending'` and `GET /api/lead/[id]/offers` filters it out, so appending a round there would leak unapproved terms into the dealer's history; the writer appends on the same condition that already gates the `offer_submitted` flip, and `applyFinancingOfferDeviationApproval` appends on CEO approval. Round 1 (the NBFC's opening terms) is **seeded lazily on the first counter** for offers that predate this file, which is why no data backfill ships with it. **REQUIRED — this one does not degrade.** Unlike E-236/E-237, whose columns are deliberately UNmirrored and reached through raw `sql`, both halves of E-238 ARE mirrored in `schema.ts` and `src/app/api/nbfc/offer/[leadId]/route.ts` does a bare `db.select().from(nbfcFinancingOffers)` — Drizzle names every column of a mirrored table in its generated SQL, so on a database without this file the NBFC Acquire offer panel fails on its first read with `column "negotiation_status" does not exist`. Apply before deploying the code. Additive and idempotent — safe to re-run. Apply with `node scripts/apply-e238.mjs`, which fails with the real reason if `nbfc_financing_offers` is absent (rather than a bare `42P01` from mid-file), verifies all 19 + 4 columns, and asserts the round index is **genuinely UNIQUE** — a non-unique index of the same name passes a name-only check while letting the duplicate-round race through. **The applier takes an explicit target**: `process.env.DATABASE_URL` overrides `.env.local`, and it prints the host *and* which environment that host IS before writing anything. That was added because `.env.local` is flipped between the two by hand and was found pointing at **database-2 (production)** at the time of this build — "whatever the env says" is a coin flip about which database you are altering, and every earlier applier in this series inherits that coin flip silently. **APPLIED to database-1 — which IS sandbox — on 2026-08-13**, then **APPLIED to database-2 — which IS production — on 2026-08-13** (both box-pairs below are those two applies, per the `local (db-2)` = `prod` = database-2 pairing above). Verified on both by querying `information_schema` / `pg_indexes` rather than trusting the absence of an exception: `nbfc_offer_negotiations` present with **19 columns and 0 rows**, all four columns on `nbfc_financing_offers`, and the round index confirmed **UNIQUE** (asserted from `pg_indexes.indexdef`, not by name). **Idempotency proven** by re-running against db-1: the second pass raised only `42P07`/`42701 … already exists, skipping` NOTICEs and left the table at 19 columns / 0 rows. Pure additive DDL — scanned for `INSERT`/`UPDATE`/`DELETE`/`TRUNCATE`/`DROP`/`ALTER TYPE`/`SET NOT NULL`/`RENAME` beforehand and contains **none** outside comments, so no production row was written. Post-apply read-only probe: **15 existing offer rows on production and 8 on sandbox all read `negotiation_status='open'`, `negotiation_round=0`**, zero NULLs in the new NOT NULL columns and zero rows spuriously marked fixed — i.e. every pre-existing offer landed in exactly the never-negotiated state, and each will seed its own round 1 from its current terms the first time a dealer counters it. | ✅ | ✅ | ✅ | ✅ |
| E-242_quotation_draft_dispatch | Five nullable columns on `dealer_lead_commercials` (`quote_number varchar(40)`, `quote_pdf_url`, `quote_pdf_generated_at`, `quote_pdf_error`, `quote_snapshot jsonb`) with a **partial** unique index on `quote_number WHERE NOT NULL`; `hsn_code varchar(8)` + `gst_rate_pct numeric(5,2)` on all three `product_master_*` tables; `contact_email` + `gstin varchar(15)` on `dealer_leads`; a bare `quotation_number_seq`; and one new table `quotation_dispatches`. Closes the half of the quotation flow that was never built: E-221/E-226/E-230 decided quotes, but the approve branch wrote a `quote_sent` touchpoint claiming the quote "is actually released to the dealer" while **nothing was generated and nothing was sent** — the only quote document was `quote_document_url`, a URL a rep typed into a modal by hand. Approval (by the CEO or by the auto-rule) now renders a draft in the business's GST-proforma format (`docs/ITPI-35 (1).pdf`), notifies the lead owner and `sales_manager`, and the send route puts it on WhatsApp/email. **New columns rather than reusing `quote_document_url`**: that column is the rep's own attachment, written from user input and read by the lead pane and the CEO queue, so overwriting it would destroy what the rep attached and make "the document on this quote" mean two different things depending on which path last wrote. **`quote_snapshot` exists for the same reason as `oem_evaluation`** — the masters, the price book and the company block all move, so the view the PDF was rendered from is stored with it; without it the only honest option is re-rendering, which can silently produce a different document under the same number. **A sequence, not `MAX()+1`**: two approvals can commit at the same instant (the CEO clicking approve while the auto-rule releases another), which would hand both the same number and fail one approval on the unique index for a reason the CEO cannot act on. **The series is deliberately `ITQ-<FY>-<nnnn>`, not `ITPI-nn`** — Zoho Invoice still mints ITPI numbers at invoicing time, and two independent systems advancing one series eventually issue two documents with the same number, where the one that loses is already in a dealer's inbox. **`hsn_code`/`gst_rate_pct` go on the masters** because the supplied format is a GST proforma where each line carries its own rate (ITPI-35 has both 18% and 5% on one document) and none of the three `product_master_*` tables — which is what `product_lines[].product_id` points into — had any tax column at all; putting the rate on the model means it is set once by whoever maintains the catalogue instead of re-keyed, and mis-keyed, per quote. **Both are nullable with no backfill**: a guessed rate is a wrong number on a tax document sent to a dealer, so NULL means "not set" and the renderer shows the line as unset, never as zero. **`quotation_dispatches` and not `buyback_notification_events`** — that table is the closest existing thing (durable, retrying, multi-channel, carries attachments) but is `NOT NULL` on `request_id` with an FK to `buyback_requests`, so a dealer-lead quotation cannot have a row in it without inventing a buyback request. It is append-only (a resend is a new row) and the two channels are written independently, so one send can produce one `sent` row and one `failed` row — WhatsApp failing must not undo a delivered email. No backfill anywhere in the file: every existing quote was decided before a draft could be generated and before anything could be sent, so NULL and "no dispatch rows" are the true statements about them. **Mirrored in `schema.ts` only where it is safe to be**: `dealer_lead_commercials` and `quotation_dispatches` are listed (drizzle names every column in an INSERT, so the inside-sales commercials route 500s until this is applied — the same trade E-226 made on the same table, and the blast radius is the quotation flow itself). `dealer_leads.contact_email` and `.gstin` are **deliberately NOT listed**, per the E-224 / E-236 precedent: drizzle also names every column in a bare `db.select().from(dealerLeads)`, of which there are ~20 (leads list, AI dialer, CEO overview, role dashboards), so listing them would hard-fail all of those at PARSE time on a DB without this file — the whole leads screen going down to add an email field. They are read by name in raw ``sql`` projections instead, so an unapplied E-242 costs the quotation feature and nothing else. Verify the rendered document against the supplied format with `npm run verify:quotation-pdf`. Additive and idempotent, wrapped in one `BEGIN`/`COMMIT`. DEPENDS ON E-221, E-226, E-230. **Renumbered from E-237 on 2026-08-16 — `main` reached E-241 while this was in flight and `E-237_neodove_crm_assignment` already held that number. The file was applied to database-1 under the OLD name; the rename changes nothing in the DB, and re-running it as E-242 is a no-op.** | ☐ | ✅ | ✅ | ☐ |
| E-243_quotation_dealer_decision | Five nullable columns on `dealer_lead_commercials` (`dealer_decision varchar(16)`, `dealer_decision_at`, `dealer_decision_via varchar(20)`, `dealer_decision_actor`, `dealer_decision_note`) plus one partial index on `(dealer_decision_at DESC) WHERE dealer_decision IS NOT NULL`. Captures the DEALER's own answer to a quotation — the half E-242 stopped short of and Kartik explicitly parked on the 2026-08-13 call ("if the dealer approves it on WhatsApp or email, then if we can bring that into our system, that would be great"). Until this, the CRM could say a quotation went out and never say what came back: the reply lived in a WhatsApp thread, somebody's inbox, or a phone call nobody logged. **Columns on the quote, not a new table**: a quotation has exactly one dealer answer and `dealer_lead_commercials` is already versioned, so one row IS one quotation — a separate table would need a uniqueness rule to say the same thing and would let a quote and its verdict drift apart (the same reasoning E-221 gave for `approval_status`). **`dealer_decision` is orthogonal to `approval_status`, not a replacement**: that column is iTarang's internal gate, this is what the dealer said about the quotation we released, and a quote we approved can still be declined by them — both facts have to be readable at once. **`via` + `actor` are the evidence**, because "the dealer approved this" is a claim about someone outside the company: `via` is `link` | `whatsapp`, `actor` is the WhatsApp number or `token`. **No token table**: the approval link carries an HMAC of `(commercial_id, version_no)` under a server secret (`src/lib/leads/quoteToken.ts`), verifiable without storage, so nothing is inserted when a quotation is sent and nothing is cleaned up when it goes unanswered; single-use is enforced by the DECISION instead — the write is conditional on `dealer_decision IS NULL`, so a replayed or forwarded link finds the answer already recorded and changes nothing rather than casting a second vote. **The lead status is deliberately NOT touched**: a dealer tapping a button is an outside party, and letting one drive the internal state machine would mean a mis-tap silently rewrites pipeline reporting — the decision is recorded, the owner is notified, a human still moves the lead. **Mirrored in `schema.ts`**, which is safe here: `dealer_lead_commercials` has no bare `db.select().from(...)` anywhere (every read names its columns), so the only statement drizzle expands is the inside-sales commercials INSERT, which already depends on E-242 — contrast `dealer_leads`, whose E-242 columns are deliberately absent from `schema.ts` for exactly that reason. No backfill: every existing quotation was sent before an answer could be captured, so NULL is the true statement about all of them, and inferring approval from a lead that later converted would invent evidence. Additive and idempotent, wrapped in one `BEGIN`/`COMMIT`. DEPENDS ON E-242. **Renumbered from E-238 on 2026-08-16 — `E-238_offer_negotiation` already held that number on `main`. Applied to database-1 under the old name; the rename is bookkeeping only.** | ☐ | ✅ | ✅ | ☐ |
| E-239_campaign_counter_reconcile | **Data-only, no DDL** — three repairs to the AI dialer campaign tables, in one file because they must run in order. (1) `UPDATE dialer_campaign_leads SET status='completed' WHERE status='done'`. **`'done'` is not a status the app knows**: the valid set is `pending|calling|completed|failed|skipped`, and `/api/ai-dialer/campaigns/[id]/leads` queries each bucket by exact string match, so a `'done'` row matches NO bucket and vanishes from every tab including All. Prod campaign `camp_msr9dbh0_gwn04wcq` had 68 of them — header read `Completed 71`, table rendered 3. The blue **"Done"** badge is only the LABEL for `status='completed'` (`campaign-status-badge.tsx:66-69`); the stored value must be `completed`. These rows came from a hand-run repair that adapted E-169's `SET` clause. (2) Re-runs the **E-169** backfill (`failed` + `dropped_empty` → `completed`). A `dropped_empty` call connected and produced a transcript — the line dropped before any qualifying info was captured — so it is not a telephony failure. E-169 fixed the Bolna path in June (`ca879030`) but `src/lib/ai/elevenlabs/finalizeCall.ts` was missed and kept passing `success:false`, so **every ElevenLabs campaign carried on generating the bad rows for two months**; that line is fixed alongside this migration. E-169 is also still `☐` on all four environments, so for most DBs this is its first application — tick E-169's row too when this lands. (3) Rebuilds `dialer_campaigns.completed_leads / failed_leads / calls_made` from the rows. Those counters were maintained as `±1` bumps issued in a **second, untransacted statement** after each row write (7 sites across `campaignTracker.ts` and `advanceCampaign.ts`), so any deploy, PM2 reload, crash or swallowed error between the pair drifted them permanently and nothing reconciled them; concurrent advances double-counted for the same reason. `campaignTracker.ts` now derives them via `syncCampaignCounters()` — a single indexed recompute over `idx_dialer_campaign_leads_campaign_status`, so counters became projected state that self-heals on the next campaign event and picks up manual SQL repairs. This statement restates every existing campaign once so the two agree from here on. **`calls_made` is set to the completed count on purpose** — it counted successes only by construction (`completeCampaignLead` only ever bumped it together with `completed_leads`), meaning "calls that actually connected and completed". **Order matters**: both status repairs must land before the recompute or it bakes in the wrong numbers. **No `schema.ts` mirror** — no columns change. Idempotent: a second run flips 0 rows and the `IS DISTINCT FROM` guard matches 0 campaigns. Verify with `SELECT status, count(*) FROM dialer_campaign_leads GROUP BY status` (expect only the five valid values). Unrelated and NOT fixed by this file: 75 of the 146 rows on that prod campaign are `trigger_failed: INVITE failed: sip status: 404: Not Found (SIP 404)` — a carrier/trunk routing failure behind `ELEVENLABS_AGENT_PHONE_NUMBER_ID`, not a data problem (numbers are normalised to E.164 at `elevenlabs/triggerCall.ts:199`). | ☐ | ☐ | ☐ | ☐ |
| E-240_nbfc_dealer_direct_requests | One additive column on `nbfc_doc_requests` (`dealer_direct boolean NOT NULL DEFAULT false`) and one new table, `nbfc_doc_request_messages` (8 columns, 2 indexes). Adds a **second, direct NBFC → dealer channel** beside the E-200 admin-gated loop. E-200 made every NBFC request stop at the admin: born `nbfc_raised`, invisible to the dealer until an admin clicks "Forward to dealer", and even then the dealer sees a generic `other_document_requests` row because `/api/kyc/[leadId]/requested-docs` projects neither `source` nor `nbfc_request_id` — so the lender's actual question ("the CURRENT account statement, not the savings one") never arrives. The admin-gated path is **untouched**: same three buttons, same statuses, same forward/push routes; a request without `route_to: "dealer"` behaves exactly as before. The admin stops being a **blocker**, not an observer — the direct thread and every message in it still render in the admin `NBFC KYC Verification` card, and the admin is still notified on both legs. **NO new statuses or request types**, so the E-202 CHECK constraints are deliberately NOT touched: a direct request is `request_type='step4_extra_items'` (already permitted, and semantically exactly this) walking `forwarded_to_dealer → pushed_to_nbfc → closed`, all three already in the status CHECK — which is why the NBFC's existing "Acknowledge & close" button works on a direct thread with no state-machine change. **A messages table rather than more columns on the wrapper** because the wrapper has exactly three text surfaces (`nbfc_comments`, `admin_notes`, E-210 `attachments`) and each holds ONE value: a dealer reply has no home at all, and a second round from either side would overwrite the question it answers. Same shape and same reasoning as E-238 `nbfc_offer_negotiations` — one row per thing one party said at one moment, append-only. **A direct wrapper has NO children**, so `recomputeWrapperStatus()` early-returns on it (it projects status from child min-state and would otherwise pin a childless wrapper); the dealer's files ride on `nbfc_doc_request_messages.attachments` and are ALSO appended to `product_selections.pre_sanction_doc_urls` (E-208), which the NBFC dossier and admin product review already read — but the **message copy is canonical**, because that bucket is capped at 10 items and the cap must never block a reply. No FK on `request_id` and no CHECK on `party`, per the `nbfc_*` family convention (E-202/E-218/E-226/E-231/E-232/E-233/E-238) — vocabulary is enforced in the route layer and recorded in `COMMENT ON COLUMN`. **REQUIRED — this one does not degrade.** `nbfc_doc_requests` IS mirrored in `schema.ts` and `listThreadForLead()` does a bare `db.select().from(nbfcDocRequests)`, so on a database without this file BOTH the NBFC Acquire request thread and the admin NBFC card fail on their first read with `column "dealer_direct" does not exist`. Apply before deploying the code. Additive and idempotent — safe to re-run; guarded `EXCEPTION WHEN undefined_table` so it is a reported no-op where E-200 has not landed. Apply with `node scripts/apply-e240.mjs`, which takes an explicit `DATABASE_URL` override, prints the host **and which environment that host IS** before writing, and verifies the column type, all 8 message columns and both indexes from `information_schema`/`pg_indexes` rather than trusting the absence of an exception. **APPLIED to database-2 — which IS production — on 2026-08-13**, and to **database-1 — which IS sandbox — on 2026-08-13** (the `local (db-2)` = `prod` = database-2 pairing above means both prod boxes are that one apply). Verified on both from `information_schema`/`pg_indexes` rather than by the absence of an exception: `dealer_direct` present as `boolean NOT NULL DEFAULT false`, `nbfc_doc_request_messages` present with **8 columns and 0 rows**, both indexes present. **Idempotency proven** by re-running against db-1: the second pass raised only `42701`/`42P07 … already exists, skipping` NOTICEs and left the table at 8 columns / 0 rows. Pure additive DDL — contains no `INSERT`/`UPDATE`/`DELETE`/`DROP`/`ALTER TYPE`/`SET NOT NULL` on an existing column, so no production row was written. Post-apply probe: **17 existing wrappers on production and 0 on sandbox, all reading `dealer_direct = false`** — i.e. every request raised before this file stays on the admin-gated path, exactly as intended. **Renumbered E-239 → E-240 on 2026-08-13**, after main merged `E-239_campaign_counter_reconcile` while this branch was open and both sides claimed 239. Ours moved because main's was already merged and applied. The rename is cosmetic only: the DDL is byte-identical and there is no migration ledger in the database, so the apply recorded above (under the old name) IS this file — do not re-apply expecting a change. | ✅ | ✅ | ✅ | ✅ |
| E-245_offer_close_vocabulary | **OPTIONAL — comment-only, no DDL, no data.** Four `COMMENT ON COLUMN` refreshes (`nbfc_offer_negotiations.kind`, `nbfc_financing_offers.negotiation_status`, `nbfc_financing_offers.status`, `nbfc_lead_assignments.status`). This is the opposite of E-238/E-239/E-240: **nothing in the E-245 application code depends on it, and skipping it breaks nothing.** E-245 turns the dealer's counter-offer into a plain MESSAGE (the six numeric fields are gone — the dealer is not the party that prices a loan, so the NBFC re-prices from what the dealer wrote), adds a dealer **Close deal** action with a mandatory note, and lets the dealer re-route the lead to another lender afterwards. It writes four values that did not exist before — `nbfc_offer_negotiations.kind='close'`, `nbfc_financing_offers.negotiation_status='closed'`, `nbfc_financing_offers.status='withdrawn'`, `nbfc_lead_assignments.status='withdrawn'` — and **none of them needs a schema change**: the first two ride on E-238's deliberate no-CHECK design (enforcement lives in `src/lib/nbfc/offer-negotiation.ts`), and `'withdrawn'` has been permitted by the E-131 and E-140 CHECKs since those migrations shipped. It had simply never had a writer: E-245's `POST /api/lead/[id]/close-offer` is the FIRST code anywhere in the repo that sets either column to `'withdrawn'`, which also means the NBFC-portal branches that read that status get exercised for the first time. What IS stale without this file is the catalogue's self-documentation — the E-238 COMMENTs enumerate the old value sets and would tell the next reader that `'close'`/`'closed'` are corruption. Also note the two semantics worth not re-deriving from the code: a **fixed** offer can still be **closed** (fixing freezes the lender's numbers, it does not oblige the customer to buy), and `'withdrawn'` is NOT `'not_selected'` — `select-winner` now skips withdrawn/declined rows in its loser sweep precisely so it cannot overwrite the record of why that conversation ended. `COMMENT ON` is a full replace, so re-running is a no-op. Applied to database-1 (sandbox) AND database-2 (production) on 2026-08-14 via scripts/apply-e241.mjs, which reads the four COMMENTs back out of col_description and also asserts that both CHECKs still permit `withdrawn` — a comment-only migration has no other observable effect to verify. **Renumbered E-241 → E-245 on 2026-08-17** when `main`'s E-241/E-242/E-243 (scraper batch queue, quotation draft/dispatch, quotation dealer decision) merged in and claimed the same three numbers. The file content is byte-identical apart from the number; it was already applied to the ticked environments under the OLD name, the database has no migration ledger, and re-running it under the new name is a no-op. Column COMMENTs written by the earlier apply still carry the old number — cosmetic, nothing reads them. For this one the old wording is not even left behind: `scripts/apply-e245.mjs` re-runs the four `COMMENT ON` statements before it verifies them, and COMMENT ON is a full replace, so applying it once under the new number rewrites `E-140/E-241` to `E-140/E-245` in place. | ✅ | ✅ | ✅ | ✅ |
| E-241_scraper_batch_job_queue | One new table `scraper_job_queue` (21 cols) plus three indexes — two of them **partial** (`idx_scraper_job_queue_claim (created_at, seq) WHERE status='queued'`, `idx_scraper_job_queue_run (run_id) WHERE run_id IS NOT NULL`) and one plain (`idx_scraper_job_queue_batch (batch_id, seq)`). Gives the scraper somewhere to **park** work. The form is one free-text input posting `{ query }` to `POST /api/scraper/run`, and that route holds a **global single-run lock** (409 while any run is `'running'`), so ten product phrasings across ten cities means the operator starts a hundred runs by hand and waits at the screen for each. The fan-out was never the missing piece — `startChunkedRun()` already crosses a query list with a city list and writes one `scraper_run_chunks` row per combination — what was missing is a durable list of "these hundred (query, city) pairs still need running" that survives the browser closing, a pm2 restart, and the end of the working day. One row = one pending scrape; the 30s dispatcher ticker (`startScraperQueueTicker` in `instrumentation-node.ts`, driven by `jobQueue.dispatchOnce()`) claims the oldest eligible row, creates an ordinary `scraper_runs` row, and calls the existing `startChunkedRun()` — chunk execution, finalize, dedupe, promotion into `dealer_leads`, the 2.5s progress poll and the run-detail view all still see a perfectly normal run. **A new table, not columns on `scraper_runs`** — that table is a documented trap: the 26-line comment at `schema.ts:~3246` records that `last_progress_at` was once added to the `scrapeRuns` drizzle object without an applied migration and, because drizzle names every column of a table object in its INSERT, the first line of **every scrape on every database** died with `column "last_progress_at" ... does not exist`. Seven scheduling columns there is seven more chances to repeat it, on a table with two duplicate declarations (`scraperRuns` ~2618, `scrapeRuns` ~3219) so a mirror edit must find the right one. A brand-new table cannot fail that way: unapplied, the new `/api/scraper/batch` routes 500 loudly and the existing single-query scraper keeps working untouched. **Still a FLAT job list, not a parent campaign** — `batch_id` is a grouping LABEL for the UI, nothing rolls up through it, and every row becomes its own independent run with its own progress, cancel and history entry. **Not `scraper_city_queue`**, which is shaped almost right but has never been written by anything (dead from an earlier design; E-227 left it alone for the same reason), has no batch and no window, and carries a `NOT NULL state` this feature has no value for — adopting it would mean altering a dead table into a live one. **The window columns copy E-228 exactly** (`window_start`/`window_end varchar(5)` `'HH:MM'` + `window_days jsonb`), which in turn mirrors `assignment_config.working_hours_start`/`working_days` (E-120), so one predicate reads either without a translation layer; times are IST and are compared against Postgres `now()`, never a JS `Date` (`src/lib/nbfc/auction/scheduler.ts` records what app-clock comparisons cost on a pm2 VPS with drifting NTP). `schedule_mode` is the whole recurrence model and is deliberately small — `now` (dispatch when free), `once` (wait for `run_after`), `daily` (dispatch only inside the IST window on a listed day) — so **"recurring until every lead is processed" needs NO pause/resume bookkeeping**: queued rows sit here while the window is shut and the ticker picks them up when it reopens, day after day, until none are left; a job already dispatched when the window closes runs to completion, the same rule E-228 sets for a call already in flight. `window_end < window_start` is legal and means an overnight window (22:00–06:00) — the wrap lives in one predicate instead of in the data. `status` is a plain `varchar(16)` with no CHECK and no enum per this family's convention (E-202/218/220/221/226/230/231/232/237/238): the vocabulary `queued\|running\|done\|failed\|cancelled` lives in `src/lib/scraper/jobQueue.ts` and is enforced by zod at the write path. `seq` exists because a whole submission is inserted in one statement and shares `created_at` exactly, so ordering by that alone cannot honour the operator's spreadsheet order — but the claim orders by **`(created_at, seq)`, created_at first**, which reads as "oldest batch first, then the operator's own row order" and drains batches FIFO. Leading with `seq` would sort ACROSS batches, since seq restarts at 0 for every submission: all the 0s would run, then all the 1s, three concurrent batches would round-robin, nothing would finish until nearly everything had, and no operator could be told when their own batch would be done. The index column order matches so the partial index serves the sort directly. (Caught by `probe-sql.mjs`-style validation against a temp table before this file was ever applied — the first draft had the two columns the other way round in the index, the ORDER BY and the schema mirror alike.) **Mirrored in `schema.ts` as `scraperJobQueue`** — safe because the table is new, so there is no pre-existing statement for an extra column to break; the two partial indexes are migration-only (drizzle's index builder has no `WHERE`). **DEPENDS ON E-227** — without `scraper_runs.last_progress_at` the reaper still force-fails any run older than 10 minutes and the dispatcher calls it every 30s tick, so a legitimately long queued job is killed at minute 11. Additive and idempotent, wrapped in one `BEGIN`/`COMMIT`. Apply with `node --env-file=.env.local scripts/apply-e241-scraper-batch-job-queue.mjs` (`--dry-run` to check without writing) — **renamed from `scripts/apply-e241.mjs` on 2026-08-17**, because the offer-close-vocabulary applier owned that path at the time and the two collided as an unresolved merge conflict (one `.mjs` cannot be both: each declares its own `url`, `sql` and top-level `try`). The newcomer moved, the same call E-240 made when main merged a conflicting E-239 mid-branch. (That collision is now historical: the other file was renumbered to `E-245_offer_close_vocabulary` with `scripts/apply-e245.mjs` on 2026-08-17, so `apply-e241.mjs` is free again — this script keeps the long name rather than churn twice.) **The `.sql` filename is deliberately NOT renumbered** — this file was already applied to db-1/sandbox under the E-241 name on 2026-08-16, there is no migration ledger in the database, and renaming an applied file would invalidate that record for no gain. So E-241 legitimately names two unrelated migrations; disambiguate by the slug, never the number, which **refuses to run unless E-227 is present**, refuses any file containing a non-additive statement, and then proves the result by querying `information_schema` / `pg_indexes` rather than trusting the absence of an exception — including that `idx_scraper_job_queue_claim` and `idx_scraper_job_queue_run` are genuinely **partial**, which matters because Drizzle's index builder has no `WHERE` and an index quietly created without its predicate would still "exist" while scanning the whole table on every 30s dispatch tick. The additive guard strips `--` comments **and single-quoted string literals** before scanning: this file is mostly `COMMENT ON` prose and one of those comments contains the word INSERT, which a naive scan reads as a data-mutating statement. **E-227 was verified already present on db-1/sandbox on 2026-08-16** (see its row above). **APPLIED to db-1 — which IS sandbox — on 2026-08-16** via pgAdmin (both middle ticks below are that one apply), and verified afterwards by querying `information_schema` / `pg_indexes` rather than trusting the absence of an exception: all **21** columns present, all three indexes present, and `idx_scraper_job_queue_claim` / `idx_scraper_job_queue_run` confirmed genuinely **partial**. The claim index was also checked for COLUMN ORDER and reads `(created_at, seq)` — the corrected FIFO order, not the pre-fix `(seq, created_at)`. That check is not pedantry: `CREATE INDEX IF NOT EXISTS` would leave a wrong-order index in place forever, since the name already exists, so re-running the file could never repair it — it would have to be dropped and recreated by hand. Table created empty (0 rows). **Still ☐ on database-2 — which IS production** — not probed, and E-227 has not been probed there either; the applier refuses to run without it. | ☐ | ✅ | ✅ | ☐ |
| E-246_kyc_auto_approval_sla | Four `varchar(16) DEFAULT 'admin'` provenance columns — `kyc_verifications.admin_action_source`, `other_document_requests.review_source`, `consent_records.verification_source`, `kyc_verification_metadata.final_decision_source` — plus three columns on `admin_verification_queue` (`sla_due_at timestamptz`, `auto_approved_at timestamptz`, `auto_approval_result varchar(24)`) and one **partial** index `admin_verification_queue_sla_due_idx (sla_due_at) WHERE auto_approved_at IS NULL AND status='pending_itarang_verification'`. Backs a time-boxed automation of the admin KYC review: when the dealer submits documents + coupon an SLA deadline is stamped on the queue row, and if the case is still untouched when it passes, a 60s ticker accepts the still-PENDING verification cards, verifies the required supporting docs, and runs the ordinary final-decision path so `leads.kyc_status` becomes `step_3_cleared` and the dealer's Step 4 unlocks. Consent is auto-verified the moment it is signed. **⚠ THE SWEEP ACCEPTS IDENTITY CARDS WITHOUT CALLING THE PROVIDERS** — no Decentro PAN/bank/bureau call, no DigiLocker Aadhaar pull; `api_response` is left exactly as the provider did or did not leave it, so a row never claims a check that never happened. That is precisely why the `*_source` columns exist: after this file `admin_action='accepted'` is **no longer evidence that a human or a registry looked at the document**, and any consumer that cares must read `admin_action_source` too. The compliance trade-off is the business's decision; the DDL only makes it auditable. **`*_source` columns rather than a sentinel user** because every actor column in this path (`kyc_verifications.admin_action_by`, `other_document_requests.reviewed_by`, `kyc_verification_metadata.final_decision_by`, `audit_logs.performed_by`) is already NULLABLE — so a system action *can* just leave the uuid NULL, but NULL is ambiguous: rows written before those actor columns existed are also NULL, so NULL alone cannot answer "did a person accept this?". A discriminator can, and it follows the one precedent already in the schema — `buyback_activity_log` carries a nullable `actor_id` beside a NOT NULL `role` documented as `'dealer'\|'admin'\|'system'`. Defaulting to `'admin'` makes every pre-existing row read as human-actioned, which is what it was. **The clock lives on `admin_verification_queue`, not `kyc_verification_metadata`**: that row is what the sweep claims with `FOR UPDATE SKIP LOCKED`, it already carries the open/terminal status vocabulary the sweep filters on, and it is already written in the dealer submit's transaction — the claim and the deadline belong on the same row, otherwise the sweep must join to discover whether the case is even still open. `auto_approved_at` is the **idempotency guard**: a row is claimed at most once regardless of outcome, so a case that legitimately fails the approve gate (an admin had already rejected a card — the deliberate behaviour, per the "respect the human verdict" decision) is recorded `auto_approval_result='blocked'` and never retried. **NO BACKFILL, ON PURPOSE** — `sla_due_at` is left NULL on every existing open queue row and the sweep skips NULL, so nothing submitted before this ships can retro-auto-approve; enabling the feature affects only cases submitted afterwards. **No CHECK constraints** on the three vocabularies, per this family's convention (E-202/E-218/E-226/E-231/E-232/E-238/E-240): enforcement lives in `src/lib/kyc/auto-approval.ts` + the settings zod schema and the values are recorded in `COMMENT ON COLUMN`; a CHECK would also turn "add a third source" into a migration. **The index is PARTIAL and migration-only** — drizzle's index builder has no `WHERE`, so it must never be "recreated" from `schema.ts`; a same-named index built without the predicate would satisfy a name-only check while scanning the whole queue on every 60s tick (the same trap E-241_scraper_batch_job_queue documents). **REQUIRED BEFORE THE CODE DEPLOYS, though not for existing behaviour**: all five tables ARE mirrored in `schema.ts` and several are read with a bare `db.select().from(...)`, and drizzle names every column of a mirrored table in its generated SQL — so on a database without this file the admin KYC review page fails on its first read with `column "admin_action_source" does not exist`. The automation itself stays inert regardless: `app_settings` key `kyc_auto_approval` ships `enabled=false`, so applying this file alone changes no behaviour at all. Additive and idempotent, wrapped in one `BEGIN`/`COMMIT`, every block `EXCEPTION WHEN undefined_table` guarded. Verify with `SELECT column_name FROM information_schema.columns WHERE column_name IN ('admin_action_source','review_source','verification_source','final_decision_source','sla_due_at','auto_approved_at','auto_approval_result')` (expect 7) and confirm the index is genuinely partial via `SELECT indexdef FROM pg_indexes WHERE indexname='admin_verification_queue_sla_due_idx'`. **APPLIED to database-2 — which IS production — on 2026-08-17** (the first and last tick below are that one apply, per the `local (db-2)` = `prod` = database-2 pairing above), via `node scripts/apply-e242.mjs` with an EXPLICIT `DATABASE_URL` override. The override was not optional: the active `DATABASE_URL` in `.env.local` pointed at **database-1 (sandbox)** at the time, with the database-2 line commented out, so a default `--env-file=.env.local` run would have silently applied this to the wrong database. The applier prints the host AND which environment that host IS before writing anything. Verified afterwards by querying `information_schema` / `pg_indexes` rather than trusting the absence of an exception: all **7** columns present with the four `*_source` columns carrying `DEFAULT 'admin'`, all 7 `COMMENT ON COLUMN` strings readable back out of `col_description`, and `admin_verification_queue_sla_due_idx` confirmed genuinely **PARTIAL** from `pg_indexes.indexdef` — `WHERE ((auto_approved_at IS NULL) AND ((status)::text = 'pending_itarang_verification'::text))` — asserted from the predicate, not by name. **Idempotency proven** by re-running against database-2: the second pass reported 7/7 columns already present, raised only `already exists, skipping` NOTICEs, and changed nothing. Pure additive DDL — the applier refuses any file containing `INSERT`/`UPDATE … SET`/`DELETE`/`TRUNCATE`/`DROP COLUMN`/`ALTER TYPE`/`SET NOT NULL`/`RENAME` (stripping `--` comments and the `$c$…$c$` COMMENT prose first, so an English "update" inside a comment is not misread as a statement), and E-246 contains none — so no production row was read or written. **Post-apply probe: 33 open KYC queue rows on production, 0 of them carrying an `sla_due_at`** — exactly the intended safe state, since no backfill ships with this file and the sweep skips NULL, so not one of those 33 pre-existing cases can ever auto-approve. **The feature is still OFF**: `app_settings` key `kyc_auto_approval` ships `enabled=false`, so this apply changed no behaviour on its own. **APPLIED to database-1 — which IS sandbox — on 2026-08-17** (the two middle ticks below are that one apply), again via an EXPLICIT `DATABASE_URL` override rather than the ambient `.env.local`, and verified the same way: 7/7 columns by (table, column) pair, the four `*_source` columns carrying `DEFAULT 'admin'`, and the index confirmed genuinely **PARTIAL** from its predicate. **Idempotency proven** by re-running: 7/7 already present, nothing changed. Post-apply probe: **57 open KYC queue rows on sandbox, 0 carrying an `sla_due_at`** — the same intended safe state as production. A cross-check of both databases afterwards reported them **identical on all seven columns and on the index predicate**. One gotcha for anyone re-verifying: counting these column names across the whole schema returns **8**, not 7 — the extra is the pre-existing `field_investigations.sla_due_at` (the NBFC Field Investigation 48h SLA), which has nothing to do with E-246. Verify by (table, column) pair, not by column name alone. **Renumbered E-242 → E-246 on 2026-08-17** when `main`'s E-241/E-242/E-243 (scraper batch queue, quotation draft/dispatch, quotation dealer decision) merged in and claimed the same three numbers. The file content is byte-identical apart from the number; it was already applied to the ticked environments under the OLD name, the database has no migration ledger, and re-running it under the new name is a no-op. Column COMMENTs written by the earlier apply still carry the old number — cosmetic, nothing reads them. | ✅ | ✅ | ✅ | ✅ |
| E-247_consent_auto_verify_sla | One column `consent_records.auto_verify_due_at timestamptz` (nullable, no default, **no backfill**) plus one **partial** index `consent_records_auto_verify_due_idx (auto_verify_due_at) WHERE auto_verify_due_at IS NOT NULL AND consent_status IN ('esign_completed','admin_review_pending','manual_uploaded')`. The consent half of E-246's SLA, and a fix for two bugs in it. E-246 verified a consent **immediately** on reaching a signed-but-unverified state, so (a) an admin never got a window in which to reject a bad one, and (b) — the serious one — the consent sweep selected on `consent_status` alone with no per-case opt-in, so switching the feature on verified **every** pending consent in the table retroactively, including ones signed months earlier. That is what happened on sandbox on 2026-08-17: 14 real consents flipped in a single tick. `auto_verify_due_at` is the scope guard: it is stamped at signing time exactly as `admin_verification_queue.sla_due_at` is stamped at dealer submit, the sweep skips NULL, and every consent predating this file (or signed while the feature is off) keeps NULL — so reaching backwards is structurally impossible rather than merely discouraged. **Do not backfill this column**; that would re-create the bug the file exists to remove. No idempotency marker is needed — verifying moves `consent_status` to `verified` and rejecting moves it to `admin_rejected`, either of which leaves the eligible set. **REQUIRED BEFORE THE CODE DEPLOYS**, and the failure is loud: `consent_records` is mirrored in `schema.ts` and read with a bare `db.select()` in `GET /api/admin/kyc/[leadId]/case-review`, and Drizzle names every column of a mirrored table in its generated SQL, so on a database missing this file the admin KYC review page 500s with `column "auto_verify_due_at" does not exist` and the 60s ticker throws `sweepDueConsents` every minute. Applying it changes no behaviour on its own — `app_settings.kyc_auto_approval` still ships `enabled=false`. Apply with `node scripts/apply-e243.mjs` (`--dry-run` to inspect first); it re-reads the column out of `information_schema` and the index predicate out of `pg_indexes` — a name-only check would pass on an index built without the WHERE clause, which would then scan every consent row on every tick and could never be repaired by `CREATE INDEX IF NOT EXISTS`. It also warns if E-246 is absent on the same host, since applying only this half leaves the other four tables broken. Applied to **database-1 and database-2 (production) on 2026-08-17**, in that order. Both verified: column and partial index present on each, and a cross-check afterwards reported the two hosts **identical on the column type/nullability and on the full index predicate**. Deadline counts at apply time were **0 on both** — db-1 had 15 signed-but-unverified consents and db-2 had 0, none carrying a deadline on either, which is the intended safe state (nothing already signed can be swept). Prod therefore no longer carries E-246's retroactive-sweep bug; the automation is still OFF on both. **Renumbered E-243 → E-247 on 2026-08-17** when `main`'s E-241/E-242/E-243 (scraper batch queue, quotation draft/dispatch, quotation dealer decision) merged in and claimed the same three numbers. The file content is byte-identical apart from the number; it was already applied to the ticked environments under the OLD name, the database has no migration ledger, and re-running it under the new name is a no-op. Column COMMENTs written by the earlier apply still carry the old number — cosmetic, nothing reads them. | ☐ | ✅ | ☐ | ✅ |
| E-248_kyc_card_sla_windows | Two columns on `admin_verification_queue` — `sla_card_due_at jsonb` and `sla_next_due_at timestamptz` — plus one **partial** index `admin_verification_queue_sla_next_due_idx (sla_next_due_at) WHERE sla_next_due_at IS NOT NULL AND auto_approved_at IS NULL AND status='pending_itarang_verification'`. Gives each verification card **its own** SLA window: an admin can clear Aadhaar in 20 minutes while the Equifax card waits an hour (`app_settings.kyc_auto_approval.cardSlaMinutes`, a partial map — a card left out inherits the global `slaMinutes` and keeps tracking it when that changes). `sla_card_due_at` is the **snapshot** of the resolved per-card deadlines taken at dealer submit, `{"aadhaar":"…Z",…}`; it is a snapshot rather than a lookup at sweep time because the settings screen promises that changing a window only affects cases submitted from now on, and resolving it late would quietly break that. `sla_next_due_at` is the sweep's **pointer** at the earliest deadline still to act on: a case now needs visiting once per distinct window where E-246 visited it exactly once, and the pointer is what keeps that selection on an index instead of scanning every open queue row each tick. **Two behaviours changed in the sweep, both deliberate.** (1) A card is accepted only when ITS window closes, and the case is finally approved only after the **last** one has — approving while a card sits inside its review window would make the window meaningless, so the longest per-card window now decides when the dealer's Step 4 unlocks. (2) The claim is now the pointer (pushed 1 minute ahead), not `auto_approved_at`; stamping the finality marker up front would have ended the case at its first, shortest window. `auto_approved_at` keeps its old meaning — the sweep has had its one FINAL attempt — and the error path now stamps it explicitly, because a 1-minute pointer claim would otherwise retry a throwing lead every tick forever. **NULL still means "never".** Both columns are NULL for every pre-E-248 row and everything submitted while the feature is off; the sweep falls back to `sla_due_at`, so an older case behaves exactly as E-246 left it with all cards maturing together. No backfill, same reason as E-247. **REQUIRED BEFORE THE CODE DEPLOYS** — `admin_verification_queue` is mirrored in `schema.ts` and read with a bare `db.select()`, so a database without this file fails the KYC review page on its first read with `column "sla_card_due_at" does not exist`. Apply with `node scripts/apply-e244.mjs` (`--dry-run` first); it re-reads both columns out of `information_schema` and the index predicate out of `pg_indexes`, warns if E-246 is absent, and asserts the two columns are still NULL everywhere (a non-zero count means a backfill this file forbids). A compile-time guard in `auto-approval.ts` (`CARD_SLA_TYPE_COVERAGE`) keeps the settings list and `ADMIN_CARD_VERIFICATION_TYPES` in lockstep in both directions. Applied to **database-1 and database-2 (production) on 2026-08-17**, in that order. Both verified column-by-column with the index predicate read back from `pg_indexes`, and a cross-check afterwards reported the two hosts **identical on both columns and on the full index predicate**. Idempotency proven by re-running against database-1: all three objects reported `already exists, skipping` and the verification still passed. Open queue rows at apply time — db-1 60, db-2 33 — with **0 carrying either column on either host**, so no existing case picked up per-card deadlines retroactively. Per-card windows stay unused until an admin sets one in /admin/settings/kyc-automation. **Renumbered E-244 → E-248 on 2026-08-17** when `main`'s E-241/E-242/E-243 (scraper batch queue, quotation draft/dispatch, quotation dealer decision) merged in and claimed the same three numbers. The file content is byte-identical apart from the number; it was already applied to the ticked environments under the OLD name, the database has no migration ledger, and re-running it under the new name is a no-op. Column COMMENTs written by the earlier apply still carry the old number — cosmetic, nothing reads them. It moved even though it never collided: it extends E-246's `auto_approved_at` in an index predicate, so it has to keep sorting AFTER it. | ☐ | ✅ | ☐ | ✅ |
| E-249_ai_dialer_disposition_and_block_vocabulary | **COMMENT-only, OPTIONAL, no DDL** (same class as E-245). Adds no columns, tables, indexes or constraints, and the application behaves identically with or without it. Documents the vocabulary introduced by the AI-connected hard block and the AI/inside-sales dispositions: `lead_touchpoints.touchpoint_type = 'ai_call'` (a legal `TOUCHPOINT_TYPE` member since Part 0 with **zero rows** until now — the AI dialer wrote `ai_call_logs` and the `dealer_leads` rollups but never a touchpoint, so AI calls were invisible to the lead timeline, the E-236 filters and every connect-rate figure); `dealer_leads.last_disposition_source ∈ {'ai_dialer','inside_sales'}` alongside the existing `'neodove'` — the day E-236's own header anticipated; `dialer_campaign_leads.call_outcome = 'ineligible_ai_connected'`; and the full key list for `dialer_campaigns.region_filter`, which despite its name has carried non-region keys since E-109 and now also holds `filters{}`, `kind:"scrape_run"`, `runId` and `runCities[]`. **There is no source precedence** — all three disposition writers guard on `last_disposition_at <= the new call time`, so the latest call owns the row whichever system saw it. **No index is added and that is deliberate**: `ai_call_logs` held 298 rows / 83 with a transcript / 27 with `signals` / 4 with a `band` on 2026-08-18, so a GIN on `signals` would index 27 documents to accelerate a two-page scan and would never be planned; a denormalised `dealer_leads.ai_connected_at` was rejected for the same reason plus a backfill and a new write in **both** providers' private `upsertAiCallLog`. Revisit at ~50k rows. **`dealer_leads.intent_band` is populated on 4 of 3,941 rows**, so every AI filter reads `ai_call_logs` through a LATERAL, never the rollups. Historical backfill of ≤298 touchpoints is a **separate re-runnable script** (`npm run backfill:ai-touchpoints`, `--apply` to write), not this file, and it deliberately does **not** write `last_touchpoint_at` (it feeds `IDLE_BASIS` — backfilling would make ~119 leads visibly younger overnight and clear stale cues) or `last_disposition*` (a current-state column whose newest AI call is months old, against 480 fresher NeoDove rows). Applied twice against db-1 during development: second run is a clean no-op. Audit query: `SELECT external_stage, external_tag, connect_status, disposition, COUNT(*) FROM lead_touchpoints WHERE touchpoint_type='ai_call' GROUP BY 1,2,3,4 ORDER BY 5 DESC;` | ☐ | ☑ | ☐ | ☐ |
| E-250_intent_review_loop | The **intent-review loop**: human corrections that actually change the lead, attached call recordings that get transcribed and scored, and a calibration set the extraction prompt reads from the DB at runtime. Five parts. (1) **`intent_score_feedback`** gains `ai_band`, `reviewer_role`, `review_kind` (default `'correction'`), `source` (default `'app'`), `external_key`, `recording_id`, `agreed`, `applied_to_lead`, `applied_at` — plus a PARTIAL unique index on `external_key` (⚠ any `ON CONFLICT` against it MUST repeat `WHERE external_key IS NOT NULL` or Postgres will not match the index) and a partial disagreement index. E-159 recorded corrections but nothing consumed them: the lead kept routing on the AI's band. (2) **`lead_call_recordings`** (new) — one attached audio file, its transcription lifecycle and the band derived from it. The table IS its own queue (`status`/`attempts`/`claimed_at`/`next_attempt_at`, claimed `FOR UPDATE SKIP LOCKED` by `startRecordingTranscriptionTicker()` in `src/instrumentation-node.ts`); one recording is one job, one-to-one, so a separate job table would be a join with no cardinality. (3) **`intent_calibration_examples`** (new) — admin-promoted few-shot examples loaded at runtime by `src/lib/ai/analysis/calibrationStore.ts`, replacing 'edit calibrationExamples.ts and redeploy' as the way to teach the model. The hand-authored array survives as the SEED and is the fallback whenever this table is empty or unreachable. (4) **`ai_call_logs`** gains `extraction_version`, `calibration_set_hash`, `human_band`, `human_reviewed_by`, `human_reviewed_at` — `band` is deliberately NOT overwritten by a correction, because the eval harness replays it and would otherwise consume its own correction and report 100% agreement forever. (5) **`dealer_leads`** gains `intent_band_source` (default `'ai'`), `intent_overridden_by`, `intent_overridden_at`. ⚠ These three are deliberately ABSENT from `dealerLeads` in `schema.ts` (same rule as E-242/E-224/E-236) — Drizzle names every column of a table object, so declaring them would hard-fail ~20 bare `db.select().from(dealerLeads)` call sites at parse time wherever this file is unapplied; they are read/written via raw `sql` in `src/lib/leads/intentOverride.ts`, confining an unapplied E-250 to the review feature. Every section is wrapped in `DO $do$ … EXCEPTION WHEN undefined_table` so a database missing E-159 raises a notice instead of aborting. Fully idempotent — applied twice against db-1, second run a clean no-op. Audit query: `SELECT review_kind, source, count(*) FROM intent_score_feedback GROUP BY 1,2;` and `SELECT status, count(*) FROM lead_call_recordings GROUP BY 1;` | ✅ | ✅ | ✅ | ✅ |
| E-251_sandbox_to_prod_drift | Closes the **structural gap between sandbox (database-1) and production (database-2)** found by `npm run db:drift` on 2026-08-18 (`docs/db/drift/2026-08-18-1417/`). Diff 3 is a LIVE introspection of both servers, not a reading of this file — which matters, because on the same day E-185 and E-202 were both found live on prod while reading `☐ ☐` here. Delivers: one table `module_usage_user_daily` (10 cols, PK `(day,module,user_id)`, 3 indexes); **E-237's three neodove columns** + its partial index; three indexes on `inventory_upload_reports`; and two FKs, `nbfc_tenant_id_fkey` and `field_investigations_agent_fk`. **Re-delivers E-237 on purpose** — `scripts/apply-e237.mjs` hardcodes `.env.local`, which IS sandbox, so it cannot reach prod at all; every statement is `IF NOT EXISTS`/guarded, so the two files converge in any order. **Tick E-237's prod box together with this one.** The two FKs are the only statements that can fail on DATA rather than schema (`ADD CONSTRAINT ... FOREIGN KEY` validates existing rows immediately); prod was measured at **0 orphans on both** (nbfc 8 rows, field_investigations 16 rows) before writing, and they are still wrapped in a `foreign_key_violation` handler that SKIPS the constraint with a NOTICE rather than aborting the file, since that count is a snapshot of a database that keeps writing. `field_investigations_agent_fk` is added HERE rather than by re-running E-148, which owns it — the 2026-07-25 note below records E-148 was deliberately not run whole on db-2 because it rewrites FI state. **`module_usage_user_daily` has NO code references on any branch** (checked with `git grep` across every remote) and no migration ever created it; it lands empty and is one deletable block if you would rather not carry it. `product_selections.sub_category` (only in PRODUCTION) is deliberately NOT mirrored back — E-103 renamed it to `model_number`; prod just never dropped the old name. db-1/sandbox already holds every object in this file (it is the source side of the diff), so applying it there is a verified no-op. Additive and idempotent. Mirrored in `schema.ts` (the new table only). Apply with `node scripts/apply-e251.mjs <env-file> --apply` — **the target is mandatory, there is no default**, because the `.env.local` default in every earlier applier is what silently sends a prod-intended change to sandbox. **APPLIED to database-1 (= sandbox) on 2026-08-18** as the idempotency proof: every statement reported `already exists, skipping` / `skip: … already present` and the after-state is unchanged — which is the expected result, since sandbox is the source side of the diff. **APPLIED to database-2 (= PRODUCTION) on 2026-08-18**: all 13 objects went MISSING -> present in one pass, both FK orphan checks read 0 against the live target immediately before the write, and the applier's own after-state re-read confirmed every one. **Independently verified by re-running `npm run db:drift`** (`docs/db/drift/2026-08-18-1504/`): Diff 3 went from `1 only in sandbox, 6 table(s) with shape diffs` to `1 table(s) with shape diffs`, the survivor being only `product_selections.sub_category` — the prod-only E-103 rename leftover this file deliberately does not mirror back. | ✅ | ✅ | ✅ | ✅ |
| E-252_auction_settlement_payment_refinance | **Renumbered 2026-08-19 from E-249** (main claimed E-249..E-251 first). Five columns on `auction_settlements` — `payment_ref varchar(120)`, `payment_provider varchar(24)`, `paid_at timestamptz`, `refinance_loan_id varchar(255)`, `failure_reason text` — plus two plain indexes on `payment_ref` and `refinance_loan_id`. **The money columns E-232 deliberately deferred**, in its own words: *"payment_ref and refinance_loan_id are deliberately NOT added here — they belong to Phase 6, which is out of scope."* Until this file the settlement ladder `payment_pending → in_transit → delivered` (E-039) recorded **no evidence that payment ever happened** — the seller flipped the status by hand, nothing verified it, and a winner who never paid left the row in `payment_pending` for ever with no way to say so and no route back to re-listing. `paid_at` becomes the gate on advancing to `in_transit`. **`refinance_loan_id` is varchar(255), NOT uuid** — it points at `loan_sanctions.id`, which is `character varying`; E-232 had to ship a whole self-correction `DO $do$` block after an earlier draft typed `winner_dealer_id` as uuid against `accounts.id` values like `ACC-ITARANG-20260409-971`, and this is the same trap one table over. `payment_provider` carries `razorpay | offline`, where **offline is an explicit audited admin action** for a bank transfer settled outside the app — a visible escape hatch rather than a silent bypass, which matters because manual testing cannot drive a live gateway. **No CHECK constraints** on either vocabulary, per this family's convention (E-202/218/226/231/232/238/240/246): enforcement lives in the service + zod and the values are recorded in `COMMENT ON COLUMN`. **REQUIRED BEFORE THE CODE DEPLOYS** — `auctionSettlements` is mirrored in `schema.ts` and read with a bare `db.select()` in `listSettlements()`, and Drizzle names every column of a mirrored table in its generated SQL, so on a database without this file the NBFC recovery page and the dealer purchases screen fail on their first read with `column "payment_ref" does not exist`. Additive and idempotent, one `BEGIN`/`COMMIT`. Verify with `SELECT column_name FROM information_schema.columns WHERE table_name='auction_settlements' AND column_name IN ('payment_ref','payment_provider','paid_at','refinance_loan_id','failure_reason')` (expect 5). **Applied to database-1 (= sandbox) on 2026-08-18** — verified 5/5 columns and 2/2 indexes via `information_schema` / `pg_indexes`. Still ☐ on database-2 (= local / prod); apply there before this branch deploys, or the NBFC recovery page and the dealer purchases screen fail on their first read. | ☐ | ✅ | ✅ | ☐ |
| E-253_remove_demo_auction_lots | **Renumbered 2026-08-19 from E-250** (main claimed E-249..E-251 first). **RENAMED + REVERSED 2026-08-18 — was `E-250_backfill_demo_auction_lots`.** Deletes the five E-129 `DEMO-LOT-001..005` rows and every child row hanging off them, instead of repairing them. The original plan was to give them a `seller_tenant_id`, an India-wide `auction_lot_visibility` rule and a frozen `auction_lot_audience`, because without a seller `closeLotNow` logs "closed with a winning bid but NO settlement" on every tick and without an audience row no dealer can see them at all (the dealer grid INNER JOINs it). **Probing database-1 killed that plan:** all five are `status='ended'` and expired between 23 and 30 May 2026, and they hold ZERO `auction_lot_items`. The backfill's window-repair statement only touches `status='live'`, so it would have been a no-op — leaving five permanently-ended, item-less lots plus 940 new audience rows (47 eligible dealers x 4 channels x 5 lots) no dealer could ever bid against. **Two defects found in the original draft, both fatal:** its seller subquery said `WHERE status = 'active'` on `nbfc_tenants`, which has no `status` column on either database (it is `is_active boolean`, and `getCurrentTenant()` filters on `eq(nbfcTenants.is_active, true)`) — that alone would have aborted the whole transaction at statement one; and its commented-out delete branch named only 4 child tables, missing `nbfc_auction_lot_actions` (5 demo rows), `auction_auto_bids` and `nbfc_auction_cancel_requests`. The shipped file deletes all 8 child tables in child-before-parent order, because `auction_lots` has **no foreign keys pointing at it** (verified against `information_schema` — the constraint list came back empty), so nothing cascades and nothing blocks. Also releases any `recovery_batteries` stuck in `state_code='lotted'` by a demo lot (0 today; guarded for a replay). **E-129 was retired in the same change** — its INSERT is commented out with a header explaining why, so a folder replay cannot resurrect the rows. Idempotent: re-running deletes nothing. **Applied to database-1 (= sandbox) on 2026-08-18** — verified 0 demo lots, 0 orphan actions/bids/settlements, 15 real lots untouched, 0 batteries stuck lotted. Still unapplied on database-2 (= local / prod). | ☐ | ✅ | ✅ | ☐ |
| E-254_dialer_campaign_schedule_mode | One column: `dialer_campaigns.schedule_mode varchar(16) NOT NULL DEFAULT 'now'`, plus a `COMMENT ON` for it and a restated `COMMENT ON dialer_campaigns.status`. No index (E-228's partial `idx_dialer_campaigns_resume` already covers the resume ticker's only predicate). **Completes E-228, which shipped the whole calling-window SCHEMA and none of the code.** E-228 assumed one answer to "what happens when the window closes" — pause tonight, wake tomorrow — so it has no way to express a campaign that should run its window ONCE. This column carries that distinction and nothing else: `'now'` = unscheduled, dial continuously (the default, and exactly the pre-E-228 behaviour for every existing row); `'single'` = dial the window, then `status='paused'` with `resume_after` NULL, which nothing claims; `'recurring'` = dial the window on every `window_days` day, closing to `status='scheduled'` with `resume_after` armed for the 60s resume ticker. Values deliberately DIVERGE from E-241's `'now'\|'once'\|'daily'` even though the two families share the `'HH:MM'` + `["mon",…]` vocabulary: E-241's `once` fires at an *instant*, ours runs a *window then stops*. Free text, no CHECK, no pgEnum (the convention since E-202); enforced by zod in `src/lib/queue/campaignWindow.ts`. **REQUIRED, not optional** — Drizzle names every column of `dialer_campaigns` in its INSERTs, so an unapplied DB throws `column "schedule_mode" does not exist` on the first campaign created, scheduled or not. Additive and idempotent. **Note on the E-228 row above:** its `☐ ☐ ☐ ☐` is STALE — a live `information_schema` read of database-1 on 2026-08-19 found all six E-228 columns and `idx_dialer_campaigns_resume` present, and E-251's 2026-08-18 drift close names no `dialer_campaigns` statement, so prod is aligned too. Run `npm run db:drift` rather than trusting either mark. | ☐ | ☐ | ☐ | ☐ |
| E-255_storage_drive_mirror | **Google Drive backup of every stored document.** One NEW table `storage_drive_mirror` (bigserial `id`, `bucket varchar(64)`, `object_key text`, `content_type`, `size_bytes bigint`, `status varchar(16) DEFAULT 'pending'` — `pending|uploading|done|failed|source_deleted`, `attempts int`, `next_attempt_at timestamptz` (backoff clock), `drive_file_id` / `drive_folder_id` / `drive_web_view_link` / `drive_md5 text`, `last_error text`, `mirrored_at`, `created_at`, `updated_at`; UNIQUE `(bucket, object_key)`; indexes `storage_drive_mirror_due_idx (status, next_attempt_at)` + `storage_drive_mirror_status_idx`). Purely additive — touches no existing table. It is the ledger/queue behind `src/lib/storage/drive-mirror.ts`: `putObject()`/`putObjectStream()` in `s3.ts` insert a row after every S3 write and try the Drive upload inline; the 60 s in-process ticker (`startDriveMirrorTicker`) + `/api/cron/gdrive-mirror` drain what is left and re-list S3 every 6 h (`runDriveMirrorBackfill`) for objects with no row. Configured at `/admin/settings/gdrive-mirror` (`app_settings.gdrive_mirror`, ships `enabled=false`; root folder id + optional impersonation user). **Not strictly required before deploy** — the write hook catches its own error and logs `[gdrive-mirror] could not enqueue … relation does not exist`, uploads still succeed — but the admin page shows a ledger error until it is applied. Applier: `node scripts/apply-e255.mjs` (explicit `DATABASE_URL`). Verifier: `scripts/_verify-e255-gdrive-mirror.ts <folderId> [impersonateUser]`. **Applied to database-1 (= sandbox) on 2026-08-19** via `apply-e255.mjs` (16/16 columns + 3 indexes verified). database-2 (= production) NOT yet applied — run the applier with the db-2 URL. NOTE: uploads need either domain-wide delegation for `tarang-sheets@itarang.iam.gserviceaccount.com` (act as a Workspace user) or a Shared Drive — a plain My-Drive folder fails with `Service Accounts do not have storage quota`. | ☐ | ✅ | ✅ | ☐ |
| E-256_product_master_gst_defaults | **Quotation GST policy: battery 18%, charger 5%, paraphernalia 18%.** Backfills `gst_rate_pct` (and `hsn_code`: 85076000 / 85044030 / 85079090) on the three `product_master_*` tables **where NULL only**, and sets those values as column DEFAULTs so new master rows carry them. Closes the gap E-242 left open on purpose: it added the columns nullable with no backfill ("a guessed rate is a wrong number on a tax document") and no write surface was ever built, so every row stayed NULL and every quotation PDF printed "IGST % Not set" plus the unset-tax banner. On 2026-08-20 the business fixed the rates by asset type — the same values the loan calculator has always encoded (`calc_component_prices`: goods ×1.18, charger ×1.05) — so they are policy now, not guesses. **Contains DML by design** (the only E- file family exception): two UPDATEs per table, both guarded `WHERE … IS NULL`, so a rate or HSN someone later hand-sets via the product-master admin (which now exposes both fields) is never overwritten and re-running is a no-op. **NOT required for the code to work** — `DEFAULT_TAX_BY_ASSET_TYPE` in `src/lib/leads/quote-pdf/view.ts` applies the same rates as a fallback when the master row has NULL, so an unapplied environment still produces a correctly-taxed quotation; this file makes the catalogue canonical and per-product overridable. No schema.ts change (columns exist since E-242; defaults are DB-side only). Apply with `node scripts/apply-e256.mjs` (prints the target host, refuses non-NULL-guarded UPDATEs, verifies 0 unrated rows + the defaults afterwards). | ☐ | ✅ | ✅ | ☐ |
| E-257_nbfc_request_sla | **Renumbered 2026-08-21 from E-254** (main claimed E-254 first; the file, `scripts/apply-e257.mjs` and `scripts/_verify-e257-sla.ts` were renamed with it — the DDL is unchanged, so the copies already applied as E-254 need no re-run). The **NBFC request SLA** — time-boxed auto-routing of the NBFC ⇄ Admin ⇄ Dealer document-request loop. Seven columns on `nbfc_doc_requests` — `sla_due_at timestamptz` (deadline of the CURRENT leg: on `nbfc_raised` = auto-forward to dealer, on `admin_review_upload` = auto-verify uploads + push to NBFC), `forward_source` / `push_source varchar(16) DEFAULT 'admin'` (`'admin'|'system'` provenance, E-246 vocabulary), `auto_forwarded_at`, `auto_pushed_at`, `sla_failure text`, `requested_items jsonb DEFAULT '[]'` — and three on `nbfc_document_verifications` — `sla_due_at`, `forward_source DEFAULT 'admin'`, `sla_failure` — plus two **partial** indexes `nbfc_doc_requests_sla_due_idx` / `nbfc_document_verifications_sla_due_idx` (`WHERE sla_due_at IS NOT NULL`, migration-only: Drizzle indexes have no WHERE). **No backfill** — `sla_due_at` stays NULL on every existing request and the sweep skips NULL, so enabling the feature never reaches back. Backs `src/lib/nbfc/request-sla.ts` (60 s in-process ticker + `/api/cron/nbfc-request-sla`), configured at `/admin/settings/nbfc-request-sla` (`app_settings.nbfc_request_sla`, ships `enabled=false`). **REQUIRED before the code deploys** — both tables are mirrored in `schema.ts` and read with a bare `db.select()` (`listThreadForLead`, `GET /api/admin/nbfc-requests`), so without this file the admin NBFC Actions card AND the NBFC Acquire request thread fail with `column "sla_due_at" does not exist`. Applier: `node scripts/apply-e257.mjs` (explicit `DATABASE_URL` — see [[two_aws_databases_drift]]). **Applied to database-1 (= sandbox) AND database-2 (= production) on 2026-08-19** via `apply-e257.mjs`; both verified column-by-column + partial-index predicates; re-run on database-1 = no-op (10/10 present). Prod had 5 open NBFC requests, all with `sla_due_at` NULL as intended. | ✅ | ✅ | ✅ | ✅ |
| E-258_scrap_consignments | **The NBFC → iTarang scrap sale.** Three new tables — `scrap_consignments` (the deal: ref_code, status draft→submitted→negotiating→agreed→paid/rejected/withdrawn, asking/agreed rate per battery, `agreed_amount`, `last_party`, payee bank details, RazorpayX payment columns), `scrap_consignment_items` (which batteries are in the lot, snapshotted serial + `is_open`) and `scrap_consignment_offers` (append-only round log, modelled on E-238's `nbfc_offer_negotiations`) — plus one nullable column `recovery_batteries.scrap_consignment_id`. Closes the dead end at `nbfc_recovery_pipeline.stage='scrap'`: the auction sells REFURBISHED stock to dealers and excludes scrap by design (SOH < 55%), so a failed-inspection battery had no buyer and no price. **REQUIRED before the code deploys** — all three tables are mirrored in `schema.ts` and read with a bare `db.select()`, and the new column sits on `recovery_batteries`, which EXISTING screens already read, so an unapplied environment fails the battery register with `column "scrap_consignment_id" does not exist`. **Two PARTIAL indexes carry rules Drizzle cannot express**: `scrap_consignment_items_open_battery_uidx` on `(battery_id) WHERE is_open` is what makes selling the same battery twice impossible, and `scrap_consignments_open_idx` backs the admin inbox's default view — this file is their only source. Pure additive DDL, no DML, no backfill; re-running is a no-op. Apply with `node scripts/apply-e258.mjs` (prints the target host and which environment it is, refuses any non-additive statement, then verifies every table, column and index predicate out of `information_schema`/`pg_indexes`). | ☐ | ✅ | ✅ | ☐ |
| E-259_scrap_payment_terms | **When iTarang pays for a scrap lot, per NBFC.** One new table `nbfc_scrap_payment_settings` (`tenant_id` UNIQUE, `payment_timing` pre_lot\|post_lot + CHECK, `note`, `updated_by`) and two nullable columns on `scrap_consignments` — `received_at` / `received_by`, the arrival a post-lot term waits on. E-258 hard-coded the term to "pay as soon as a rate is agreed", which is a commercial decision, not a technical one; `assertPayable()` in `src/lib/nbfc/scrap/payment.ts` now reads the tenant's term and blocks BOTH money paths (RazorpayX payout and the offline record) until `received_at` is set under post_lot. **REQUIRED before the code deploys** — `received_at` is selected by the bare `db.select()` behind every scrap consignment read, so an unapplied environment fails the Scrap Purchase desk and the NBFC's Scrap Sales page with `column "received_at" does not exist`, and `getScrapPaymentTiming()` 500s on the missing table rather than falling back. **NOT backfilled on purpose**: an NBFC with no row reads as `post_lot` (the safer term) and the settings screen shows it as "default" rather than as a decision nobody took. Set the term at Settings → NBFC → Payments. Pure additive DDL, no DML; re-running is a no-op. Apply with `node scripts/apply-e259.mjs` (prints the target host and which environment it is, refuses any non-additive statement — INSERT included, so nothing can quietly backfill the default — then verifies every table, column, index and the payment_timing CHECK out of `information_schema`/`pg_indexes`/`pg_constraint`). | ☐ | ✅ | ☐ | ☐ |
| E-260_scrap_itemised_pricing | **Per-battery pricing on a scrap consignment.** Adds `scrap_consignments.pricing_mode` (`flat`\|`itemised`, default `flat`, + CHECK) and `asking_amount`, plus `scrap_consignment_items.asking_rate`. E-258 could only price a lot as one rate × `battery_count`, so an NBFC holding a clean 48V pack and a swollen 60V one had to split them into separate consignments to say so. **The negotiation now runs on the TOTAL in both modes** — `asking_amount` and the existing `scrap_consignment_offers.amount` are authoritative; `asking_rate_per_battery` is a flat-mode convenience and is NULL on an itemised lot, as is `agreed_rate_per_battery` (an average nobody agreed to would read as a price that was never named). An itemised lot is countered with one number for the pile, not a re-price of each battery; `asking_rate` is never rewritten after submission because it is the NBFC's breakdown behind its ask. **REQUIRED before the code deploys** — all three columns are read by the bare `db.select()` behind every consignment read. **Contains ONE `UPDATE`**, unlike every other E- file: it restates `asking_rate_per_battery × battery_count` into `asking_amount` for rows created before the column, which is the number those rows already negotiate on, not a new fact. Guarded to `WHERE asking_amount IS NULL AND asking_rate_per_battery IS NOT NULL`. Apply with `node scripts/apply-e260.mjs` (its additive guard permits that one UPDATE and bans everything else; verification then proves no priced consignment is left without a total). | ☐ | ✅ | ☐ | ☐ |
| E-261_scrap_offer_item_rates | **Per-battery counters in the scrap negotiation.** Adds `scrap_consignment_offers.pricing_mode` (`lot`\|`itemised`, default `lot`, + CHECK), the child table `scrap_consignment_offer_items` (offer_id → item_id → rate, the breakdown of ONE round) and `scrap_consignment_items.agreed_rate`. E-260 let the NBFC price each battery but the answer could only be one number for the pile, so iTarang could not say *which* pack it disagreed about. `pricing_mode` sits on the ROUND, not the consignment: either side may answer an itemised ask with a lot price or a flat ask battery by battery — that is the flexibility. `scrap_consignment_offers.amount` stays authoritative (an itemised round writes the SUM of its item rates there), so acceptance and payment never need to know how a round was phrased. `agreed_rate` is frozen at acceptance when the accepted round was itemised and **cleared when it was not** — a leftover breakdown would not sum to `agreed_amount`, and a wrong breakdown is worse than none. **REQUIRED before the code deploys** — `pricing_mode` and `agreed_rate` are read by the bare `db.select()` on every consignment read, and `getConsignment()` queries `scrap_consignment_offer_items` unconditionally, so an unapplied environment fails both scrap screens outright. The unique index on `(offer_id, item_id)` is load-bearing: the round's total is a SUM over these rows, so a retried write would otherwise double it. Pure additive DDL, no DML — every existing round is `lot`, which is what it was. Apply with `node scripts/apply-e261.mjs`. | ☐ | ✅ | ☐ | ☐ |
| E-262_recovery_agent_dispatch | **The physical leg between "flagged for recovery" and "on the inspection bench".** Three new tables — `nbfc_recovery_agents` (per-NBFC directory; agents are NOT iTarang users and have no login, the same stance `nbfc_fi_agents` takes), `recovery_assignments` (one dispatch: agent, single-use `link_token`, live GPS + `distance_from_address_m`, condition notes, the NBFC's review decision, and cancellation with `cancel_source` = manual|emi_payment|reassigned) and `recovery_assignment_photos` (one row per watermarked photograph, GPS + server timestamp + `watermark_applied`). Modelled field-for-field on `field_investigations` / `nbfc_fi_agents` / `field_investigation_photos` (E-148), which already solved assign → tokenised link → GPS + photo capture → reviewer decision. **REQUIRED before the code deploys** — all three are mirrored in `schema.ts` and read with a bare `db.select()`. **Three PARTIAL indexes carry rules Drizzle cannot express and this file is their only source**: `recovery_assignments_open_unique` on `(loan_sanction_id) WHERE is_current AND status IN ('assigned','in_progress','collected')` is what makes two agents at one borrower's door impossible (the service check alone is a select-then-insert race); `recovery_assignments_link_token_unique` guards the credential on an unauthenticated route (FI left its equivalent unindexed); `recovery_assignment_photos_slot_unique` on `(assignment_id, photo_type) WHERE photo_type <> 'extra'` stops the agent's auto-retry duplicating a shot. **Also adds a unique index E-232 assumed but never wrote**: `nbfc_recovery_pipeline_tenant_serial_unique` on `(tenant_id, battery_serial)` — `flagLoanForRecovery` find-or-creates on that pair and the Recovery queue maps by it, and the new assign guard reads the stage off that row, so a duplicate would make it a coin flip. Deliberately NOT wrapped in a `DO` block: if it fails the pair is not unique on that database, which is worth stopping on. Check first with `SELECT tenant_id, battery_serial, count(*) FROM nbfc_recovery_pipeline GROUP BY 1,2 HAVING count(*) > 1;` (verified 0 on database-2 before writing). Pure additive DDL, no DML, no backfill; re-running is a no-op. Ships alongside four code fixes to pre-existing defects it depends on — see the E-262 notes in `src/lib/nbfc/recovery/flag.ts` and `battery.ts`. **Applied to database-1 on 2026-08-21** via `apply-e262.mjs` — 3/3 tables, 22/22 columns, 5/5 plain indexes and all 3 partial indexes verified BY PREDICATE (an index without its WHERE enforces a different rule and would do so silently); duplicate check on `(tenant_id, battery_serial)` read 0 against the live target immediately before the write; re-run = clean no-op. | ☐ | ✅ | ☐ | ☐ |
| E-263_recovery_visit_attempts | **The doorstep nobody answered.** One new table `recovery_visit_attempts` (assignment_id → attempt_no → `outcome` not_present|refused|address_not_found|battery_missing|other, its own GPS fix + server timestamp + `distance_from_address_m`, notes, `next_visit_at`) and two cached columns on `recovery_assignments` — `next_visit_at` and `visit_attempt_count`. E-262 modelled a collection as ONE event, so an agent who drove out and found the customer absent had two options in the UI: claim a collection that did not happen, or do nothing — and "do nothing" is indistinguishable from never having left. **Append-only, one row per journey, deliberately not a set of columns on the parent**: a third visit would overwrite the second, and "we attended twice at the agreed time and nobody was there" is exactly the history a disputed repossession turns on. The two parent columns are a CACHE so the recovery queue can render without a subquery per row; the log is authoritative. **The collection token is NOT consumed by an attempt** — the agent is going back on the same job with the same link — and the service pushes `link_expires_at` out when the agreed return falls outside the window, because a link that dies before the visit it was issued for is a support call, not a security control. **REQUIRED before the code deploys**: `recovery_assignments` is read with a bare `db.select()` on the Recovery queue, so an unapplied environment fails that page with `column "next_visit_at" does not exist`. `recovery_visit_attempts_no_unique` on `(assignment_id, attempt_no)` is load-bearing — the agent's phone auto-retries a failed submit, and without it a dropped connection turns one wasted journey into two rows claiming two. `recovery_assignments_next_visit_idx` is PARTIAL (`WHERE next_visit_at IS NOT NULL`) and therefore lives only in the SQL. Pure additive DDL, no DML, no backfill — there is no history of visits to reconstruct. Apply with `node scripts/apply-e263.mjs`. **Applied to database-1 on 2026-08-21** — 1/1 table, 11/11 columns, 3/3 plain indexes and the partial index verified BY PREDICATE. | ☐ | ✅ | ☐ | ☐ |
| E-264_whatsapp_journey_foundation | **The substrate for running the whole customer journey over WhatsApp.** One new table `lead_action_tokens` (hashed, purpose-scoped, no-login magic links: `co_borrower\|step4\|offers\|step5`, 72h TTL, sha256-at-rest, nullable `consumed_at` because step4/offers links are legitimately re-openable), three columns on `whatsapp_onboarding_sessions` (`pending_prompt` jsonb, `pending_prompt_at`, `window_nudges_sent`), three on `leads` (`assignment_status` NOT NULL DEFAULT 'assigned', `dealer_assigned_at`, `dealer_assigned_by`), and five indexes. **Why `pending_prompt` exists**: a Meta template cannot carry a 10-row list, cannot carry more than three buttons, and rejects newlines in body params — so a template can never BE a step of the journey, only a doorbell. And sending one does NOT re-open the 24-hour service window; only the customer's reply does. The real interactive prompt is therefore parked here and replayed on their next inbound. **Why `assignment_status` and not a NULL `dealer_id`**: `leads.dealer_id` is nullable in DDL, but nearly every read path compares it against the caller's dealer code, so a NULL does not mean "unassigned", it means invisible to the very queue meant to rescue it — the house dealer stays in `dealer_id` as an honest holding pen and ownership is expressed by the flag. The jsonb expression index on `(context -> 'lead' ->> 'leadId')` retires the full scan that `pushSignedConsentToWhatsApp` has always done; tolerable at one Digio-webhook call site, not at five journey events per lead. `co_borrowers_lead_unique` is created inside a **guarded `DO` block** — both `requestCoBorrowerForLead()` and the coborrower POST route do find-or-insert with no constraint behind them, so a database that already has duplicate `lead_id` rows falls back to a non-unique index and raises a NOTICE rather than failing the migration. Strictly additive: nothing dropped, narrowed, or destructively backfilled; re-running is a no-op. Apply with `node scripts/apply-e264.mjs` (`--dry-run` first). **Applied to database-2 on 2026-08-24** — 1/1 table, 15/15 columns, 3/3 plain indexes, and both partial indexes verified BY PREDICATE; 38 pre-existing leads all defaulted to `assignment_status='assigned'`, so no behaviour changed. `co_borrowers_lead_unique` was created cleanly (no duplicate `lead_id` rows present). **NOT yet on database-1** — the code will fail there with `column "assignment_status" does not exist` until it is. | ✅ | ☐ | ☐ | ✅ |
| E-265_offer_negotiation_customer_party | **The customer as an author on the offer thread.** COMMENT-ONLY — no DDL, nothing breaks if it is never applied; it exists so the database describes its own vocabulary, the same way `E-245_offer_close_vocabulary` did. `nbfc_offer_negotiations.party` now admits a third value, `'customer'`: the borrower writing from their own WhatsApp chat, as opposed to a dealer countering for them from the portal. **No schema change was needed** — the column is `varchar(8)` with no CHECK (E-238 chose route-layer enforcement on purpose) and `'customer'` is exactly eight characters. What this file records is the *reasoning that is not visible from the column definitions*, and which the next person to touch this WILL otherwise get wrong: (a) `negotiation_status` deliberately does **not** gain a matching value — it is `varchar(16)` and `'customer_countered'` is nineteen characters, so it would be rejected on write; a customer's counter therefore also sets `'dealer_countered'`, read as "the BORROWER SIDE countered". The split is `negotiation_status` = whose TURN it is, `party` = who actually TYPED. Every existing reader (`can_act` on the NBFC offer panel, the "Revise offer" button, the dealer's Negotiate gate) keys off the status and needed no change, which is the whole point of doing it this way. (b) `created_by` is left NULL on a customer-authored round because a self-serve customer has no `users` row, and an invented author is worse than an honest NULL — the compliance trail lives on the matching `nbfc_audit_log` row, whose `user_id` is NOT NULL and carries the user accountable for the lead (its dealer's uploader), with `after_state.party` recording who actually spoke. Apply with `node scripts/apply-migration.mjs drizzle/E-265_offer_negotiation_customer_party.sql`; re-running is a no-op. | ☐ | ☐ | ☐ | ☐ |
| E-266_recompute_campaign_calls_made | **NO DDL — a counter re-derive, plus one `COMMENT ON COLUMN`.** Adds no column, table, index or constraint, so an unapplied E-266 is never an outage (contrast E-254, which is REQUIRED): it is only stale counters on finished campaigns. Re-derives `dialer_campaigns.calls_made` as **attempts** (`completed_leads + failed_leads`) instead of the alias of `completed_leads` it had been since the counters were first written. Two visible consequences of the old definition: the campaign detail header printed **"Calls made 71" beside "Completed 71"** on a 146-lead campaign where 75 leads had been dialled and failed — two cards, one number, one mislabelled; and the progress bar, which is `calls_made / total_eligible`, showed **0% for a campaign whose leads all failed**, during its run and for ever after (sandbox `camp_mpx…`: 25 leads, 25 failed, `status='completed'`, bar at 0%). **Cost per call is unaffected** — `cost-analytics-query.ts` divides by its own `COUNT(acl.call_id)` (`cost_calls`), never by this column; the only other readers are display counts and that progress bar. **Idempotent by construction**: it is a re-derive, not an increment, and it is guarded by `IS DISTINCT FROM` so a settled database updates zero rows. Safe to apply BEFORE the code deploys — `syncCampaignCounters()` in `src/lib/queue/campaignTracker.ts` now writes the same expression on every campaign event, which is also why this file only really matters for campaigns that will never fire another event (completed / stopped / draft); live ones heal themselves on their next webhook. A second statement zeroes campaigns that have **no** `dialer_campaign_leads` rows at all, which the main join cannot reach. Wrapped in `DO $do$ … EXCEPTION WHEN undefined_table` so a database without the dialer tables raises a notice instead of aborting. Ships with the call-duration correctness fix (`call-duration/derive.ts`): the same campaign screen was also counting `trigger_failed` leads as connected calls. Audit query: `SELECT id, calls_made, completed_leads, failed_leads FROM dialer_campaigns WHERE calls_made <> completed_leads + failed_leads;` — expect **0 rows** after applying.  **APPLIED to database-1 (= db-1 = sandbox) on 2026-08-24** — verified live by `scripts/verify-duration-histogram.ts`, whose `calls_made = completed + failed on every campaign` check went from FAIL (5+ campaigns drifting) to PASS. Still ☐ on database-2 (= local / prod). | ☐ | ✅ | ✅ | ☐ |
| E-267_ai_call_logs_transcript_turns | **One column: `ai_call_logs.transcript_turns jsonb`** (nullable, no index, no backfill). Stores the provider's conversation array VERBATIM — one object per turn carrying `{role, message}` and, when sent, `{time_in_call_secs}`. **ElevenLabs hands us those timings on every call and we have been throwing them away**: `elevenlabs/webhookHandler.ts` and `elevenlabs/getCallStatus.ts` both flatten the array to `"<speaker>: <message>"` lines the moment it arrives, and no raw payload is retained, so the timing is gone the instant the string is written. The array ALREADY travelled as far as `finalizeElevenLabsCall` (the `conversation` field of `ElevenLabsFinalizePayload`) and was dropped on the floor at the writer; this column is where it now lands. Unlocks *"how long did the dealer wait before hanging up"* — the one question of the nine on the campaign call-quality panel that stored data could not answer — and makes the flat string a derived view rather than the source of truth. **`transcript` is NOT replaced**: it is read by the transcript drawer, the Excel export, the analysis prompt and the scoring harness, and rewriting those is a separate change; both columns coexist and the string stays authoritative for every existing reader. **No backfill is possible** — the 83 historical transcripts were flattened before this column existed and their timings are unrecoverable; `call-quality/transcript.ts` falls back to parsing the string whenever the column is null, so history keeps working and only the timing metric is dark for it. ⚠ **`time_in_call_secs` is OPTIONAL in the provider's schema and had never been read by this codebase**, so whether it is actually populated could not be verified before writing — deliberately made harmless: turns are stored whatever fields they carry, the metric renders only when timings are present, and `normaliseTurns()` logs `storing N turn(s), M with timings` on every finalize so the first live call answers it. Also fixes a **two-path divergence found while wiring this**: `pollCallStatus.ts` dropped `rawTranscriptTurns` at its hand-off, so a call finalized by the POLLER would have stored no turns while the same call finalized by the WEBHOOK stored them. **No index, deliberately**, matching E-249's reasoning for `signals`: ~300 rows / 83 with a transcript, so a GIN would index a handful of documents to accelerate a two-page scan and would never be planned. Revisit at ~50k rows. **SAFE TO SKIP at deploy time, by design.** The obvious shape — mirroring the column on `aiCallLogs` in `schema.ts` — would make this REQUIRED, because Drizzle names every column of a mirrored table in its generated SQL and there are **21 `aiCallLogs` call sites including three bare `db.insert()` on the call-finalize path**; an unapplied E-267 would then fail EVERY finalized AI call. With no auto-runner and ticks known to drift, that trades one dark metric for the whole AI call-logging pipeline, so the column is deliberately **ABSENT from `schema.ts`** (the E-250/E-242/E-224/E-236 rule) and written by a guarded raw UPDATE (`persistTranscriptTurns`) that catches SQLSTATE 42703 and logs a one-line notice naming this file. An unapplied E-267 therefore costs exactly the feature that needs it. Additive and idempotent. Verify: `SELECT column_name FROM information_schema.columns WHERE table_name='ai_call_logs' AND column_name='transcript_turns';`  **APPLIED to database-1 (= db-1 = sandbox) on 2026-08-24** — verified live: `information_schema` reports `transcript_turns jsonb` nullable, and `col_description` returns this file's own COMMENT text, so it was this migration that created it. 298 logs / 0 with turns, which is the expected state until new ElevenLabs calls land. The read path flipped from `ABSENT` to `present` with no code change, exercising the `hasTranscriptTurnsColumn()` gate end to end. Still ☐ on database-2 (= local / prod). | ☐ | ✅ | ✅ | ☐ |
| E-280_drive_sales_invoices | **Four new tables so CEO revenue can be read out of Google Drive: `sales_invoices`, `sales_invoice_folders`, `sales_scan_runs`, `sales_scan_files`.** The company moved off Zoho Invoice onto **Vyapar, which has no API**, and now files sales invoices as PDFs in the shared "Tax Invoices & Expenses" Drive folder. `zoho_invoices` therefore stopped growing — verified live: **August 2026 revenue read 0** on db-1 while the Drive folder held 12 invoices for that month. This is the revenue-side mirror of E-216, which did the same for the purchase side. **SAFE TO SKIP at deploy time, by design — and that decision is the whole shape of this file.** The obvious form was a `kind` column on `drive_scan_runs`/`drive_expense_files` plus `sales_enabled` on `drive_expense_folders`. That was rejected under the E-267/E-250/E-242/E-236 rule: Drizzle names every column of a mirrored table in its generated SQL and `driveScan.ts` reads `drive_expense_folders` with a bare `db.select()`, so mirroring three new columns would have made this REQUIRED and an unapplied E-280 would fail every **EXPENSE** scan in order to add a feature beside it. Separate tables mean an unapplied E-280 costs only the sales feature: `src/lib/dashboard/revenueSource.ts` probes with `to_regclass('public.sales_invoices')` (cached 5 min, so a running app picks the migration up on its own without a pm2 reload) and falls back to Zoho-only revenue — exactly what the dashboard showed before. It also sidesteps `drive_expense_folders.drive_folder_id` being UNIQUE, which makes registering the same accounts root twice impossible, and means **`driveScan.ts` is not edited by this change at all**. **Why a new table and not rows in `zoho_invoices`** — the inverse of E-216's reasoning: there, Drive rows joined `expense_submissions` because four readers filtered on `source='ai'`; here the target is named, shaped and owned by a vendor we no longer use (`zoho_invoice_id` is NOT NULL + UNIQUE and the hourly sync upserts on it), so filing Vyapar invoices there would need a synthetic Zoho id inside the table that sync rewrites hourly. The union happens once, in `revenueSource.ts`, which all six revenue readers now go through so the card, the chart, the snapshot, both drill-downs and `/ceo/invoices` cannot drift apart. **`sales_invoices_number_key_unique` is the load-bearing index.** A full backfill was chosen over a cutover date, and the Drive tree holds BOTH eras, so `invoice_number_key` (normalised: separators folded, leading zeros stripped per segment) is the only thing standing between the backfill and double-counted revenue — the same invoice is spelled `ITD/202627/013` in Zoho, `ITD_202627_013.pdf` as a filename and `ITG/202627/041` when read back off a page whose file is named `..._41.pdf`. It is PARTIAL (`WHERE invoice_number_key IS NOT NULL`) because an invoice whose number could not be read is still kept as a `needs_attention` row and several such rows must coexist. `sales_scan_files_file_version_unique` is what makes a re-scan free (no download, no vision call for an unchanged file), and `sales_scan_runs_running_idx` is PARTIAL — it is the concurrency guard shared by the admin button, the ticker and the cron route, scoped to sales so a sales scan never blocks an expense scan. **No `balance` column, deliberately**: it is `COALESCE(total,0) - amount_paid`, derived in `revenueSource.ts`, because a PDF carries only a "Balance Due" printed at issue time and a stored copy is one more thing that can disagree with its own inputs. `organization_id` **reuses E-171's Zoho ids** (60064046518 = Delhi/ITD, 60060919257 = Haryana/ITG) so a future GROUP BY spans both sources instead of splitting one legal entity in two. `status` reuses the Zoho vocabulary for the same reason. Strictly additive: no DROP, no type narrowing, no DML, no backfill; re-running is a no-op. Apply with `node scripts/apply-e280.mjs` (`--dry-run` first) — it verifies all four partial indexes **BY PREDICATE**, and asserts the E-216 expense tables were NOT touched, which is the property that makes this skippable. **APPLIED to database-1 (= db-1 = sandbox) on 2026-09-02** — 4/4 tables, 34/34 columns, 8/8 plain indexes, 4/4 partial indexes verified by predicate, 2/2 check constraints, and the expense tables confirmed untouched. `scripts/verify-revenue-source.ts` then proved the union reports the identical figure to the old inline `zoho_invoices` queries across four windows before any Drive row existed. **APPLIED to database-2 (= prod) on 2026-09-02** — same script, same full verification: 4/4 tables, 34/34 columns, 8/8 plain and 4/4 partial indexes by predicate, 2/2 checks, expense tables untouched, 0 rows written. `verify-revenue-source.ts` run against db-2 afterwards reported `sales_invoices present: true` with every window unchanged (all-time ₹2,50,50,490), confirming the union path is live on prod and moved no number. Applied on BOTH databases. | ✅ | ✅ | ✅ | ✅ |
